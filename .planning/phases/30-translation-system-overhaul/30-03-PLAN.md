---
phase: 30-translation-system-overhaul
plan: 03
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - client/src/lib/translations.ts
  - client/src/components/admin/DashboardSection.tsx
  - client/src/components/admin/EstimatesSection.tsx
autonomous: true
requirements:
  - TRX-01
  - TRX-04
  - TRX-05

must_haves:
  truths:
    - "DashboardSection imports useTranslation and wraps all visible UI strings via t()"
    - "EstimatesSection imports useTranslation and wraps all visible UI strings via t()"
    - "All new keys added have correct Brazilian Portuguese translations in translations.ts"
    - "npm run check exits 0 after all changes"
  artifacts:
    - path: "client/src/lib/translations.ts"
      provides: "~50 new keys for DashboardSection and EstimatesSection"
      contains: "'Estimates': 'Orçamentos'"
    - path: "client/src/components/admin/DashboardSection.tsx"
      provides: "useTranslation import + t() on all visible strings"
      contains: "useTranslation"
    - path: "client/src/components/admin/EstimatesSection.tsx"
      provides: "useTranslation import + t() on all visible strings in both EstimatesSection and EstimateDialogForm"
      contains: "t('Estimates')"
  key_links:
    - from: "client/src/components/admin/DashboardSection.tsx"
      to: "client/src/lib/translations.ts"
      via: "useTranslation hook"
      pattern: "t('Total Leads')"
    - from: "client/src/components/admin/EstimatesSection.tsx"
      to: "client/src/lib/translations.ts"
      via: "useTranslation hook"
      pattern: "t('Estimates')"
---

<objective>
Add useTranslation to DashboardSection and EstimatesSection — both have ZERO t() usage today — and wrap all visible user-facing strings via t(). This is the highest-effort plan in Phase 30.

Purpose: These two components are the last major admin sections with completely untranslated strings. Plan 30-01 established type safety so every key added here is compile-verified. Runs in parallel with 30-02.

Output:
- translations.ts: ~50 new keys
- DashboardSection.tsx: full t() coverage
- EstimatesSection.tsx: full t() coverage (covers SortableServiceRow, EstimateDialogForm, and EstimatesSection)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-translation-system-overhaul/30-RESEARCH.md
@.planning/phases/30-translation-system-overhaul/30-01-SUMMARY.md

@client/src/lib/translations.ts
@client/src/components/admin/DashboardSection.tsx
@client/src/components/admin/EstimatesSection.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add all DashboardSection and EstimatesSection keys to translations.ts</name>
  <files>client/src/lib/translations.ts</files>

  <read_first>
    - client/src/lib/translations.ts (read in full after Plan 30-01 edits — find end of file to append new sections)
    - .planning/phases/30-translation-system-overhaul/30-RESEARCH.md (DashboardSection and EstimatesSection tables in Hardcoded Strings section)
  </read_first>

  <action>
Append two new comment sections to translations.ts. Add them after the last existing section (`// Presentation Viewer (Phase 20)`).

**Section 1: `// Admin — Dashboard Section (Phase 30)`**

Add these keys exactly (do not alter keys or PT values):
  'Total Leads': 'Total de Leads',
  'Leads (7 Days)': 'Leads (7 Dias)',
  'Hot Leads': 'Leads Quentes',
  'Open Chats': 'Chats Abertos',
  'Complete Forms': 'Formulários Completos',
  'In Progress': 'Em Andamento',
  'Abandoned': 'Abandonados',
  'Chat Response': 'Resposta do Chat',
  'High priority contacts': 'Contatos de alta prioridade',
  'Last 24h activity': 'Atividade nas últimas 24h',
  'Needs follow-up': 'Precisa de acompanhamento',
  'Lead Funnel': 'Funil de Leads',
  'Lead Sources': 'Fontes de Leads',
  'Chat:': 'Chat:',
  'Qualification': 'Qualificação',
  'Hot:': 'Quente:',
  'Complete:': 'Completo:',
  'Recent Leads': 'Leads Recentes',
  'View all': 'Ver todos',
  'No leads yet.': 'Nenhum lead ainda.',
  'Brand Profile': 'Perfil da Marca',
  'Primary email': 'E-mail principal',
  'Main logo': 'Logo principal',
  'Hero content': 'Conteúdo do banner',
  'Complete Company Profile': 'Completar Perfil da Empresa',
  'Integrations': 'Integrações',
  'Manage': 'Gerenciar',
  'Quick Actions': 'Ações Rápidas',
  'Edit Website': 'Editar Site',
  'Publish Content': 'Publicar Conteúdo',
  'Review Conversations': 'Revisar Conversas',
  'Qualify Leads': 'Qualificar Leads',
  'New': 'Novo',
  'Contacted': 'Contatado',
  'Qualified': 'Qualificado',
  'Converted': 'Convertido',
  'Discarded': 'Descartado',
  'Enabled': 'Habilitado',
  'Disabled': 'Desabilitado',
  'Configured': 'Configurado',
  'Disconnected': 'Desconectado',

Note: 'Company name' key — check if it already exists in the dictionary (it does NOT appear in the current dictionary). Add it here:
  'Company name': 'Nome da empresa',

**Section 2: `// Admin — Estimates Section (Phase 30)`**

Add these keys exactly:
  'Estimates': 'Orçamentos',
  'Create and manage client proposals — each generates a shareable link': 'Crie e gerencie propostas para clientes — cada uma gera um link compartilhável',
  'New Estimate': 'Novo Orçamento',
  'Edit Estimate': 'Editar Orçamento',
  'No estimates yet': 'Nenhum orçamento ainda',
  'Create your first estimate to generate a shareable proposal link.': 'Crie seu primeiro orçamento para gerar um link de proposta compartilhável.',
  'Contact name': 'Nome do contato',
  'At least one of company or contact name is required.': 'Pelo menos um entre nome da empresa ou do contato é obrigatório.',
  'Note (optional)': 'Nota (opcional)',
  'Access code (optional)': 'Código de acesso (opcional)',
  'Leave blank to disable gate': 'Deixe em branco para desativar a proteção',
  'Add from catalog': 'Adicionar do catálogo',
  'Add custom row': 'Adicionar linha personalizada',
  'Save Changes': 'Salvar Alterações',
  'Create Estimate': 'Criar Orçamento',
  'Estimate created': 'Orçamento criado',
  'Failed to create estimate': 'Falha ao criar orçamento',
  'Estimate updated': 'Orçamento atualizado',
  'Failed to update estimate': 'Falha ao atualizar orçamento',
  'Estimate deleted': 'Orçamento excluído',
  'Failed to delete estimate': 'Falha ao excluir orçamento',
  'Access code updated': 'Código de acesso atualizado',
  'Failed to update access code': 'Falha ao atualizar o código de acesso',
  'Slug updated': 'Slug atualizado',
  'Failed to update slug': 'Falha ao atualizar o slug',
  'Delete estimate?': 'Excluir orçamento?',
  'Password Protection': 'Proteção por Senha',
  'Leave blank to remove password protection': 'Deixe em branco para remover a proteção por senha',

Total new keys across both sections: ~51 keys.

Do NOT modify any existing key. The file size will grow toward ~550-580 lines total — well within the 600-line project limit.
  </action>

  <verify>
    <automated>grep "'Estimates': 'Orçamentos'" client/src/lib/translations.ts</automated>
    <automated>grep "'Total Leads': 'Total de Leads'" client/src/lib/translations.ts</automated>
    <automated>grep "'New Estimate': 'Novo Orçamento'" client/src/lib/translations.ts</automated>
    <automated>grep "'Contacted': 'Contatado'" client/src/lib/translations.ts</automated>
    <automated>npm run check</automated>
  </verify>

  <acceptance_criteria>
    - translations.ts contains: `'Estimates': 'Orçamentos',`
    - translations.ts contains: `'Total Leads': 'Total de Leads',`
    - translations.ts contains: `'New Estimate': 'Novo Orçamento',`
    - translations.ts contains: `'Contacted': 'Contatado',`
    - translations.ts contains: `'Delete estimate?': 'Excluir orçamento?',`
    - translations.ts contains: `'Password Protection': 'Proteção por Senha',`
    - translations.ts contains: `'Company name': 'Nome da empresa',`
    - translations.ts contains: `'Qualified': 'Qualificado',`
    - npm run check exits 0
  </acceptance_criteria>

  <done>All ~51 new keys for DashboardSection and EstimatesSection added to translations.ts; npm run check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add useTranslation to DashboardSection and wrap all visible strings</name>
  <files>client/src/components/admin/DashboardSection.tsx</files>

  <read_first>
    - client/src/components/admin/DashboardSection.tsx (read in full — this file has ZERO useTranslation usage; identify the funnelStages array, integration status labels, and all hardcoded UI strings)
  </read_first>

  <action>
DashboardSection.tsx has zero t() usage. Apply this pattern:

1. Add import at the top of the file (after existing imports):
   `import { useTranslation } from '@/hooks/useTranslation';`

2. Inside the exported component function, add after the first existing hook call:
   `const { t } = useTranslation();`

3. The `funnelStages` array is CRITICAL. It is almost certainly defined at module level (outside the component). Move it INSIDE the component function body (after the `const { t }` line) so it can call t(). Then wrap each label:
   - `{ label: 'New', ... }` → `{ label: t('New'), ... }`
   - `{ label: 'Contacted', ... }` → `{ label: t('Contacted'), ... }`
   - `{ label: 'Qualified', ... }` → `{ label: t('Qualified'), ... }`
   - `{ label: 'Converted', ... }` → `{ label: t('Converted'), ... }`
   - `{ label: 'Discarded', ... }` → `{ label: t('Discarded'), ... }`

4. Wrap ALL visible hardcoded strings in JSX with t(). Based on the research audit, these include:
   - 'Total Leads', 'Leads (7 Days)', 'Hot Leads', 'Open Chats' (stat card labels)
   - 'Complete Forms', 'In Progress', 'Abandoned' (form stat labels)
   - 'Chat Response', 'High priority contacts', 'Last 24h activity', 'Needs follow-up' (other stat helpers)
   - 'Lead Funnel' (h3 heading)
   - 'Lead Sources' (h3 heading)
   - 'Chat:' (source label — note 'Form:' is already in the dictionary)
   - 'Qualification', 'Hot:', 'Complete:' (qualification section)
   - 'Recent Leads' (h3 heading)
   - 'View all' (link text)
   - 'No leads yet.' (empty state)
   - 'Brand Profile' (h3 heading)
   - 'Company name' (profile check label)
   - 'Primary email', 'Main logo', 'Hero content' (profile check labels)
   - 'Complete Company Profile' (button text)
   - 'Integrations' (h3 heading)
   - 'Manage' (button/link text)
   - 'Quick Actions' (h3 heading)
   - 'Edit Website', 'Publish Content', 'Review Conversations', 'Qualify Leads' (action button labels)
   - Integration status values 'Enabled', 'Disabled', 'Configured', 'Disconnected'

5. Leave dynamic data strings unwrapped: lead names, email addresses, phone numbers, date values, percentage strings like `${completion}%`.

6. Leave already-in-dictionary strings that are used via hardcoded JSX as-is if they already have t() wrappers. Strings like 'Form:' which IS in the dictionary — wrap it: `t('Form:')`.

After all changes, run `npm run check` and fix any type errors.
  </action>

  <verify>
    <automated>grep "useTranslation" client/src/components/admin/DashboardSection.tsx</automated>
    <automated>grep "t('Total Leads')" client/src/components/admin/DashboardSection.tsx</automated>
    <automated>grep "t('Lead Funnel')" client/src/components/admin/DashboardSection.tsx</automated>
    <automated>grep "t('Contacted')" client/src/components/admin/DashboardSection.tsx</automated>
    <automated>npm run check</automated>
  </verify>

  <acceptance_criteria>
    - DashboardSection.tsx contains `import { useTranslation } from '@/hooks/useTranslation';`
    - DashboardSection.tsx contains `const { t } = useTranslation();`
    - DashboardSection.tsx contains `t('Total Leads')`
    - DashboardSection.tsx contains `t('Lead Funnel')`
    - DashboardSection.tsx contains `t('Contacted')` (inside the component body, not at module level)
    - DashboardSection.tsx does NOT contain `funnelStages` defined at module level with hardcoded string labels — the array must be inside the component function
    - `grep -c "t('" client/src/components/admin/DashboardSection.tsx` returns a number greater than 20 — confirms full coverage, not partial wrapping
    - npm run check exits 0
  </acceptance_criteria>

  <done>DashboardSection.tsx has full t() coverage; funnelStages array moved inside component; npm run check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add useTranslation to EstimatesSection and wrap all visible strings</name>
  <files>client/src/components/admin/EstimatesSection.tsx</files>

  <read_first>
    - client/src/components/admin/EstimatesSection.tsx (read in full — this file has ZERO useTranslation usage; it co-locates SortableServiceRow, EstimateDialogForm, and EstimatesSection in one file per the CLAUDE.md admin design pattern)
    - .planning/phases/30-translation-system-overhaul/30-RESEARCH.md (EstimatesSection table in Hardcoded Strings — lists file:line for every string)
  </read_first>

  <action>
EstimatesSection.tsx has zero t() usage across all three co-located components. Apply useTranslation to EACH component that renders user-facing strings.

1. Add ONE import at the top of the file:
   `import { useTranslation } from '@/hooks/useTranslation';`

2. In the `EstimateDialogForm` component function, add after existing hooks:
   `const { t } = useTranslation();`

3. In the main `EstimatesSection` component function, add after existing hooks:
   `const { t } = useTranslation();`

4. In `SortableServiceRow` component (if it renders any visible user-facing text), add the same if needed.

5. Wrap ALL hardcoded visible strings with t(). Full list from research (exact key strings to use):

   In EstimateDialogForm:
   - DialogTitle: `t('Edit Estimate')` or `t('New Estimate')` (conditional on mode)
   - Label 'Company name' → `t('Company name')`
   - Label 'Contact name' → `t('Contact name')`
   - Validation message → `t('At least one of company or contact name is required.')`
   - Label 'Note (optional)' → `t('Note (optional)')`
   - Label 'Access code (optional)' → `t('Access code (optional)')`
   - Helper text 'Leave blank to disable gate' → `t('Leave blank to disable gate')`
   - Section header span 'Services' → `t('Services')` (key already in dict)
   - Button 'Add from catalog' → `t('Add from catalog')`
   - Button 'Add custom row' → `t('Add custom row')`
   - Button 'Cancel' → `t('Cancel')` (key already in dict)
   - Button 'Save Changes' / 'Create Estimate' → `t('Save Changes')` / `t('Create Estimate')`

   In EstimatesSection (main component):
   - SectionHeader title → `t('Estimates')`
   - SectionHeader description → `t('Create and manage client proposals — each generates a shareable link')`
   - Button 'New Estimate' → `t('New Estimate')`
   - Empty state 'No estimates yet' → `t('No estimates yet')`
   - Empty state description → `t('Create your first estimate to generate a shareable proposal link.')`
   - AlertDialogTitle 'Delete estimate?' → `t('Delete estimate?')`
   - Buttons 'Cancel', 'Delete' → `t('Cancel')`, `t('Delete')` (both already in dict)
   - DialogTitle 'Password Protection' → `t('Password Protection')`
   - Label 'Access code (optional)' → `t('Access code (optional)')`
   - Helper text 'Leave blank to remove password protection' → `t('Leave blank to remove password protection')`
   - Buttons 'Cancel', 'Save' → `t('Cancel')`, `t('Save')` (both in dict)

   Toast messages (in mutation callbacks):
   - 'Estimate created' → `t('Estimate created')`
   - 'Failed to create estimate' → `t('Failed to create estimate')`
   - 'Estimate updated' → `t('Estimate updated')`
   - 'Failed to update estimate' → `t('Failed to update estimate')`
   - 'Estimate deleted' → `t('Estimate deleted')`
   - 'Failed to delete estimate' → `t('Failed to delete estimate')`
   - 'Access code updated' → `t('Access code updated')`
   - 'Failed to update access code' → `t('Failed to update access code')`
   - 'Slug updated' → `t('Slug updated')`
   - 'Failed to update slug' → `t('Failed to update slug')`

6. Leave dynamic template strings (e.g., `Are you sure you want to delete estimate for ${name}?`) as-is unless the surrounding static parts are in the dictionary. Do NOT break template literal logic.

7. After all changes, run `npm run check` and fix any type errors. All these keys were added in Task 1.
  </action>

  <verify>
    <automated>grep "useTranslation" client/src/components/admin/EstimatesSection.tsx</automated>
    <automated>grep "t('Estimates')" client/src/components/admin/EstimatesSection.tsx</automated>
    <automated>grep "t('New Estimate')" client/src/components/admin/EstimatesSection.tsx</automated>
    <automated>grep "t('Estimate created')" client/src/components/admin/EstimatesSection.tsx</automated>
    <automated>grep "t('Delete estimate?')" client/src/components/admin/EstimatesSection.tsx</automated>
    <automated>npm run check</automated>
  </verify>

  <acceptance_criteria>
    - EstimatesSection.tsx contains `import { useTranslation } from '@/hooks/useTranslation';`
    - EstimatesSection.tsx contains `t('Estimates')` (section header)
    - EstimatesSection.tsx contains `t('New Estimate')` (button)
    - EstimatesSection.tsx contains `t('Estimate created')` (toast)
    - EstimatesSection.tsx contains `t('Delete estimate?')` (alert dialog)
    - EstimatesSection.tsx contains `t('Contact name')` (label in form)
    - EstimatesSection.tsx contains `t('Password Protection')` (dialog title)
    - EstimatesSection.tsx does NOT use `useTranslation` more than twice (two component functions need it)
    - npm run check exits 0
  </acceptance_criteria>

  <done>EstimatesSection.tsx has full t() coverage across all co-located components; npm run check passes.</done>
</task>

</tasks>

<verification>
Run npm run check — must exit 0.

Spot-check key coverage:
  grep -c "t('" client/src/components/admin/DashboardSection.tsx  # should be > 20
  grep -c "t('" client/src/components/admin/EstimatesSection.tsx  # should be > 30
</verification>

<success_criteria>
- TRX-01: All t() calls in DashboardSection and EstimatesSection map to dictionary keys
- TRX-04: DashboardSection and EstimatesSection have no visible hardcoded strings outside t()
- TRX-05: Placeholder 'Contact name', 'Company name' use t(); 'Services' label uses t()
- npm run check exits 0 (TRX-09)
</success_criteria>

<output>
After completion, create `.planning/phases/30-translation-system-overhaul/30-03-SUMMARY.md`
</output>

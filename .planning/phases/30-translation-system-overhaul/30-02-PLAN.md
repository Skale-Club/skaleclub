---
phase: 30-translation-system-overhaul
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - client/src/lib/translations.ts
  - client/src/components/admin/PresentationsSection.tsx
  - client/src/components/admin/LeadsSection.tsx
  - client/src/components/admin/SEOSection.tsx
  - client/src/components/admin/NewFormDialog.tsx
  - client/src/components/admin/LinksSection.tsx
autonomous: true
requirements:
  - TRX-01
  - TRX-02
  - TRX-03
  - TRX-04
  - TRX-05

must_haves:
  truths:
    - "PresentationsSection has zero t() calls referencing keys absent from translations.ts"
    - "LeadsSection section header renders in Portuguese when PT language is active"
    - "SEO page slug labels render localized text in PT mode"
    - "New Form dialog fields render localized placeholder text in PT mode"
    - "LinksSection link title placeholder renders in Portuguese when PT is active"
    - "All four 'Back to X' key variants are present in translations.ts (TRX-03)"
    - "npm run check exits 0 after all changes"
  artifacts:
    - path: "client/src/lib/translations.ts"
      provides: "New keys for PresentationsSection (3), LeadsSection (2), LinksSection (1), NewFormDialog (1)"
      contains: "'Search presentations...'"
    - path: "client/src/components/admin/LeadsSection.tsx"
      provides: "useTranslation import + t() on all visible strings"
      contains: "useTranslation"
    - path: "client/src/components/admin/NewFormDialog.tsx"
      provides: "useTranslation import + t() on placeholder"
      contains: "t('e.g. Contact Us')"
  key_links:
    - from: "client/src/components/admin/PresentationsSection.tsx"
      to: "client/src/lib/translations.ts"
      via: "useTranslation hook"
      pattern: "t('Create')"
    - from: "client/src/components/admin/LinksSection.tsx"
      to: "client/src/lib/translations.ts"
      via: "useTranslation hook"
      pattern: "t('My Portfolio')"
---

<objective>
Add the missing translation keys for PresentationsSection, LeadsSection, SEOSection, NewFormDialog, and LinksSection. Wire t() calls into the three components that currently have none.

Purpose: Plan 30-01 established type safety. This plan fills the key gaps for the admin components listed in TRX-02 / TRX-04 / TRX-05. Runs in parallel with 30-03.

Output:
- translations.ts: 7 new keys added
- PresentationsSection.tsx: 3 missing keys now resolve
- LeadsSection.tsx: useTranslation imported, visible strings wrapped
- SEOSection.tsx: 'Contact' label wrapped in t()
- NewFormDialog.tsx: placeholder wrapped in t()
- LinksSection.tsx: 'My Portfolio' placeholder uses t()
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-translation-system-overhaul/30-RESEARCH.md
@.planning/phases/30-translation-system-overhaul/30-01-SUMMARY.md

@client/src/lib/translations.ts
@client/src/components/admin/PresentationsSection.tsx
@client/src/components/admin/LeadsSection.tsx
@client/src/components/admin/SEOSection.tsx
@client/src/components/admin/NewFormDialog.tsx
@client/src/components/admin/LinksSection.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add 7 missing keys to translations.ts</name>
  <files>client/src/lib/translations.ts</files>

  <read_first>
    - client/src/lib/translations.ts (read the current file after Plan 30-01 edits — the Presentations Section and Links Page sections are the insertion targets)
  </read_first>

  <action>
Add the following keys to client/src/lib/translations.ts in their logical sections.

Under `// Admin — Presentations Section (Phase 19)`, append these 3 keys:
  'Create': 'Criar',
  'Search presentations...': 'Pesquisar apresentações...',
  'Failed to create presentation': 'Falha ao criar apresentação',

Under `// Admin — Links Page (Phase 12)`, append this 1 key after the existing 'Drag to reorder' entry:
  'My Portfolio': 'Meu Portfólio',

Create a new comment section `// Admin — Leads Section (Phase 30)` and add 2 keys:
  'Leads': 'Leads',
  'All captured leads with ratings and follow-up status': 'Todos os leads capturados com classificações e status de acompanhamento',

Create a new comment section `// Admin — Forms / SEO (Phase 30)` and add 1 key:
  'e.g. Contact Us': 'ex. Fale Conosco',

Total new keys: 7. Do NOT modify any existing key or value.
  </action>

  <verify>
    <automated>grep "'Create': 'Criar'" client/src/lib/translations.ts</automated>
    <automated>grep "'Search presentations...'" client/src/lib/translations.ts</automated>
    <automated>grep "'My Portfolio'" client/src/lib/translations.ts</automated>
    <automated>grep "'e.g. Contact Us'" client/src/lib/translations.ts</automated>
    <automated>npm run check</automated>
  </verify>

  <acceptance_criteria>
    - translations.ts contains: `'Create': 'Criar',`
    - translations.ts contains: `'Search presentations...': 'Pesquisar apresentações...',`
    - translations.ts contains: `'Failed to create presentation': 'Falha ao criar apresentação',`
    - translations.ts contains: `'My Portfolio': 'Meu Portfólio',`
    - translations.ts contains: `'Leads': 'Leads',`
    - translations.ts contains: `'All captured leads with ratings and follow-up status': 'Todos os leads capturados com classificações e status de acompanhamento',`
    - translations.ts contains: `'e.g. Contact Us': 'ex. Fale Conosco',`
    - npm run check exits 0
  </acceptance_criteria>

  <done>7 new keys added to translations.ts; npm run check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix PresentationsSection — the 3 missing keys now resolve</name>
  <files>client/src/components/admin/PresentationsSection.tsx</files>

  <read_first>
    - client/src/components/admin/PresentationsSection.tsx (read in full to confirm it already imports useTranslation and confirm the exact lines calling t('Create'), t('Search presentations...'), t('Failed to create presentation'))
  </read_first>

  <action>
PresentationsSection.tsx already imports useTranslation and calls t(). After Task 1 added the 3 keys to translations.ts, these calls now resolve statically.

Verify the file contains all three t() calls:
  t('Create')  — in the Dialog footer submit button (line ~767)
  t('Search presentations...')  — in the search Input placeholder (line ~494)
  t('Failed to create presentation')  — in the createMutation onError callback

If any of these exact strings is spelled differently, correct it to match the key exactly as added to translations.ts.

No import changes needed — the file already uses useTranslation. This task is primarily verification but may require correcting a string literal if there is a mismatch.

After any edits (or confirming no edits needed), run `npm run check`.
  </action>

  <verify>
    <automated>grep "t('Create')" client/src/components/admin/PresentationsSection.tsx</automated>
    <automated>grep "t('Search presentations...')" client/src/components/admin/PresentationsSection.tsx</automated>
    <automated>grep "t('Failed to create presentation')" client/src/components/admin/PresentationsSection.tsx</automated>
    <automated>npm run check</automated>
  </verify>

  <acceptance_criteria>
    - PresentationsSection.tsx contains `t('Create')`
    - PresentationsSection.tsx contains `t('Search presentations...')`
    - PresentationsSection.tsx contains `t('Failed to create presentation')`
    - npm run check exits 0
  </acceptance_criteria>

  <done>PresentationsSection.tsx has all 3 previously-missing keys resolved via the dictionary.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add useTranslation to LeadsSection, SEOSection, NewFormDialog + fix LinksSection placeholder</name>
  <files>
    client/src/components/admin/LeadsSection.tsx,
    client/src/components/admin/SEOSection.tsx,
    client/src/components/admin/NewFormDialog.tsx,
    client/src/components/admin/LinksSection.tsx
  </files>

  <read_first>
    - client/src/components/admin/LeadsSection.tsx (read in full — confirm zero useTranslation usage; find SectionHeader title/description)
    - client/src/components/admin/SEOSection.tsx (read in full — find PAGE_SLUG_FIELDS array with hardcoded 'Contact' label)
    - client/src/components/admin/NewFormDialog.tsx (read in full — find placeholder="e.g. Contact Us" at line ~123)
    - client/src/components/admin/LinksSection.tsx (read in full — already uses useTranslation; find placeholder="My Portfolio" at line ~152)
  </read_first>

  <action>
**LeadsSection.tsx:**
1. Add import at top: `import { useTranslation } from '@/hooks/useTranslation';`
2. Inside the component function, add after existing hooks: `const { t } = useTranslation();`
3. Locate the SectionHeader component call (search for `<SectionHeader`). Update its props:
   - Change title prop value to: `{t('Leads')}`
   - Change description prop value to: `{t('All captured leads with ratings and follow-up status')}`
4. These are the minimum TRX-04 strings to wrap for this component per the research findings. Do NOT wrap dynamic data strings (status values, lead names, etc.) in this task — that is out of scope.

**SEOSection.tsx:**
1. Add import at top: `import { useTranslation } from '@/hooks/useTranslation';`
2. Inside the component function, add after existing hooks: `const { t } = useTranslation();`
3. Find the PAGE_SLUG_FIELDS array definition. It contains an object with `{ key: 'contact', label: 'Contact', ... }`. The 'Contact' key is already in translations.ts.
4. IMPORTANT: PAGE_SLUG_FIELDS is likely defined at module level (outside the component). Move it inside the component function body so it can access `t`. Then change `label: 'Contact'` to `label: t('Contact')`.
5. If other PAGE_SLUG_FIELDS entries have hardcoded label strings that exist in the dictionary, wrap those too. Only wrap labels that correspond to existing dictionary keys.

**NewFormDialog.tsx:**
1. Add import at top: `import { useTranslation } from '@/hooks/useTranslation';`
2. Inside the component function, add after existing hooks: `const { t } = useTranslation();`
3. Find `placeholder="e.g. Contact Us"` and change to `placeholder={t('e.g. Contact Us')}`
4. The dialog title and description strings ('New Form', 'Name the form and pick a URL slug...') are also hardcoded. Wrap them with t() too if they have dictionary keys. Check: 'New Form' IS in the dictionary. 'Name the form and pick a URL slug. You\'ll add questions in the next step.' IS in the dictionary. Wrap both.

**LinksSection.tsx:**
1. The file already imports and uses useTranslation — do NOT add a second import.
2. Find `placeholder="My Portfolio"` (line ~152) and change to `placeholder={t('My Portfolio')}`
3. Also find any other hardcoded Label strings ('Visible', 'Link Title', 'Destination URL') that are already in the dictionary but not yet wrapped. Wrap those with t() as well.

After all changes, run `npm run check`. Fix any TypeScript errors.
  </action>

  <verify>
    <automated>grep "useTranslation" client/src/components/admin/LeadsSection.tsx</automated>
    <automated>grep "t('Leads')" client/src/components/admin/LeadsSection.tsx</automated>
    <automated>grep "useTranslation" client/src/components/admin/SEOSection.tsx</automated>
    <automated>grep "t('Contact')" client/src/components/admin/SEOSection.tsx</automated>
    <automated>grep "t('e.g. Contact Us')" client/src/components/admin/NewFormDialog.tsx</automated>
    <automated>grep "t('My Portfolio')" client/src/components/admin/LinksSection.tsx</automated>
    <automated>npm run check</automated>
  </verify>

  <acceptance_criteria>
    - LeadsSection.tsx contains `import { useTranslation } from '@/hooks/useTranslation';`
    - LeadsSection.tsx contains `t('Leads')`
    - LeadsSection.tsx contains `t('All captured leads with ratings and follow-up status')`
    - SEOSection.tsx contains `import { useTranslation } from '@/hooks/useTranslation';`
    - SEOSection.tsx contains `t('Contact')`
    - NewFormDialog.tsx contains `import { useTranslation } from '@/hooks/useTranslation';`
    - NewFormDialog.tsx contains `t('e.g. Contact Us')`
    - NewFormDialog.tsx contains `t('New Form')`
    - LinksSection.tsx contains `t('My Portfolio')`
    - `npm run check` exits 0
  </acceptance_criteria>

  <done>Four admin components now use t() for their visible strings; npm run check passes with zero errors.</done>
</task>

</tasks>

<verification>
Run npm run check — must exit 0.

Spot-check grep commands:
  grep "t('Create')" client/src/components/admin/PresentationsSection.tsx
  grep "t('My Portfolio')" client/src/components/admin/LinksSection.tsx
  grep "t('e.g. Contact Us')" client/src/components/admin/NewFormDialog.tsx
  grep "'Create': 'Criar'" client/src/lib/translations.ts
</verification>

<success_criteria>
- TRX-01: PresentationsSection t() calls all resolve to dictionary keys
- TRX-02: PresentationsSection, LeadsSection, SEOSection, NewFormDialog, LinksSection covered
- TRX-03: All 4 'Back to X' variants preserved — grep `translations.ts` for 'Back to Home', 'Back to homepage', 'Back to presentations', 'Back to website' returns 4 matches (these are NOT removed by Plan 30-01 dead-key sweep)
- TRX-04: LeadsSection, SEOSection, NewFormDialog, LinksSection section headers / labels wrapped
- TRX-05: Placeholder 'e.g. Contact Us' and 'My Portfolio' use t()
- npm run check exits 0 (TRX-09)
</success_criteria>

<output>
After completion, create `.planning/phases/30-translation-system-overhaul/30-02-SUMMARY.md`
</output>

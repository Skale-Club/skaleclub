---
phase: 30-translation-system-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/lib/translations.ts
  - client/src/hooks/useTranslation.ts
  - client/src/pages/not-found.tsx
autonomous: true
requirements:
  - TRX-07
  - TRX-08
  - TRX-09
  - TRX-10

must_haves:
  truths:
    - "TypeScript compile step rejects any t() call whose key is absent from translations.ts"
    - "not-found.tsx renders the correct heading and paragraph copy via valid keys"
    - "translations.ts contains zero duplicate keys"
    - "npm run check exits 0 after all changes in this plan"
  artifacts:
    - path: "client/src/hooks/useTranslation.ts"
      provides: "t() with parameter typed as TranslationKey"
      contains: "text: TranslationKey"
    - path: "client/src/lib/translations.ts"
      provides: "Static PT dictionary with keys 'Page Not Found' and the correct 404 paragraph"
      contains: "'Page Not Found'"
    - path: "client/src/pages/not-found.tsx"
      provides: "404 page using correct dictionary keys"
      contains: "t('Page Not Found')"
  key_links:
    - from: "client/src/hooks/useTranslation.ts"
      to: "client/src/lib/translations.ts"
      via: "TranslationKey import"
      pattern: "type TranslationKey"
---

<objective>
Establish the TypeScript type-safety foundation for the translation system.

Purpose: Once t() is typed as TranslationKey, every subsequent plan's t() additions are compile-time verified. This plan must ship first because plans 30-02 and 30-03 depend on this type being tight — any missing key they add will be caught immediately by npm run check.

Output:
- translations.ts: dead keys removed, correct 404 keys added
- useTranslation.ts: t() parameter typed as TranslationKey (TRX-08)
- not-found.tsx: uses the correct key strings (TRX-10)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-translation-system-overhaul/30-RESEARCH.md

@client/src/lib/translations.ts
@client/src/hooks/useTranslation.ts
@client/src/pages/not-found.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove dead keys and add correct 404 keys to translations.ts</name>
  <files>client/src/lib/translations.ts</files>

  <read_first>
    - client/src/lib/translations.ts (current 321-key dictionary — read in full before editing)
    - .planning/phases/30-translation-system-overhaul/30-RESEARCH.md (Unused Keys section)
  </read_first>

  <action>
Edit client/src/lib/translations.ts with these two changes:

**REMOVE** the following keys from the `translations.pt` object (confirmed unused — no t() call references them anywhere in client/src):

Under `// Booking` section — remove all 11 keys:
  'Select Service', 'Select Date', 'Select Time', 'Your Information', 'Booking Summary', 'Total', 'Subtotal', 'Tax', 'Confirm Booking', 'Booking Confirmed', 'Thank you for your booking!'

Under `// Hero Section` — remove 1 key:
  'Schedule Free Consultation'
  (keep 'Schedule a Free Call' — that one IS used via placeholder attributes in admin components)

Under `// 404 Page` section — remove both existing wrong keys:
  '404 Page Not Found', 'The page you are looking for does not exist.'

Under `// Portfolio Page` section — remove 3 unused keys:
  'Open tool', 'Open tool in new tab', 'We help companies achieve unprecedented growth through modern marketing systems.'

Under `// Admin — Links Page (Phase 12)` section — remove 1 stale key:
  'Live preview coming in Phase 13'

**ADD** the following new keys. Place them in a new comment block `// 404 Page` replacing the old one:
  'Page Not Found': 'Página Não Encontrada',
  'The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.': 'A página que você procura pode ter sido removida, teve seu nome alterado ou está temporariamente indisponível.',

Total keys removed: 18. Total keys added: 2. Net change: -16 keys (from 321 to ~305).

Do NOT touch any other keys. Do NOT reorder sections.
  </action>

  <verify>
    <automated>grep "'Page Not Found'" client/src/lib/translations.ts</automated>
    <automated>grep "'404 Page Not Found'" client/src/lib/translations.ts | wc -l</automated>
    <automated>grep "'Select Service'\|'Confirm Booking'\|'Select Date'" client/src/lib/translations.ts | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - translations.ts contains: `'Page Not Found': 'Página Não Encontrada',`
    - translations.ts contains: `'The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.': 'A página que você procura pode ter sido removida, teve seu nome alterado ou está temporariamente indisponível.',`
    - grep for `'404 Page Not Found'` in translations.ts returns 0 matches
    - grep for `'Select Service'` in translations.ts returns 0 matches
    - grep for `'Confirm Booking'` in translations.ts returns 0 matches
    - grep for `'Open tool'` in translations.ts returns 0 matches
    - grep for `'Live preview coming in Phase 13'` in translations.ts returns 0 matches
    - grep for `'Schedule Free Consultation'` in translations.ts returns 0 matches (the key 'Schedule a Free Call' must remain)
  </acceptance_criteria>

  <done>translations.ts has 18 dead keys removed and the 2 correct 404 keys added; file has no duplicate keys.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tighten t() to TranslationKey type in useTranslation.ts</name>
  <files>client/src/hooks/useTranslation.ts</files>

  <read_first>
    - client/src/hooks/useTranslation.ts (read in full — must see current line 3 import and line 117 t() signature)
    - client/src/lib/translations.ts (already modified by Task 1 — the TranslationKey type now reflects the reduced dictionary)
  </read_first>

  <action>
Make exactly two edits to client/src/hooks/useTranslation.ts:

**Edit 1 — line 3, update the import:**
BEFORE:
  import { translations as staticTranslations } from '@/lib/translations';
AFTER:
  import { translations as staticTranslations, type TranslationKey } from '@/lib/translations';

**Edit 2 — line 117, tighten t() parameter type:**
BEFORE:
  const t = useCallback((text: string): string => {
AFTER:
  const t = useCallback((text: TranslationKey): string => {

**Edit 3 — line 131, remove the cast (now unnecessary):**
BEFORE:
  const staticValue = staticTranslations.pt[text as keyof typeof staticTranslations.pt];
AFTER:
  const staticValue = staticTranslations.pt[text];

These are the only changes. Do NOT modify any other logic in the file.

Note: The EN pass-through `if (language === 'en' || !text) return text;` still works because TranslationKey extends string and is assignable to string for return purposes.
  </action>

  <verify>
    <automated>grep "text: TranslationKey" client/src/hooks/useTranslation.ts</automated>
    <automated>grep "type TranslationKey" client/src/hooks/useTranslation.ts</automated>
  </verify>

  <acceptance_criteria>
    - useTranslation.ts line containing the t() callback reads: `const t = useCallback((text: TranslationKey): string => {`
    - useTranslation.ts import line reads: `import { translations as staticTranslations, type TranslationKey } from '@/lib/translations';`
    - `staticTranslations.pt[text as keyof typeof staticTranslations.pt]` is gone — replaced with `staticTranslations.pt[text]`
    - npm run check exits 0 (run this to verify — if it fails, a component is calling t() with a string not in the dictionary, which means Task 1 missed adding a key or removed a key that IS in use)
  </acceptance_criteria>

  <done>useTranslation.ts t() parameter is typed as TranslationKey; npm run check passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fix not-found.tsx to use correct key strings</name>
  <files>client/src/pages/not-found.tsx</files>

  <read_first>
    - client/src/pages/not-found.tsx (read in full — confirm current t() calls at lines 25 and 29)
    - client/src/lib/translations.ts (confirm 'Page Not Found' key is now present from Task 1)
  </read_first>

  <action>
The file currently calls:
  t('Page Not Found')  — line 25
  t('The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.')  — line 29

After Task 1, these keys ARE in translations.ts. The file does NOT need key changes — the keys are correct. However, if for some reason the file had been changed to use the old wrong keys during a prior edit, verify and restore.

Confirm the file contains EXACTLY these two t() calls:
  {t('Page Not Found')}  on line 25 (inside the h2 element)
  {t('The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.')}  on line 29 (inside the p element)

If either call uses a different string (e.g. '404 Page Not Found'), update it to match the exact key strings above.

After confirming, run `npm run check` to verify the full TypeScript build passes with the tightened type. Fix any errors that appear.
  </action>

  <verify>
    <automated>grep "t('Page Not Found')" client/src/pages/not-found.tsx</automated>
    <automated>npm run check</automated>
  </verify>

  <acceptance_criteria>
    - not-found.tsx contains: `{t('Page Not Found')}`
    - not-found.tsx contains: `{t('The page you are looking for might have been removed, had its name changed, or is temporarily unavailable.')}`
    - not-found.tsx does NOT contain `t('404 Page Not Found')`
    - `npm run check` exits 0 with zero TypeScript errors
  </acceptance_criteria>

  <done>not-found.tsx uses the correct keys; npm run check is green across the entire codebase.</done>
</task>

</tasks>

<verification>
After all three tasks, run:
  npm run check

Expected: exits 0 with zero errors.

If check fails after tightening t() type:
  - The error message will name the exact file and key string that is missing
  - Add that key to translations.ts and re-run check
  - Do NOT revert the type tightening

Also verify key removal was safe:
  grep -r "t('Schedule Free Consultation')" client/src — should return 0 results
  grep -r "t('Select Service')" client/src — should return 0 results
</verification>

<success_criteria>
- translations.ts has 18 dead keys removed, 2 correct 404 keys added
- useTranslation.ts t() parameter is TranslationKey (not string)
- not-found.tsx uses 'Page Not Found' and the long paragraph key
- npm run check passes with zero errors
- TRX-07 (no duplicates): verified by inspection — no new keys duplicate existing ones
- TRX-08 (compile-time enforcement): verified by npm run check passing
- TRX-09 (check passes): verified by npm run check
- TRX-10 (correct 404 keys): verified by grep
</success_criteria>

<output>
After completion, create `.planning/phases/30-translation-system-overhaul/30-01-SUMMARY.md`
</output>

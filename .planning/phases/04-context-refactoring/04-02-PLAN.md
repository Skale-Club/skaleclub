---
phase: 04-context-refactoring
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - client/src/pages/xpot/XpotApp.tsx
  - client/src/pages/xpot/XpotContext.tsx
autonomous: true
requirements:
  - CTX-02
  - CTX-03

must_haves:
  truths:
    - "Old pages work without modification during transition"
    - "New imports work after refactoring"
  artifacts:
    - path: "client/src/pages/xpot/XpotContext.tsx"
      provides: "Backward-compatible facade with all exports"
    - path: "client/src/pages/xpot/XpotApp.tsx (modified)"
      provides: "App imports hooks individually"
  key_links:
    - from: "XpotContext facade"
      to: "hooks/*"
      via: "re-exports from hooks"
      pattern: "export.*use.*from.*hooks"
---

<objective>
Create backward-compatible facade in XpotContext re-exporting from hooks, update XpotApp to use hooks directly.

Purpose: Enable zero-change migration - old imports work, new direct hook imports also work.
Output: Updated XpotApp.tsx uses hooks directly, XpotContext.tsx re-exports as facade
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
# What was built in plan 01

@client/src/pages/xpot/hooks/useXpotShared.ts — shared geoState, loadCurrentLocation, invalidateXpotData
@client/src/pages/xpot/hooks/useXpotQueries.ts — auth + core queries
@client/src/pages/xpot/hooks/useAccounts.ts — account management
@client/src/pages/xpot/hooks/useVisits.ts — visits with race condition guard
@client/src/pages/xpot/hooks/useCheckIn.ts — check-in flow with recording
@client/src/pages/xpot/hooks/useSales.ts — opportunities + tasks

# Consumer pages to update

1. XpotDashboard.tsx — consumes useXpotQueries
2. XpotCheckIn.tsx — consumes useCheckIn, useXpotQueries, useAccounts
3. XpotAccounts.tsx — consumes useAccounts
4. XpotSales.tsx — consumes useSales
5. XpotVisits.tsx — consumes useVisits
6. XpotApp.tsx — Provider wrapper, needs to compose all hooks

# Route structure

/xpot → XpotApp.tsx (Provider) → XpotRouter.tsx → individual page components
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create XpotContext facade</name>
  <files>client/src/pages/xpot/XpotContext.tsx</files>
  <action>
Rewrite XpotContext.tsx to be a facade that re-exports from hooks. The file should:

1. Import all hooks:
```typescript
import { useXpotShared } from "./hooks/useXpotShared";
import { useXpotQueries } from "./hooks/useXpotQueries";
import { useAccounts } from "./hooks/useAccounts";
import { useVisits } from "./hooks/useVisits";
import { useCheckIn } from "./hooks/useCheckIn";
import { useSales } from "./hooks/useSales";
```

2. Create XpotAppProvider that composes all hooks and provides a unified context. Must call useXpotShared() and spread its values (geoState, loadCurrentLocation, invalidateXpotData) into the context value for backward compatibility.

3. Preserve EXACT XpotAppContextValue interface from original (for backward compatibility)

4. Preserve the useXpotApp() hook - but make it wrap the context provider

5. IMPORTANT: The context value returned must be identical to what was in the original 729-line file - this ensures old consumers that import via useXpotApp() continue to work

This is the CTX-02 requirement - backward-compatible facade hook.
  </action>
  <verify>Build passes: npm run check -- --filter=client, verify no type errors in XpotContext.tsx</verify>
  <done>Facade re-exports from hooks, old useXpotApp() calls still work</done>
</task>

<task type="auto">
  <name>Task 2: Update XpotApp to use hooks directly</name>
  <files>client/src/pages/xpot/XpotApp.tsx</files>
  <action>
Update XpotApp.tsx to import hooks directly instead of using the monolithic context.

Read current XpotApp.tsx to see how it uses useXpotApp().

Change it to:
1. Import individual hooks from ./xpot/hooks/*
2. Compose them in the provider component
3. Pass unified value to XpotAppProvider

The file should import useXpotQueries, useAccounts, useVisits, useCheckIn, useSales and pass their outputs to the provider.

Keep XpotAppProvider for the provider wrapper - but now it composes individual hooks.
  </action>
  <verify>npm run check passes, no import errors</verify>
  <done>XpotApp uses individual hooks instead of monolithic context</done>
</task>

</tasks>

<verification>
[Run build to verify no errors]
- npm run build
[Manual verification - app loads]
</verification>

<success_criteria>
XpotApp.tsx uses hooks directly, XpotContext facade provides backward compatibility, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-context-refactoring/{phase}-{plan}-SUMMARY.md`
</output>
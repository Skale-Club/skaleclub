---
phase: 08-admin-ui-estimatessection
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - client/src/components/admin/shared/types.ts
  - client/src/components/admin/shared/constants.ts
  - client/src/pages/Admin.tsx
autonomous: false
requirements:
  - EST-06

must_haves:
  truths:
    - "Navigating to /admin/estimates in a browser renders EstimatesSection (not a blank section or wrong component)"
    - "'estimates' is a valid value for AdminSection type — TypeScript accepts it everywhere AdminSection is used"
    - "The sidebar shows an 'Estimates' menu item with Receipt icon after the 'Xpot' entry"
    - "Admin.tsx slug maps handle estimates in both READ direction (URL segment → AdminSection) and WRITE direction (AdminSection → URL segment)"
    - "'estimates' is in sectionsWithOwnHeader so the generic SectionHeader is suppressed"
  artifacts:
    - path: "client/src/components/admin/shared/types.ts"
      provides: "AdminSection union extended with 'estimates'"
      contains: "| 'estimates'"
    - path: "client/src/components/admin/shared/constants.ts"
      provides: "SIDEBAR_MENU_ITEMS with Estimates entry and Receipt icon import"
      contains: "Receipt"
    - path: "client/src/pages/Admin.tsx"
      provides: "Slug maps, sectionsWithOwnHeader, and render switch updated"
      contains: "EstimatesSection"
  key_links:
    - from: "Admin.tsx slug map (read)"
      to: "AdminSection 'estimates'"
      via: "slugMap['estimates'] = 'estimates'"
      pattern: "estimates.*estimates"
    - from: "Admin.tsx render switch"
      to: "EstimatesSection component"
      via: "activeSection === 'estimates' && <EstimatesSection />"
      pattern: "activeSection.*estimates.*EstimatesSection"
---

<objective>
Wire `EstimatesSection` into the admin dashboard: extend the `AdminSection` union, add the sidebar menu item, and update the three insertion points in Admin.tsx (read slug map, write slug map, sectionsWithOwnHeader array, and render switch).

Purpose: Makes the Estimates tab reachable via `/admin/estimates` and renders the component created in Plan 01.
Output: Three edited files — types.ts, constants.ts, Admin.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-admin-ui-estimatessection/08-CONTEXT.md
@.planning/phases/08-admin-ui-estimatessection/08-01-SUMMARY.md

<!-- Reference the exact current state of files before editing -->
@client/src/components/admin/shared/types.ts
@client/src/components/admin/shared/constants.ts
@client/src/pages/Admin.tsx
</context>

<interfaces>
<!-- Exact state of files before modification — executor must read these first -->

From client/src/components/admin/shared/types.ts (current AdminSection union — ADD 'estimates' at the end):
```typescript
export type AdminSection =
  | 'dashboard' | 'leads' | 'forms' | 'website' | 'company'
  | 'seo' | 'faqs' | 'users' | 'chat' | 'integrations'
  | 'blog' | 'portfolio' | 'links' | 'vcards' | 'fieldSales';
  // ADD: | 'estimates'
```

From client/src/components/admin/shared/constants.ts (current lucide-react import — ADD Receipt):
```typescript
import { Building2, Briefcase, ClipboardList, FileText, HelpCircle, Image, LayoutDashboard, Link, MessageSquare, Puzzle, Search, Sparkles, Users, Smartphone, MapPinned } from 'lucide-react';
// ADD Receipt to this import
```

Current SIDEBAR_MENU_ITEMS last entry:
```typescript
{ id: 'fieldSales', title: 'Xpot', description: 'Field sales check-ins, visits and rep tracking', icon: MapPinned },
// ADD after this line:
{ id: 'estimates', title: 'Estimates', description: 'Client proposals with shareable links', icon: Receipt },
```

From Admin.tsx — read slug map (line ~40-57):
```typescript
const slugMap: Record<string, AdminSection> = {
  // ... existing entries ...
  'field-sales': 'fieldSales',
  // ADD: estimates: 'estimates',
};
```

From Admin.tsx — write slug map inside handleSectionSelect (line ~93-110):
```typescript
const slugMap: Record<AdminSection, string> = {
  // ... existing entries ...
  fieldSales: 'field-sales',
  // ADD: estimates: 'estimates',
};
```

From Admin.tsx — sectionsWithOwnHeader array (line ~183):
```typescript
const sectionsWithOwnHeader: AdminSection[] = ['leads', 'forms', 'faqs', 'users', 'blog', 'portfolio', 'links', 'vcards', 'fieldSales', 'company', 'website', 'seo', 'integrations'];
// ADD 'estimates' to this array
```

From Admin.tsx — render switch (after line ~207):
```typescript
{activeSection === 'fieldSales' && <XpotSalesSection />}
// ADD after:
{activeSection === 'estimates' && <EstimatesSection />}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend AdminSection union and add sidebar menu item</name>
  <read_first>
    - client/src/components/admin/shared/types.ts (current AdminSection union — read exact current state)
    - client/src/components/admin/shared/constants.ts (current SIDEBAR_MENU_ITEMS and lucide-react import)
  </read_first>
  <files>
    client/src/components/admin/shared/types.ts
    client/src/components/admin/shared/constants.ts
  </files>
  <action>
**Edit 1 — types.ts:**

Add `| 'estimates'` to the AdminSection union after `| 'fieldSales'`. The full union after the edit:

```typescript
export type AdminSection =
  | 'dashboard'
  | 'leads'
  | 'forms'
  | 'website'
  | 'company'
  | 'seo'
  | 'faqs'
  | 'users'
  | 'chat'
  | 'integrations'
  | 'blog'
  | 'portfolio'
  | 'links'
  | 'vcards'
  | 'fieldSales'
  | 'estimates';
```

Note: the current file uses a single-line pipe-separated union. Either format (multi-line or single-line) is acceptable as long as `'estimates'` is added. Do NOT change the rest of the file.

**Edit 2 — constants.ts:**

Step 1: Add `Receipt` to the lucide-react named import. The current import is:
```
import { Building2, Briefcase, ClipboardList, FileText, HelpCircle, Image, LayoutDashboard, Link, MessageSquare, Puzzle, Search, Sparkles, Users, Smartphone, MapPinned } from 'lucide-react';
```
Change to:
```
import { Building2, Briefcase, ClipboardList, FileText, HelpCircle, Image, LayoutDashboard, Link, MessageSquare, Puzzle, Receipt, Search, Sparkles, Users, Smartphone, MapPinned } from 'lucide-react';
```

Step 2: Append a new entry to SIDEBAR_MENU_ITEMS array after the existing last entry (fieldSales):
```typescript
{ id: 'estimates', title: 'Estimates', description: 'Client proposals with shareable links', icon: Receipt },
```

The entry must appear as the last element in the array, after the `fieldSales` entry. Do NOT change any other entries.
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/skaleclub && npm run check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "estimates" client/src/components/admin/shared/types.ts` returns a match containing `'estimates'`
    - `grep "Receipt" client/src/components/admin/shared/constants.ts` returns a match (both import and usage)
    - `grep "id: 'estimates'" client/src/components/admin/shared/constants.ts` returns a match
    - `npm run check` exits 0
  </acceptance_criteria>
  <done>AdminSection union includes 'estimates' and SIDEBAR_MENU_ITEMS has the Estimates entry with Receipt icon</done>
</task>

<task type="auto">
  <name>Task 2: Wire EstimatesSection into Admin.tsx (slug maps + header suppression + render switch)</name>
  <read_first>
    - client/src/pages/Admin.tsx (full file — read current state before any edits; both slug maps live here)
    - client/src/components/admin/EstimatesSection.tsx (confirm the export name before writing the import)
  </read_first>
  <files>client/src/pages/Admin.tsx</files>
  <action>
Make four targeted edits to Admin.tsx. Read the file first to locate exact line numbers; the positions described below are based on the current file state.

**Edit 1 — Add import for EstimatesSection** (after the XpotSalesSection import on line ~25):
```typescript
import { EstimatesSection } from '@/components/admin/EstimatesSection';
```

**Edit 2 — READ slug map** (inside the `useMemo`, the `Record<string, AdminSection>` object):
Add after the `'field-sales': 'fieldSales'` entry:
```typescript
estimates: 'estimates',
```

**Edit 3 — WRITE slug map** (inside `handleSectionSelect`, the `Record<AdminSection, string>` object):
Add after the `fieldSales: 'field-sales'` entry:
```typescript
estimates: 'estimates',
```

**Edit 4 — sectionsWithOwnHeader array** (line ~183):
Add `'estimates'` to the array. Current array:
```typescript
const sectionsWithOwnHeader: AdminSection[] = ['leads', 'forms', 'faqs', 'users', 'blog', 'portfolio', 'links', 'vcards', 'fieldSales', 'company', 'website', 'seo', 'integrations'];
```
After edit:
```typescript
const sectionsWithOwnHeader: AdminSection[] = ['leads', 'forms', 'faqs', 'users', 'blog', 'portfolio', 'links', 'vcards', 'fieldSales', 'estimates', 'company', 'website', 'seo', 'integrations'];
```

**Edit 5 — Render switch** (after `{activeSection === 'fieldSales' && <XpotSalesSection />}`, line ~207):
```typescript
{activeSection === 'estimates' && <EstimatesSection />}
```

**CRITICAL — Pitfall 5 from RESEARCH.md:** Both slug maps must be updated. The read-direction map converts URL segment → AdminSection (line ~40-57, inside the `useMemo`). The write-direction map converts AdminSection → URL slug (line ~93-110, inside `handleSectionSelect`). Missing either will cause navigation to silently fall back to 'dashboard'. Verify both are updated before completing.
  </action>
  <verify>
    <automated>cd /c/Users/Vanildo/Dev/skaleclub && npm run check 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "EstimatesSection" client/src/pages/Admin.tsx` returns at least 2 matches (import + JSX render)
    - `grep -c "estimates.*estimates" client/src/pages/Admin.tsx` returns at least 2 (both slug maps)
    - `grep "sectionsWithOwnHeader" client/src/pages/Admin.tsx | grep estimates` returns a match
    - `npm run check` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>Admin.tsx imports EstimatesSection, both slug maps include 'estimates', sectionsWithOwnHeader suppresses the generic header, and the render switch renders EstimatesSection when activeSection === 'estimates'</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual QA — verify all EstimatesSection flows in browser</name>
  <what-built>
    Full EstimatesSection admin UI wired into the dashboard:
    - Estimates tab in the admin sidebar (Receipt icon, "Estimates" label)
    - List view showing client name, slug, creation date, copy-link/edit/delete actions
    - Create dialog with catalog checklist picker (checkboxes auto-fill service rows)
    - Edit dialog showing existing service rows, "Add from catalog" expansion button
    - "Add custom row" appends a blank editable row
    - dnd-kit drag reorder inside the dialog (GripVertical handles)
    - Delete with AlertDialog confirmation
    - All mutations show Loader2 spinner on pending
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Navigate to `http://localhost:5000/admin/estimates`
    3. Verify the "Estimates" sidebar item is visible (Receipt icon)
    4. Verify list shows "No estimates yet" empty state
    5. Click "New Estimate" — dialog opens with catalog checklist visible
    6. Check one or more catalog services — service rows appear with pre-filled title/description/price
    7. Add a custom row — blank row appears at bottom
    8. Drag a row by its GripVertical handle — row reorders
    9. Fill in a client name and click "Create Estimate" — estimate appears in the list
    10. Click the Copy icon on the new estimate — toast "Link copied" appears
    11. Click the Pencil/Edit icon — dialog opens with existing rows (no checklist visible by default)
    12. Click "Add from catalog" in edit mode — checklist expands inline
    13. Click the Trash2/Delete icon — AlertDialog "Delete estimate?" appears; confirm — estimate removed from list
  </how-to-verify>
  <action>Human verifies all admin flows work correctly in the browser by following the numbered steps in how-to-verify.</action>
  <verify>
    <automated>MANUAL — follow the steps in how-to-verify above</automated>
  </verify>
  <done>All 13 verification steps pass — estimates tab visible, CRUD flows functional, drag reorder works, copy-link copies correct URL</done>
  <resume-signal>Type "approved" if all flows work, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
After Tasks 1 and 2 (automated):
- `npm run check` exits 0
- `grep "'estimates'" client/src/components/admin/shared/types.ts` returns a match
- `grep "Receipt" client/src/components/admin/shared/constants.ts` returns 2 matches (import + usage)
- `grep "EstimatesSection" client/src/pages/Admin.tsx` returns 2+ matches
- `grep -c "estimates.*estimates" client/src/pages/Admin.tsx` returns 2+ (both slug maps)

After Task 3 (manual QA):
- Admin sidebar shows Estimates tab
- All CRUD flows functional
- Drag reorder works and order persists on save + reopen
</verification>

<success_criteria>
- AdminSection union includes 'estimates' (types.ts)
- SIDEBAR_MENU_ITEMS has Estimates with Receipt icon (constants.ts)
- Both slug maps in Admin.tsx include estimates (read + write direction)
- 'estimates' in sectionsWithOwnHeader array
- EstimatesSection renders at /admin/estimates
- Manual QA passes all 13 verification steps
- `npm run check` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/08-admin-ui-estimatessection/08-02-SUMMARY.md`
</output>

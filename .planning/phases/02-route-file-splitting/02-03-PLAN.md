---
phase: 02-route-file-splitting
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - server/routes.ts
autonomous: false
requirements:
  - SPLIT-04
  - SPLIT-05

must_haves:
  truths:
    - "server/routes.ts imports registerXpotRoutes from the barrel index (not the old file)"
    - "TypeScript compiles with zero errors"
    - "All 28 /api/xpot/* endpoints respond with the same status codes and JSON shapes as before"
    - "No dead imports or orphaned code remains"
  artifacts:
    - path: "server/routes.ts"
      provides: "Updated import path for registerXpotRoutes"
      contains: "./routes/xpot/index"
  key_links:
    - from: "server/routes.ts"
      to: "server/routes/xpot/index.ts"
      via: "import { registerXpotRoutes } from './routes/xpot/index.js'"
      pattern: "import.*registerXpotRoutes.*routes/xpot"
---

<objective>
Update `server/routes.ts` import to point to the new barrel file and verify all 28 routes work identically via TypeScript compilation and manual smoke test.

Purpose: SPLIT-04 requires routes.ts to import from the new barrel. SPLIT-05 requires verification that all routes respond identically.
Output: Clean compilation, smoke test confirmation, phase complete.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/routes.ts
@server/routes/xpot/index.ts (created in Plan 02-02)

Phase 1 completed: All Xpot endpoints return errors in `{ message, errors? }` shape via global middleware in `server/app.ts:64-88`. The split routers inherit this — no local error handlers needed.

Current state after Plan 02-02:
- `server/routes/xpot.ts` is DELETED
- `server/routes/xpot/index.ts` exports `registerXpotRoutes(app)`
- `server/routes.ts` line 32 still has: `import { registerXpotRoutes } from "./routes/xpot.js"`
- Node.js module resolution may resolve `./routes/xpot.js` → `./routes/xpot/index.js` (directory index), but TypeScript may flag this. The explicit path is cleaner.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update routes.ts import path</name>
  <files>server/routes.ts</files>
  <read_first>server/routes.ts</read_first>
  <action>
    Change line 32 of `server/routes.ts` from:
    ```typescript
    import { registerXpotRoutes } from "./routes/xpot.js";
    ```
    to:
    ```typescript
    import { registerXpotRoutes } from "./routes/xpot/index.js";
    ```

    This is the ONLY change to routes.ts. The function signature `registerXpotRoutes(app)` is identical — no downstream changes needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `server/routes.ts` line 32 imports from `./routes/xpot/index.js`
    - TypeScript compiles with zero errors (`npm run check` passes)
    - No other changes to routes.ts
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Smoke test all 28 Xpot API endpoints</name>
  <files>(none — human verification only)</files>
  <action>Human verifies the split routes work by testing the application. The agent's automated work is complete — this task confirms correctness.</action>
  <verify>
    <automated>echo "CHECKPOINT — human verification required"</automated>
  </verify>
  <done>All 28 /api/xpot/* endpoints return expected responses. No 404 or 500 errors. Server console clean.</done>
  <what-built>
    All 28 Xpot API routes have been split from a single 1,042-line monolith into 10 domain router files under `server/routes/xpot/`. The routes.ts import has been updated. TypeScript compiles cleanly.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `npm run dev`
    2. Open the Xpot application in browser (navigate to the sales/field module)
    3. Verify these critical flows work:
       - **Auth**: Dashboard loads (calls GET /api/xpot/me)
       - **Place Search**: Search for a location (GET /api/xpot/place-search)
       - **Accounts**: View accounts list, create a new account (GET/POST /api/xpot/accounts)
       - **Visits**: Check in to an account, check out (POST /api/xpot/visits/check-in, POST /api/xpot/visits/:id/check-out)
       - **Opportunities**: Create an opportunity (POST /api/xpot/opportunities)
       - **Tasks**: Create a task (POST /api/xpot/tasks)
       - **Sync**: Trigger sync flush (POST /api/xpot/sync/flush)
       - **Admin** (if admin user): View admin overview (GET /api/xpot/admin/overview)
    4. Check browser Network tab — all /api/xpot/* requests should return 200/201 (not 404 or 500)
    5. Check server console — no import errors, no "Cannot find module" messages
  </how-to-verify>
  <resume-signal>Type "approved" or describe any broken endpoints</resume-signal>
</task>

</tasks>

<verification>
- `npm run check` passes with zero TypeScript errors
- `grep "registerXpotRoutes" server/routes.ts` shows import from `./routes/xpot/index.js`
- `grep -c "registerXpotRoutes" server/routes/xpot/index.ts` = 1 (single export)
- `test ! -f server/routes/xpot.ts` confirms old file is gone
- Manual: All 28 /api/xpot/* endpoints respond correctly
</verification>

<success_criteria>
- [ ] routes.ts imports from `./routes/xpot/index.js`
- [ ] TypeScript compiles with zero errors
- [ ] All 28 API endpoints respond identically (same URLs, same JSON shapes)
- [ ] No server console errors on startup
- [ ] No 404 responses from /api/xpot/* endpoints
</success_criteria>

<output>
After completion, create `.planning/phases/02-route-file-splitting/02-03-SUMMARY.md`
</output>

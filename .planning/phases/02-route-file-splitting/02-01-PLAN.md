---
phase: 02-route-file-splitting
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - server/routes/xpot/middleware.ts
  - server/routes/xpot/helpers.ts
autonomous: true
requirements:
  - SPLIT-01
  - SPLIT-02

must_haves:
  truths:
    - "Shared middleware functions are importable from a single module"
    - "Shared helper functions are importable from a single module"
    - "TypeScript compiles without errors after extraction"
  artifacts:
    - path: "server/routes/xpot/middleware.ts"
      provides: "requireXpotUser, requireXpotManager, ensureXpotRep, getCurrentSessionUser, SessionUser type"
      exports: ["requireXpotUser", "requireXpotManager", "ensureXpotRep", "getCurrentSessionUser", "SessionUser"]
    - path: "server/routes/xpot/helpers.ts"
      provides: "getDistanceMeters, syncAccountToGhl, syncOpportunityToGhl, syncTaskToGhl"
      exports: ["getDistanceMeters", "syncAccountToGhl", "syncOpportunityToGhl", "syncTaskToGhl"]
  key_links:
    - from: "server/routes/xpot/middleware.ts"
      to: "server/storage.js"
      via: "import storage singleton"
      pattern: "import.*from.*\\.{1,2}/storage"
    - from: "server/routes/xpot/helpers.ts"
      to: "server/integrations/ghl.js"
      via: "import GHL integration functions"
      pattern: "import.*from.*ghl"
---

<objective>
Extract shared middleware and helper functions from `server/routes/xpot.ts` into two dedicated modules under `server/routes/xpot/`. These modules will be imported by all 10 domain routers created in the next plan.

Purpose: SPLIT-01 requires shared middleware extraction; SPLIT-02 requires shared helper extraction. These are prerequisites for all domain routers.
Output: `server/routes/xpot/middleware.ts` and `server/routes/xpot/helpers.ts`
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/routes/xpot.ts
@server/routes/xpot.ts (lines 18-99: SessionUser type, getCurrentSessionUser, ensureXpotRep, requireXpotUser, requireXpotManager)
@server/routes/xpot.ts (lines 26-252: getDistanceMeters, syncAccountToGhl, syncOpportunityToGhl, syncTaskToGhl)

Phase 1 completed: `express-async-errors` installed at `server/app.ts:1`, global error middleware handles ZodError → 400 with `err.flatten().fieldErrors`. Domain routers must NOT add local error handlers — let errors propagate to global middleware.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract middleware to server/routes/xpot/middleware.ts</name>
  <files>server/routes/xpot/middleware.ts</files>
  <read_first>server/routes/xpot.ts</read_first>
  <action>
    Create the directory `server/routes/xpot/` and create `middleware.ts` by extracting the following from `server/routes/xpot.ts`:

    1. Import: `import type { NextFunction, Request, Response } from "express";` and `import { storage } from "../storage.js";`
    2. Export the `SessionUser` type (lines 18-24):
       ```typescript
       export type SessionUser = {
         userId: string;
         email: string | null;
         firstName: string | null;
         lastName: string | null;
         isAdmin: boolean;
       };
       ```
    3. Export `getCurrentSessionUser` function (lines 39-52) — takes `req: Request`, returns `Promise<SessionUser | null>`, reads `req.session.userId/email/firstName/lastName/isAdmin`
    4. Export `ensureXpotRep` function (lines 54-75) — calls `getCurrentSessionUser`, then `storage.getSalesRepByUserId` or `storage.upsertSalesRep`, returns `{ user, rep }` or `null`
    5. Export `requireXpotUser` async middleware (lines 77-87) — calls `ensureXpotRep`, returns 401 if no actor, 403 if `!actor.rep.isActive`, sets `(req as any).xpotActor = actor`
    6. Export `requireXpotManager` async middleware (lines 89-99) — calls `ensureXpotRep`, returns 401 if no actor, 403 if not admin/manager role, sets `(req as any).xpotActor = actor`

    IMPORTANT: Copy function bodies EXACTLY — do not modify logic. Only change:
    - Add `export` keyword to each function/type
    - Fix import paths: `../storage.js` (was `./storage.js` in original, but original is at routes/xpot.ts so same depth)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `server/routes/xpot/middleware.ts` exists and exports: SessionUser, getCurrentSessionUser, ensureXpotRep, requireXpotUser, requireXpotManager
    - TypeScript compiles without errors (`npm run check` passes)
    - Function bodies match original xpot.ts exactly (grep for `xpotActor`, `isActive`, `ensureXpotRep`)
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract helpers to server/routes/xpot/helpers.ts</name>
  <files>server/routes/xpot/helpers.ts</files>
  <read_first>server/routes/xpot.ts</read_first>
  <action>
    Create `server/routes/xpot/helpers.ts` by extracting the following from `server/routes/xpot.ts`:

    1. Imports needed:
       ```typescript
       import { storage } from "../storage.js";
       import { getOrCreateGHLContact, createGHLOpportunity, updateGHLOpportunity, createGHLTask } from "../integrations/ghl.js";
       ```
    2. Export `getDistanceMeters` function (lines 26-37) — Haversine formula, takes (lat1, lng1, lat2, lng2), returns `Math.round(earthRadiusMeters * c)`
     3. Export `syncAccountToGhl` async function (lines 101-153) — takes `leadId: number`, calls `storage.getIntegrationSettings("gohighlevel")`, `storage.getSalesLead`, `getOrCreateGHLContact`, `storage.updateSalesLead`, `storage.createSalesSyncEvent`. Returns `{ synced, message?, ghlContactId? }`
    4. Export `syncOpportunityToGhl` async function (lines 155-214) — takes `opportunityId: number`, calls `storage.getIntegrationSettings("gohighlevel")`, `storage.getSalesAppSettings()`, `storage.listSalesOpportunities()`, `updateGHLOpportunity` or `createGHLOpportunity`. Returns `{ synced, message?, ghlOpportunityId? }`
    5. Export `syncTaskToGhl` async function (lines 216-252) — takes `taskId: number`, calls `storage.getIntegrationSettings("gohighlevel")`, `storage.listSalesTasks()`, `createGHLTask`. Returns `{ synced, message?, ghlTaskId? }`

    IMPORTANT: Copy function bodies EXACTLY — do not modify logic. Only add `export` keyword.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `server/routes/xpot/helpers.ts` exists and exports: getDistanceMeters, syncAccountToGhl, syncOpportunityToGhl, syncTaskToGhl
    - TypeScript compiles without errors (`npm run check` passes)
    - GHL integration imports resolve correctly from `../integrations/ghl.js`
  </done>
</task>

</tasks>

<verification>
- `ls server/routes/xpot/` shows: middleware.ts, helpers.ts (no other files yet)
- `npm run check` passes with zero errors
- `grep -c "export" server/routes/xpot/middleware.ts` ≥ 5 (5 exports: type + 4 functions)
- `grep -c "export" server/routes/xpot/helpers.ts` ≥ 4 (4 functions)
</verification>

<success_criteria>
- [x] `server/routes/xpot/middleware.ts` created with all auth middleware + SessionUser type
- [x] `server/routes/xpot/helpers.ts` created with all 4 helper functions
- [x] Both files pass TypeScript type checking
- [x] No changes to `server/routes/xpot.ts` yet (it still works as-is)
</success_criteria>

<output>
After completion, create `.planning/phases/02-route-file-splitting/02-01-SUMMARY.md`
</output>

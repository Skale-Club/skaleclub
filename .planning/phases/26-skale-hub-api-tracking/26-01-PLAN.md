---
phase: 26-skale-hub-api-tracking
plan: 01
type: execute
wave: 1
depends_on:
  - 25-01
files_modified:
  - shared/schema/hub.ts
  - server/storage.ts
  - server/routes/skaleHub.ts
  - server/routes.ts
autonomous: true
requirements:
  - HUB-05
  - HUB-06
  - HUB-07
  - HUB-08
  - HUB-09

must_haves:
  truths:
    - "GET /api/skale-hub/active returns a safe public live payload or { live: null, message } when no live is active"
    - "POST /api/skale-hub/register upserts the participant for the current live, creates or refreshes the registration, logs a granted gate_check event, and returns an unlocked response"
    - "POST /api/skale-hub/:liveId/access logs granted or denied join/replay events so admin can distinguish registration unlock from later live-link usage"
    - "Admin-authenticated Skale Hub APIs support live list, create, update, and detail flows"
    - "Setting one live to status live automatically demotes every other live row out of live status so only one active live exists at a time"
    - "Admin analytics APIs return dashboard totals, per-live participant summaries, and participant history data from typed storage methods"
    - "`npm run check` passes after the route module, storage helpers, and shared contracts are added"
---

<objective>
Expose the full server contract for v1.6 Skale Hub API and tracking: public active-live discovery, visitor registration, access-event logging, admin live CRUD, single-active-live enforcement, and admin analytics reads.
</objective>

<output>
After completion, create `.planning/phases/26-skale-hub-api-tracking/26-01-SUMMARY.md`
</output>

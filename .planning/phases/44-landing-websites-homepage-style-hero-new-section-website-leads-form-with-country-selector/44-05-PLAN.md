---
phase: 44-landing-websites-homepage-style-hero-new-section-website-leads-form-with-country-selector
plan: 05
type: execute
wave: 3
depends_on: ["44-04"]
files_modified: []
autonomous: false
requirements:
  - SUCCESS-6
  - SUCCESS-7
  - SUCCESS-9

must_haves:
  truths:
    - "`npm run check` exits 0 against the post-44-04 codebase."
    - "`npm run build` exits 0 with no new Vite warnings about chunk sizes beyond what Phase 42 already accepted."
    - "On a fresh `npm run dev` server, visiting `http://localhost:1000/websites` shows: brand-blue hero with the pt-BR headline, trust badges, the 4-step process stepper, reviews, and the dark CTA section with the `Falar com o time` button — IN THAT ORDER, with no console errors."
    - "Clicking the hero's yellow `Quero meu site` button OR the bottom dark CTA opens the LeadFormModal."
    - "The phone field shows the country selector flag dropdown with at least the 17 PHONE_COUNTRIES entries; selecting Brazil shows the +55 dial code and `(##) #####-####` format placeholder."
    - "Submitting the form with a valid Brazilian phone, name, and email creates a new row in `form_leads` (verified via the admin Forms panel OR a direct DB select), with `telefone` in `+55 (##) #####-####` international format and `customAnswers.countryCode === 'BR'`."
    - "The landing renders without horizontal scroll at 375px (iPhone SE), 768px (tablet), 1280px (laptop)."
  artifacts: []
  key_links:
    - from: "/websites browser visit"
      to: "DynamicLanding + sectionRegistry"
      via: "App.tsx /:slug catch-all → fetch /api/landing-pages/slug/websites → registry lookup per section"
      pattern: "DynamicLanding"
---

<objective>
End-to-end smoke verification of the /websites landing. No new code — purely a human-driven test pass against everything 44-01 through 44-04 built, plus the post-build sanity check.

Purpose: Phase 44's SUCCESS criteria 6, 7, and 9 require human eyes (visual tone matches the homepage; mobile responsive at 3 breakpoints; form submission creates a real lead). These can't be automated reliably without a Playwright/Cypress investment that's out of scope. We treat this as a checkpoint to confirm "ship it" or "fix something specific."

Output: A green/red signal on shipping the phase. If red, the executor returns to whichever plan owns the broken piece (e.g. "hero looks wrong" → revisit 44-01) and creates a follow-up.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-landing-websites-homepage-style-hero-new-section-website-leads-form-with-country-selector/44-CONTEXT.md
@.planning/phases/44-landing-websites-homepage-style-hero-new-section-website-leads-form-with-country-selector/44-01-SUMMARY.md
@.planning/phases/44-landing-websites-homepage-style-hero-new-section-website-leads-form-with-country-selector/44-02-SUMMARY.md
@.planning/phases/44-landing-websites-homepage-style-hero-new-section-website-leads-form-with-country-selector/44-03-SUMMARY.md
@.planning/phases/44-landing-websites-homepage-style-hero-new-section-website-leads-form-with-country-selector/44-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + typecheck pass</name>
  <files></files>
  <action>
**Run both validation commands sequentially. They must exit 0.**

```bash
npm run check
npm run build
```

Capture the build output. If Vite emits any NEW "chunks larger than 500 kB" warning that wasn't present before 44-01 (Phase 42 baseline is captured in `42-02-SUMMARY.md`), that's a regression — the new section files should each compress to under 10 KB and not push any existing chunk over the line. If a warning fires:
- Confirm which chunk it's in (admin vs landing).
- If the landing chunk: this is unexpected; investigate.
- If an unrelated chunk: not a Phase 44 regression — note in the SUMMARY and proceed.

DO NOT silence warnings to make this pass. DO NOT change `vite.config.ts` chunk-size limits.
  </action>
  <verify>
    <automated>npm run check && npm run build</automated>
  </verify>
  <done>Both commands exit 0. Build output captured. No NEW chunk-size warnings attributable to Phase 44.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The /websites landing — composed from heroWebsites + trustBadges + processStepper + reviews + leadFormCta(formSlug=website-leads). The form contains 8 questions including a `phoneCountry` field using the shared `<PhoneCountrySelect>`. Submissions write a row to `form_leads` with the international phone and the ISO country code stashed in `customAnswers.countryCode`.
  </what-built>
  <how-to-verify>
    1. **Start the dev server:**
       ```
       npm run dev
       ```

    2. **Visit `http://localhost:1000/websites`** in a desktop browser (Chrome or Firefox).

    3. **Visual check — section order and tone (per SUCCESS criterion 6 — "homepage's visual tone — same fonts, colors, spacing system"):**
       - [ ] First section: brand-blue hero (`#1C53A3`) with white headline `Sites que vendem para o seu negócio` (gradient text), pt-BR subheadline, and a YELLOW pill button `Quero meu site` (black bold text). The decorative gradient overlay should be present.
       - [ ] Second section: trust badges (whatever the company-settings currently has — same as the Home page).
       - [ ] Third section: light-bg (`bg-zinc-50`) process stepper with 4 numbered circles in brand blue, yellow corner badges (1/2/3/4), titles `Descoberta`, `Design`, `Construção`, `Lançamento`, and pt-BR descriptions. On desktop, a thin connecting line runs between the circles.
       - [ ] Fourth section: reviews (same component used on the Home).
       - [ ] Fifth section: dark `bg-zinc-950` CTA with heading `Pronto para tirar o site do papel?`, subheading, and a yellow pill button `Falar com o time`.
       - [ ] No console errors in the DevTools console (a few React-Query background fetch logs are fine).

    4. **CTA bridge check:**
       - [ ] Click the hero's `Quero meu site` button → the LeadFormModal opens (it's the same modal as the bottom CTA opens — the hero CTA programmatically triggers `[data-landing-lead-cta]`).
       - [ ] Click the bottom CTA's `Falar com o time` button directly → same modal opens.

    5. **Form walkthrough (per SUCCESS criterion 2 — country selector working end-to-end):**
       - [ ] Step 1: title `Qual é o seu nome?` — enter `Test User`.
       - [ ] Step 2: title `Qual é o seu WhatsApp?` — the PHONE COUNTRY SELECTOR appears next to the input. Click the country button → a dropdown of countries appears (rendered via portal). Confirm at minimum: United States, Brazil, Portugal, Spain, Mexico are present. Pick Brazil (or confirm it auto-selected via `detectDefaultPhoneCountry` if you're on a Brazilian browser locale). Enter `11 98765-4321`. The input should auto-format to `(11) 98765-4321`. Click Next.
       - [ ] Step 3: `Qual é o seu e-mail?` — enter `test@example.com`.
       - [ ] Step 4: `Qual é o nome do seu negócio ou projeto?` — leave empty (it's optional) OR enter `Test Project`.
       - [ ] Step 5: `Que tipo de site você precisa?` — pick `Landing page`.
       - [ ] Step 6: `Qual é o orçamento previsto?` — pick `US$ 1.000 a US$ 3.000`.
       - [ ] Step 7: `Qual é o prazo desejado?` — pick `1 a 3 meses`.
       - [ ] Step 8: `Algo mais que devamos saber sobre o projeto?` — leave empty OR enter a note. Click `Finish`.
       - [ ] After submission, the user is redirected to the thank-you page (matches the Home form behavior).

    6. **Lead verification:**
       - [ ] Login to /admin, open Forms section, click into `Website Leads`, confirm the new lead appears at the top.
       - [ ] Click the lead → confirm `telefone` shows `+55 (11) 98765-4321` (international format with dial code).
       - [ ] Open the customAnswers JSON (admin lead detail or a direct DB select) → confirm it contains: `{ "countryCode": "BR", "tipoProjeto": "landing-page", "orcamento": "1k-3k", "prazo": "1-3-meses" }` at minimum. If `nomeProjeto` and/or `observacoes` were filled, they should also appear.

    7. **Responsive check** — open DevTools device toolbar and verify at three widths:
       - [ ] **375px (iPhone SE):** no horizontal scrollbar. The hero stacks. The process stepper stacks 1-column. The CTA buttons go full-width.
       - [ ] **768px (iPad portrait):** the process stepper goes to 2-column. The hero still has the headline visible.
       - [ ] **1280px (small laptop):** the hero is 2-column layout. The process stepper is 4-column with the connecting line visible.
       - [ ] At every width, the phone country dropdown opens without being clipped by container overflow.

    8. **Pixel cohesion with Home:**
       - [ ] Open the Home (`http://localhost:1000/`) in another tab. The fonts (Outfit headings, Inter body) and the brand-blue hero tone should feel cohesive with /websites. Tiny intentional differences (yellow CTA on /websites vs blue CTA on Home) are EXPECTED per Brand Guidelines.

    **If anything in steps 3-8 fails:** describe the failure, name the broken piece, and which plan owns it:
    - Visual issue in hero → 44-01
    - Process stepper layout broken → 44-02
    - Country selector missing / phone validation broken / submission missing country code → 44-03
    - Wrong sections rendered / wrong order / form has wrong questions → 44-04 (the seed)
  </how-to-verify>
  <resume-signal>
    Reply `approved — ship it` if all 8 steps pass.

    Or reply with: `failed — <which plan> — <specific issue>` and the next planning loop will create a focused gap-closure plan (e.g. `/gsd:plan-phase 44 --gaps`).
  </resume-signal>
</task>

</tasks>

<verification>
This plan IS the verification. Acceptance is the human's signal after Task 2.
</verification>

<success_criteria>
- All three SUCCESS criteria gated by this plan are confirmed: visual tone matches Home (6), mobile responsive across 375/768/1280 (7), build+typecheck green (9).
- One real test lead lives in `form_leads` for `website-leads` form with the country code captured.
- Phase 44 is ready for the next deploy.
</success_criteria>

<output>
After completion (post-human approval), create `.planning/phases/44-landing-websites-homepage-style-hero-new-section-website-leads-form-with-country-selector/44-05-SUMMARY.md` with:
- The exact `npm run check` and `npm run build` output (last 30 lines of each, especially any warnings).
- A note for each of the 8 smoke-test steps with PASS/FAIL.
- The lead row id created during the smoke test for traceability.
- Any deferrals captured for a future phase (e.g. expand PHONE_COUNTRIES from 17 → 250, SEO meta tags, animations).
</output>

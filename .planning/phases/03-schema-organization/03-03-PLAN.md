---
phase: 03-schema-organization
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
  - 03-02
files_modified:
  - shared/schema.ts
  - drizzle.config.ts
autonomous: true
requirements:
  - SCHM-02
  - SCHM-03
  - SCHM-04
user_setup: []

must_haves:
  truths:
    - "shared/schema.ts contains ONLY barrel re-exports from 6 domain files (no table definitions)"
    - "drizzle.config.ts schema field points to './shared/schema' (folder path, not file)"
    - "npm run check passes with zero TypeScript errors"
    - "npm run db:push connects and detects all tables without 'duplicate table' warnings"
    - "All 64 existing import sites resolve correctly through the barrel re-export"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Barrel re-export for all 64 import sites"
      contains: "export * from './schema/auth.js' through settings.js"
      max_lines: 30
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit config pointing to schema folder"
      contains: 'schema: "./shared/schema"'
  key_links:
    - from: "shared/schema.ts"
      to: "shared/schema/*.ts"
      via: "export * re-exports"
      pattern: "export \\* from ['\"]\\./schema/"
    - from: "drizzle.config.ts"
      to: "shared/schema/"
      via: "schema field"
      pattern: 'schema:.*"./shared/schema"'
    - from: "server/db.ts"
      to: "shared/schema.ts barrel"
      via: "import * as schema from #shared/schema.js"
      pattern: "import \\* as schema"
---

<objective>
Convert `shared/schema.ts` from monolith to barrel re-export, update `drizzle.config.ts` to use folder path, and verify everything works.

Purpose: Complete the schema split by wiring the barrel re-export (preserving all 64 import sites) and pointing drizzle-kit at the new folder structure. Then verify with TypeScript and drizzle-kit.
Output: Updated `shared/schema.ts` (barrel), updated `drizzle.config.ts`, verification results
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@shared/schema.ts
@drizzle.config.ts
@server/db.ts
</context>

<interfaces>
<!-- Key exports the executor must ensure are in the barrel -->
From server/db.ts:
```typescript
import * as schema from "#shared/schema.js";
const db = drizzle(pool, { schema });
```
This means the barrel MUST re-export all pgTable objects so drizzle can register them.

From 64 import sites across codebase:
- `import { users, sessions, ... } from "@shared/schema"` or `#shared/schema.js`
- All must continue working after barrel conversion
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Convert shared/schema.ts to barrel re-export</name>
  <files>shared/schema.ts</files>
  <read_first>shared/schema.ts, shared/schema/auth.ts, shared/schema/chat.ts, shared/schema/cms.ts, shared/schema/forms.ts, shared/schema/sales.ts, shared/schema/settings.ts</read_first>
  <action>
Replace the ENTIRE contents of `shared/schema.ts` (1,004 lines) with this barrel re-export:

```typescript
export * from "./schema/auth.js";
export * from "./schema/cms.js";
export * from "./schema/chat.js";
export * from "./schema/forms.js";
export * from "./schema/sales.js";
export * from "./schema/settings.js";
```

**CRITICAL CONSTRAINTS:**
- This file MUST stay at `shared/schema.ts` (OUTSIDE the `shared/schema/` directory)
- Do NOT create a `shared/schema/index.ts` file — drizzle-kit scans all `.ts` files in the folder and would see duplicate tables
- Use `.js` extensions on all relative imports (TypeScript with `"moduleResolution": "bundler"` maps `.js` → `.ts`)
- The order of re-exports does not matter — all are `export *` so all names are merged
- Verify each domain file exists before referencing it (all 6 files from Plans 01 and 02)

After writing, verify the file is under 30 lines and contains NO table definitions, NO imports from drizzle-orm, NO createInsertSchema calls — only `export *` statements.
  </action>
  <acceptance_criteria>
    - File `shared/schema.ts` is under 30 lines
    - grep: `export \\* from ["']\\./schema/auth\\.js["']` present
    - grep: `export \\* from ["']\\./schema/cms\\.js["']` present
    - grep: `export \\* from ["']\\./schema/chat\\.js["']` present
    - grep: `export \\* from ["']\\./schema/forms\\.js["']` present
    - grep: `export \\* from ["']\\./schema/sales\\.js["']` present
    - grep: `export \\* from ["']\\./schema/settings\\.js["']` present
    - grep: `pgTable` NOT present (no table definitions in barrel)
    - grep: `createInsertSchema` NOT present (no schema generation in barrel)
  </acceptance_criteria>
  <verify>
    <automated>wc -l shared/schema.ts</automated>
    Expected: under 30 lines
  </verify>
  <done>shared/schema.ts converted to barrel re-export with 6 export * statements, zero table definitions remain</done>
</task>

<task type="auto">
  <name>Task 2: Update drizzle.config.ts schema path</name>
  <files>drizzle.config.ts</files>
  <read_first>drizzle.config.ts</read_first>
  <action>
Change line 9 of `drizzle.config.ts` from:
```typescript
    schema: "./shared/schema.ts",
```
to:
```typescript
    schema: "./shared/schema",
```

This tells drizzle-kit to recursively scan the `shared/schema/` directory for all `.ts` files containing pgTable/pgEnum exports. The barrel file at `shared/schema.ts` is OUTSIDE this folder so it won't be scanned (preventing duplicate table detection).

No other lines in drizzle.config.ts need to change.
  </action>
  <acceptance_criteria>
    - grep: `schema: "./shared/schema"` present (without `.ts` extension)
    - grep: `schema: "./shared/schema.ts"` NOT present (old value removed)
    - All other lines unchanged (out, dialect, dbCredentials)
  </acceptance_criteria>
  <verify>
    <automated>grep 'schema:' drizzle.config.ts</automated>
    Expected: `schema: "./shared/schema",`
  </verify>
  <done>drizzle.config.ts updated to use folder path for schema scanning</done>
</task>

<task type="auto">
  <name>Task 3: Verify TypeScript and drizzle-kit</name>
  <files></files>
  <read_first></read_first>
  <action>
Run verification commands in sequence:

1. `npm run check` — TypeScript type checking. Must pass with zero errors. This validates:
   - All 6 domain files have correct imports
   - The barrel re-export resolves all types
   - Cross-domain FK imports (sales → auth, sales → cms) work
   - settings.ts pageSlugs import path (`../pageSlugs.js`) resolves correctly
   - All 64 consumer import sites still compile

2. `npm run db:push` — Drizzle-kit schema detection. Must connect to database and push schema without errors. Watch for:
   - "duplicate table name" warnings (indicates barrel file trap — check no index.ts in shared/schema/)
   - Connection errors (DATABASE_URL or POSTGRES_URL missing)
   - Schema diff errors (indicates missing table exports)

If `npm run check` fails:
- Read the error messages carefully
- Most likely causes: wrong import path in settings.ts, missing .js extension, or missing type import for FormConfig
- Fix the specific domain file and re-run

If `npm run db:push` fails with "duplicate table":
- Check for accidental `shared/schema/index.ts` — delete it if present
- Verify barrel file is at `shared/schema.ts` (NOT inside `shared/schema/`)

After both pass, verify the split with: `ls shared/schema/ && wc -l shared/schema/*.ts shared/schema.ts`
  </action>
  <acceptance_criteria>
    - `npm run check` exits with code 0 (zero TypeScript errors)
    - `npm run db:push` completes without "duplicate table" warnings
    - `ls shared/schema/` shows exactly 6 .ts files (auth, chat, cms, forms, sales, settings) and NO index.ts
    - Each domain file is under 300 lines
    - `shared/schema.ts` (barrel) is under 30 lines
  </acceptance_criteria>
  <verify>
    <automated>npm run check 2>&1; echo "EXIT: $?"</automated>
    Expected: "EXIT: 0"
  </verify>
  <done>TypeScript compiles without errors, drizzle-kit detects all tables from split files, no duplicate table warnings</done>
</task>

</tasks>

<verification>
Final verification checklist:
- [ ] 6 domain files exist under `shared/schema/` (no index.ts)
- [ ] `shared/schema.ts` is barrel re-export only (~6 lines)
- [ ] `drizzle.config.ts` uses `schema: "./shared/schema"`
- [ ] `npm run check` passes
- [ ] `npm run db:push` completes without errors
- [ ] Each domain file under 300 lines
- [ ] Cross-domain FKs intact: sales → auth (users), sales → cms (vcards)
- [ ] pageSlugs import in settings.ts uses `../pageSlugs.js`
</verification>

<success_criteria>
- Phase goal met: 1,004-line monolith split into 6 files under 300 lines each
- SCHM-01: 6 domain files created ✓
- SCHM-02: Barrel re-export preserves all 64 import sites ✓
- SCHM-03: drizzle.config.ts uses folder path ✓
- SCHM-04: npm run db:push detects schema changes correctly ✓
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-organization/03-03-SUMMARY.md`
</output>

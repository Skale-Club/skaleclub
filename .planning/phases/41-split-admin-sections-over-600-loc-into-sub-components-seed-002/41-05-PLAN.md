---
phase: 41-split-admin-sections-over-600-loc-into-sub-components-seed-002
plan: 05
type: execute
wave: 5
depends_on: [41-04]
files_modified:
  - client/src/components/admin/PortfolioSection.tsx
  - client/src/components/admin/portfolio/PortfolioServiceCard.tsx
  - client/src/components/admin/portfolio/PortfolioServiceForm.tsx
autonomous: true
requirements: [REFACTOR-41-05]
must_haves:
  truths:
    - "client/src/components/admin/PortfolioSection.tsx is ≤ 600 lines"
    - "Two new sub-components exist under client/src/components/admin/portfolio/"
    - "All React Query keys unchanged (['/api/portfolio-services'])"
    - "All data-testid attributes preserved (button-add-service, service-item-${id}, button-edit-service-${id}, button-delete-service-${id}, button-save-service)"
    - "Admin.tsx import of PortfolioSection still resolves (named export unchanged)"
    - "npm run check passes"
  artifacts:
    - path: "client/src/components/admin/PortfolioSection.tsx"
      provides: "Top-level Portfolio section: queries, mutations, drag-and-drop orchestration, list rendering"
      max_lines: 600
    - path: "client/src/components/admin/portfolio/PortfolioServiceCard.tsx"
      provides: "Sortable service card (image preview + identity + active badge + edit/delete actions)"
    - path: "client/src/components/admin/portfolio/PortfolioServiceForm.tsx"
      provides: "Service create/edit form rendered inside the dialog (image upload, identity, pricing, features, appearance)"
  key_links:
    - from: "client/src/components/admin/PortfolioSection.tsx"
      to: "client/src/components/admin/portfolio/PortfolioServiceCard.tsx"
      via: "import { PortfolioServiceCard } from './portfolio/PortfolioServiceCard'"
    - from: "client/src/components/admin/PortfolioSection.tsx"
      to: "client/src/components/admin/portfolio/PortfolioServiceForm.tsx"
      via: "import { PortfolioServiceForm } from './portfolio/PortfolioServiceForm'"
---

<objective>
Split `client/src/components/admin/PortfolioSection.tsx` (752 LOC) into the orchestrator parent plus two sub-components under `client/src/components/admin/portfolio/`:
- `PortfolioServiceCard` (formerly `SortableServiceCard`, lines 238-367)
- `PortfolioServiceForm` (formerly `ServiceForm`, lines 369-751 — by far the biggest chunk at ~380 LOC)

The form alone is large enough that extracting it gets the parent comfortably under 600 LOC.
</objective>

<context>
@CLAUDE.md
@client/src/components/admin/PortfolioSection.tsx

The folder `client/src/components/admin/portfolio/` does not exist yet — create it. The form holds all the visual sections (Identity, Pricing, Features, Appearance) and the image upload UI. It is purely controlled by `formData` state managed inside; the parent passes a `service` prop (or null for "new") and an `onSubmit` callback.
</context>

<interfaces>
Types already in parent:
```ts
import type { PortfolioService, InsertPortfolioService } from '@shared/schema';
```
Helpers used by both card and form:
```ts
import { uploadFileToServer, getOriginalImageUrl } from './shared/utils';
```
The card uses `getOriginalImageUrl`; the form uses both. Keep imports per-file.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract PortfolioServiceCard</name>
  <files>
    client/src/components/admin/portfolio/PortfolioServiceCard.tsx,
    client/src/components/admin/PortfolioSection.tsx
  </files>
  <action>
    Step A — create `client/src/components/admin/portfolio/PortfolioServiceCard.tsx`. Move the `SortableServiceCard` function (current PortfolioSection.tsx lines 238-367) into it, RENAMED to `PortfolioServiceCard` as a named export. The signature and behavior are unchanged:

      type PortfolioServiceCardProps = {
        service: PortfolioService;
        onEdit: (service: PortfolioService) => void;
        onDelete: (id: number) => void;
      };
      export function PortfolioServiceCard({ service, onEdit, onDelete }: PortfolioServiceCardProps): JSX.Element

    Imports for PortfolioServiceCard.tsx:
      import { useState } from 'react';
      import { GripVertical, Image, Pencil, Trash2 } from 'lucide-react';
      import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from '@/components/ui/alert-dialog';
      import { Badge } from '@/components/ui/badge';
      import { Button } from '@/components/ui/button';
      import { useSortable } from '@dnd-kit/sortable';
      import { CSS } from '@dnd-kit/utilities';
      import { getOriginalImageUrl } from '../shared/utils';
      import type { PortfolioService } from '@shared/schema';

    Preserve EXACTLY these data-testid attributes (verify with grep before/after):
      - `service-item-${service.id}` on the root div
      - `button-edit-service-${service.id}` on the edit button
      - `button-delete-service-${service.id}` on the trigger of the delete AlertDialog

    Step B — update parent `PortfolioSection.tsx`:
      - Remove the inline `SortableServiceCard` definition.
      - Add `import { PortfolioServiceCard } from './portfolio/PortfolioServiceCard';`
      - Update the `<SortableServiceCard ... />` usage inside the SortableContext map to `<PortfolioServiceCard ... />` (same props).

    Run `npm run check`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/PortfolioSection.tsx client/src/components/admin/portfolio/PortfolioServiceCard.tsx && echo "---testids in card---" && grep -E "data-testid" client/src/components/admin/portfolio/PortfolioServiceCard.tsx</automated>
  </verify>
  <done>
    - `PortfolioServiceCard.tsx` exists; all 3 data-testid template strings present.
    - `PortfolioSection.tsx` `wc -l` ≤ 620 (down from 752 after removing ~130 lines).
    - `npm run check` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract PortfolioServiceForm</name>
  <files>
    client/src/components/admin/portfolio/PortfolioServiceForm.tsx,
    client/src/components/admin/PortfolioSection.tsx
  </files>
  <action>
    Step A — create `client/src/components/admin/portfolio/PortfolioServiceForm.tsx`. Move the `ServiceForm` function (current PortfolioSection.tsx lines 369-751) into it, RENAMED to `PortfolioServiceForm` as a named export. Signature unchanged:

      type PortfolioServiceFormProps = {
        service: PortfolioService | null;
        onSubmit: (data: Partial<InsertPortfolioService>) => void;
        isLoading: boolean;
        nextOrder: number;
      };
      export function PortfolioServiceForm({ service, onSubmit, isLoading, nextOrder }: PortfolioServiceFormProps): JSX.Element

    Imports for PortfolioServiceForm.tsx:
      import { useState } from 'react';
      import { Image, Trash2 } from 'lucide-react';
      import { Badge } from '@/components/ui/badge';
      import { Button } from '@/components/ui/button';
      import { DialogClose, DialogTitle } from '@/components/ui/dialog';
      import { Input } from '@/components/ui/input';
      import { Label } from '@/components/ui/label';
      import { Loader2 } from '@/components/ui/loader';
      import { Switch } from '@/components/ui/switch';
      import { Textarea } from '@/components/ui/textarea';
      import { useToast } from '@/hooks/use-toast';
      import { uploadFileToServer, getOriginalImageUrl } from '../shared/utils';
      import type { InsertPortfolioService, PortfolioService } from '@shared/schema';

    Preserve EXACTLY this data-testid:
      - `button-save-service` on the submit button (was at original line 436).

    Step B — update parent `PortfolioSection.tsx`:
      - Remove the inline `ServiceForm` definition (lines 369-751).
      - Add `import { PortfolioServiceForm } from './portfolio/PortfolioServiceForm';`
      - Replace `<ServiceForm ... />` (at line 180 in the original `DialogContent`) with `<PortfolioServiceForm ... />` — same props.

    Run `npm run check`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/PortfolioSection.tsx client/src/components/admin/portfolio/*.tsx && echo "---testid count---" && grep -c "data-testid" client/src/components/admin/PortfolioSection.tsx client/src/components/admin/portfolio/*.tsx</automated>
  </verify>
  <done>
    - `PortfolioServiceForm.tsx` exists; the `button-save-service` data-testid is present.
    - `PortfolioSection.tsx` `wc -l` ≤ 250 (after removing the ~380-line form).
    - Sum of `data-testid` occurrences across the 3 files equals the original count from `git show HEAD:client/src/components/admin/PortfolioSection.tsx | grep -c data-testid`.
    - `npm run check` passes.
    - Named export `PortfolioSection` preserved.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `wc -l client/src/components/admin/PortfolioSection.tsx` ≤ 600 (target ≤ 250).
2. `npm run check` passes.
3. `grep -E "queryKey: \['/api/portfolio-services" client/src/components/admin/PortfolioSection.tsx` still shows the same query key.
4. `Admin.tsx` import of `PortfolioSection` is unchanged.
</verification>

<success_criteria>
- [x] Parent ≤ 600 LOC.
- [x] 2 new sub-components in `admin/portfolio/`.
- [x] `npm run check` passes.
- [x] All 5 `data-testid` values preserved.
- [x] React Query key unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/41-split-admin-sections-over-600-loc-into-sub-components-seed-002/41-05-SUMMARY.md`.
</output>

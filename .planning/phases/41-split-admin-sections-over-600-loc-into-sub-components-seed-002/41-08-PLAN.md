---
phase: 41-split-admin-sections-over-600-loc-into-sub-components-seed-002
plan: 08
type: execute
wave: 8
depends_on: [41-07]
files_modified:
  - client/src/components/admin/ChatSection.tsx
  - client/src/components/admin/chat/ChatBubble.tsx
  - client/src/components/admin/chat/ChatSortableObjectiveItem.tsx
  - client/src/components/admin/chat/ChatSettingsSheet.tsx
  - client/src/components/admin/chat/ChatConversationsListPanel.tsx
  - client/src/components/admin/chat/ChatConversationPanel.tsx
autonomous: true
requirements: [REFACTOR-41-08]
must_haves:
  truths:
    - "client/src/components/admin/ChatSection.tsx is ≤ 600 lines"
    - "Five new sub-components exist under client/src/components/admin/chat/"
    - "All React Query keys unchanged (/api/chat/settings, /api/company-settings, /api/chat/conversations, /api/integrations/openai, /api/integrations/gemini, /api/integrations/openrouter, /api/chat/response-time, /api/forms)"
    - "Streaming-message scroll-to-bottom logic stays co-located with the message panel"
    - "Admin.tsx import of ChatSection still resolves (named export unchanged)"
    - "npm run check passes"
  artifacts:
    - path: "client/src/components/admin/ChatSection.tsx"
      provides: "Top-level Chat section: settings state + queries + mutations + composition of sheet/list/panel"
      max_lines: 600
    - path: "client/src/components/admin/chat/ChatBubble.tsx"
      provides: "Single message bubble (assistant vs user styling, markdown rendering, timestamp)"
    - path: "client/src/components/admin/chat/ChatSortableObjectiveItem.tsx"
      provides: "Sortable item for the intake-objectives list inside the Settings Sheet"
    - path: "client/src/components/admin/chat/ChatSettingsSheet.tsx"
      provides: "Settings Sheet body: general/AI/lead-qualification sections (large form)"
    - path: "client/src/components/admin/chat/ChatConversationsListPanel.tsx"
      provides: "Left sidebar Card: search + open/archived tabs + conversation list"
    - path: "client/src/components/admin/chat/ChatConversationPanel.tsx"
      provides: "Right Card: chat header + messages area (with messagesEndRef scroll) + input area"
  key_links:
    - from: "client/src/components/admin/ChatSection.tsx"
      to: "client/src/components/admin/chat/*.tsx"
      via: "named imports from './chat/...'"
---

<objective>
Split `client/src/components/admin/ChatSection.tsx` (1018 LOC) into the orchestrator parent plus five sub-components under a new `admin/chat/` folder. Special care is needed for:
1. The streaming-message `messagesEndRef.scrollIntoView` effect — it stays with the message panel.
2. The Settings Sheet — it owns a LOT of form state. Keep `settingsDraft` ownership in the parent and pass `settingsDraft` + `updateField` + auxiliary handlers down to the sheet as props.

Output: 1 refactored parent + 5 new sibling files.
</objective>

<context>
@CLAUDE.md
@client/src/components/admin/ChatSection.tsx

The parent file's structure:
- Lines 49-90: `ChatBubble` inline component
- Lines 92-121: `SortableObjectiveItem` inline component (used inside settings sheet)
- Lines 123-985: main `ChatSection` component:
  - Lines 125-164: large block of `useState` declarations
  - Lines 166-262: 7 `useQuery` hooks + `defaultSystemPrompt` useMemo + response-time query
  - Lines 264-322: hydration effects (settings sync, cleanup)
  - Lines 324-420: callbacks (`saveSettings`, `updateField`, calendar staff handlers, toggleChat, URL rules CRUD, objectives DnD)
  - Lines 422-487: avatar upload + `openConversation` + status/delete mutations
  - Lines 497-552: derived values (assistantName, visibleConversations memo, pagination, scroll effect, handleSendMessage)
  - Lines 556-984: JSX (SectionHeader with embedded Sheet + main 2-column layout: conversations list + chat panel)
- Lines 987-1016: a SECOND `ObjectiveRow` component (appears unused — confirm with grep before deleting OR keep as separate file)

VERIFY before extracting: `grep -n "ObjectiveRow" client/src/components/admin/ChatSection.tsx` — if no other usages, the component is dead code that can be removed (but per the "no behavior change" rule, prefer to extract it into the same file as `ChatSortableObjectiveItem` to be safe).
</context>

<interfaces>
Types and constants already imported:
```ts
import { DEFAULT_CHAT_OBJECTIVES, SIDEBAR_MENU_ITEMS } from './shared/constants';
import type { ChatSettingsData, CompanySettingsData, ConversationMessage, ConversationSummary, IntakeObjective, UrlRule } from './shared/types';
import { ensureArray, uploadFileToServer } from './shared/utils';
```
These are all from the existing `./shared/` modules — sub-components should import directly from there (not re-export through the parent).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract ChatBubble + ChatSortableObjectiveItem</name>
  <files>
    client/src/components/admin/chat/ChatBubble.tsx,
    client/src/components/admin/chat/ChatSortableObjectiveItem.tsx,
    client/src/components/admin/ChatSection.tsx
  </files>
  <action>
    Step A — create folder `client/src/components/admin/chat/`.

    Step B — create `ChatBubble.tsx`. Move the `ChatBubble` function (current ChatSection.tsx lines 49-90) verbatim as a named export. Signature unchanged.

    Imports:
      import { MessageSquare } from 'lucide-react';
      import { renderMarkdown } from '@/lib/markdown';
      import { format } from 'date-fns';
      import type { ConversationMessage } from '../shared/types';

      export function ChatBubble({ message, assistantAvatar }: { message: ConversationMessage; assistantAvatar?: string }): JSX.Element

    Step C — create `ChatSortableObjectiveItem.tsx`. Move BOTH `SortableObjectiveItem` (lines 92-121) AND the second `ObjectiveRow` (lines 987-1016) into this file. Rename `SortableObjectiveItem` to `ChatSortableObjectiveItem` for clarity; keep `ObjectiveRow` as a non-exported helper for now (or export it if grep finds external uses).

    Imports:
      import { GripVertical } from 'lucide-react';
      import { useSortable } from '@dnd-kit/sortable';
      import { CSS } from '@dnd-kit/utilities';
      import { Switch } from '@/components/ui/switch';
      import type { IntakeObjective } from '../shared/types';

      export function ChatSortableObjectiveItem({ objective, onToggle }: { objective: IntakeObjective; onToggle: (enabled: boolean) => void }): JSX.Element

    Step D — update parent `ChatSection.tsx`:
      - Remove the inline `ChatBubble`, `SortableObjectiveItem`, and `ObjectiveRow` definitions.
      - Add `import { ChatBubble } from './chat/ChatBubble';`
      - Add `import { ChatSortableObjectiveItem } from './chat/ChatSortableObjectiveItem';`
      - Update the JSX inside the Settings Sheet that currently renders `<SortableObjectiveItem ... />` to `<ChatSortableObjectiveItem ... />`. Update the JSX inside the messages map that renders `<ChatBubble ... />` — already correct, just confirm the import path.

    Run `npm run check`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/ChatSection.tsx client/src/components/admin/chat/*.tsx</automated>
  </verify>
  <done>
    - Both new files exist with named exports.
    - `ChatSection.tsx` `wc -l` ≤ 920 (down from 1018 after removing ~95 lines).
    - `npm run check` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract ChatConversationsListPanel + ChatConversationPanel</name>
  <files>
    client/src/components/admin/chat/ChatConversationsListPanel.tsx,
    client/src/components/admin/chat/ChatConversationPanel.tsx,
    client/src/components/admin/ChatSection.tsx
  </files>
  <action>
    Step A — create `ChatConversationsListPanel.tsx`. Move the entire left sidebar `<Card className="w-80 md:w-96 flex flex-col ...">...</Card>` (current ChatSection.tsx lines 801-873) into a named-export component.

    Props (parent retains conversations data + filters):
      type ChatConversationsListPanelProps = {
        conversations: ConversationSummary[] | undefined; // for the open/closed counts
        visibleConversations: ConversationSummary[];
        loadingConversations: boolean;
        selectedConversation: ConversationSummary | null;
        searchTerm: string;
        statusFilter: 'open' | 'closed' | 'all';
        onSearchChange: (v: string) => void;
        onStatusFilterChange: (s: 'open' | 'closed' | 'all') => void;
        onSelect: (conv: ConversationSummary) => void;
        onRefresh: () => void;
      };
      export function ChatConversationsListPanel(props: ChatConversationsListPanelProps): JSX.Element

    The `openConversations` / `closedConversations` counts are derived from `conversations` inside the panel. The parent passes the raw + filtered arrays.

    Imports:
      import { MessageSquare, RotateCcw, Search } from 'lucide-react';
      import { Card } from '@/components/ui/card';
      import { Button } from '@/components/ui/button';
      import { Input } from '@/components/ui/input';
      import { Loader2 } from '@/components/ui/loader';
      import { EmptyState } from '../shared';
      import { clsx } from 'clsx';
      import { format } from 'date-fns';
      import type { ConversationSummary } from '../shared/types';

    Step B — create `ChatConversationPanel.tsx`. Move the right-side chat `<Card className="flex-1 flex flex-col rounded-2xl shadow-none overflow-hidden">...</Card>` (current ChatSection.tsx lines 876-975) into a named-export component.

    CRITICAL: the `messagesEndRef` AND the scroll-to-bottom `useEffect` (lines 544-548) MUST travel into this file. The parent passes `messages`, `isMessagesLoading`, and `selectedConversation`; the panel owns the ref locally.

    Props:
      type ChatConversationPanelProps = {
        selectedConversation: ConversationSummary | null;
        messages: ConversationMessage[];
        isMessagesLoading: boolean;
        assistantAvatar: string;
        onToggleStatus: () => void;
        onDelete: () => void;
        newMessage: string;
        onNewMessageChange: (v: string) => void;
        onSend: () => void;
      };
      export function ChatConversationPanel(props: ChatConversationPanelProps): JSX.Element

    Internally:
      - declare `const messagesEndRef = useRef<HTMLDivElement>(null);`
      - declare the scroll-to-bottom `useEffect` triggered on `[messages, isMessagesLoading]`
      - render the AlertDialog for delete confirmation inline (the parent provides `onDelete` which fires the mutation)

    Imports:
      import { useEffect, useRef } from 'react';
      import { Archive, MessageSquare, RotateCcw, Send, Trash2 } from 'lucide-react';
      import { Card } from '@/components/ui/card';
      import { Button } from '@/components/ui/button';
      import { Textarea } from '@/components/ui/textarea';
      import { Loader2 } from '@/components/ui/loader';
      import { AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription, AlertDialogFooter, AlertDialogHeader, AlertDialogTitle, AlertDialogTrigger } from '@/components/ui/alert-dialog';
      import { ChatBubble } from './ChatBubble';
      import type { ConversationMessage, ConversationSummary } from '../shared/types';

    Step C — update parent `ChatSection.tsx`:
      - Remove `messagesEndRef`, the scroll-to-bottom effect (lines 544-548).
      - Remove the two large `<Card>` blocks (lines 801-873 and 876-975).
      - Add `import { ChatConversationsListPanel } from './chat/ChatConversationsListPanel';` and `import { ChatConversationPanel } from './chat/ChatConversationPanel';`
      - Replace the JSX with:

          <div className="flex flex-1 min-h-0 gap-4 overflow-hidden">
            <ChatConversationsListPanel
              conversations={conversations}
              visibleConversations={visibleConversations}
              loadingConversations={loadingConversations}
              selectedConversation={selectedConversation}
              searchTerm={searchTerm}
              statusFilter={statusFilter}
              onSearchChange={setSearchTerm}
              onStatusFilterChange={setStatusFilter}
              onSelect={openConversation}
              onRefresh={() => refetchConversations()}
            />
            <ChatConversationPanel
              selectedConversation={selectedConversation}
              messages={messages}
              isMessagesLoading={isMessagesLoading}
              assistantAvatar={assistantAvatar}
              onToggleStatus={() => selectedConversation && statusMutation.mutate({ id: selectedConversation.id, status: selectedConversation.status === 'open' ? 'closed' : 'open' })}
              onDelete={() => selectedConversation && deleteMutation.mutate(selectedConversation.id)}
              newMessage={newMessage}
              onNewMessageChange={setNewMessage}
              onSend={handleSendMessage}
            />
          </div>

    Run `npm run check`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/ChatSection.tsx client/src/components/admin/chat/*.tsx && echo "---scroll ref---" && grep -n "messagesEndRef\\|scrollIntoView" client/src/components/admin/ChatSection.tsx client/src/components/admin/chat/ChatConversationPanel.tsx</automated>
  </verify>
  <done>
    - Both new panels exist with named exports.
    - `messagesEndRef` and `scrollIntoView` ONLY appear in `ChatConversationPanel.tsx` (parent grep is empty).
    - `ChatSection.tsx` `wc -l` ≤ 750.
    - `npm run check` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Extract ChatSettingsSheet</name>
  <files>
    client/src/components/admin/chat/ChatSettingsSheet.tsx,
    client/src/components/admin/ChatSection.tsx
  </files>
  <action>
    Step A — create `ChatSettingsSheet.tsx`. Move the entire `<Sheet open={settingsOpen} ...>...</Sheet>` block including the SheetTrigger Button (current ChatSection.tsx lines 563-795) into a named-export component.

    Because this form is LARGE and reads many settings + integration query results, design the props to be a thin pass-through:

      type ChatSettingsSheetProps = {
        open: boolean;
        onOpenChange: (open: boolean) => void;
        settingsDraft: ChatSettingsData;
        onSettingsDraftChange: React.Dispatch<React.SetStateAction<ChatSettingsData>>;
        updateField: <K extends keyof ChatSettingsData>(field: K, value: ChatSettingsData[K]) => void;
        onToggleChat: (checked: boolean) => Promise<void>;
        isUploadingAvatar: boolean;
        avatarFileInputRef: React.RefObject<HTMLInputElement>;
        onAvatarUpload: (e: React.ChangeEvent<HTMLInputElement>) => Promise<void>;
        formsList: Array<{ id: number; slug: string; name: string; isDefault: boolean; isActive: boolean }> | undefined;
        openaiSettings: { enabled: boolean; hasKey: boolean } | undefined;
        geminiSettings: { enabled: boolean; hasKey: boolean } | undefined;
        openRouterSettings: { enabled: boolean; hasKey: boolean } | undefined;
        objectivesSensors: ReturnType<typeof import('@dnd-kit/core').useSensors>;
        onObjectivesDragEnd: (event: import('@dnd-kit/core').DragEndEvent) => void;
        onToggleObjective: (id: IntakeObjective['id'], enabled: boolean) => void;
        // URL rules
        onAddRule: () => void;
        onUpdateRule: (index: number, field: keyof UrlRule, value: string) => void;
        onRemoveRule: (index: number) => void;
      };
      export function ChatSettingsSheet(props: ChatSettingsSheetProps): JSX.Element

    Note: the original code stores `setSettingsDraft(prev => ({ ...prev, intakeObjectives: DEFAULT_CHAT_OBJECTIVES }))` in the Reset Defaults button — use `onSettingsDraftChange` for that one-off.

    Imports:
      import { LayoutGrid, Settings, User } from 'lucide-react';
      import { SiGoogle, SiOpenai } from 'react-icons/si';
      import { Link } from 'wouter';
      import { DndContext, closestCenter } from '@dnd-kit/core';
      import { SortableContext, verticalListSortingStrategy } from '@dnd-kit/sortable';
      import { Button } from '@/components/ui/button';
      import { Input } from '@/components/ui/input';
      import { Label } from '@/components/ui/label';
      import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
      import { Sheet, SheetContent, SheetDescription, SheetHeader, SheetTitle, SheetTrigger } from '@/components/ui/sheet';
      import { Switch } from '@/components/ui/switch';
      import { Textarea } from '@/components/ui/textarea';
      import { Loader2 } from '@/components/ui/loader';
      import { clsx } from 'clsx';
      import { DEFAULT_CHAT_OBJECTIVES } from '../shared/constants';
      import { ChatSortableObjectiveItem } from './ChatSortableObjectiveItem';
      import type { ChatSettingsData, IntakeObjective, UrlRule } from '../shared/types';

    Step B — update parent `ChatSection.tsx`:
      - Remove the entire `<Sheet open={settingsOpen} ...>...</Sheet>` block from the SectionHeader action.
      - Replace the `action={...}` prop on `SectionHeader` with `action={<ChatSettingsSheet ... />}` passing all props listed above.
      - Add `import { ChatSettingsSheet } from './chat/ChatSettingsSheet';`
      - Keep all state declarations, queries, mutations, callbacks, refs in the parent — the parent's job is now COMPOSITION.

    Run `npm run check` and confirm parent ≤ 600.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/ChatSection.tsx client/src/components/admin/chat/*.tsx</automated>
  </verify>
  <done>
    - `ChatSettingsSheet.tsx` exists with named export.
    - `ChatSection.tsx` `wc -l` ≤ 530 (target — settings sheet was ~230 lines).
    - `npm run check` passes.
    - Named export `ChatSection` preserved.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `wc -l client/src/components/admin/ChatSection.tsx` ≤ 600.
2. `npm run check` passes.
3. `grep -E "queryKey: \[" client/src/components/admin/ChatSection.tsx` still shows the same 7 distinct query keys (`/api/chat/settings`, `/api/company-settings`, `/api/chat/conversations`, `/api/integrations/openai`, `/api/integrations/gemini`, `/api/forms`, `/api/integrations/openrouter`, `/api/chat/response-time`).
4. `Admin.tsx` import of `ChatSection` is unchanged.
</verification>

<success_criteria>
- [x] Parent ≤ 600 LOC.
- [x] 5 new sub-components in `admin/chat/`.
- [x] `npm run check` passes.
- [x] `messagesEndRef` scroll-to-bottom logic lives only in `ChatConversationPanel.tsx`.
- [x] React Query keys unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/41-split-admin-sections-over-600-loc-into-sub-components-seed-002/41-08-SUMMARY.md`.
</output>

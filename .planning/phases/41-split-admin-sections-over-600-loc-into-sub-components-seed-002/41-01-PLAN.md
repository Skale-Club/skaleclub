---
phase: 41-split-admin-sections-over-600-loc-into-sub-components-seed-002
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/admin/LeadsSection.tsx
  - client/src/components/admin/leads/LeadsTable.tsx
  - client/src/components/admin/leads/LeadDetailDialog.tsx
autonomous: true
requirements: [REFACTOR-41-01]
must_haves:
  truths:
    - "client/src/components/admin/LeadsSection.tsx is ≤ 600 lines"
    - "Two new files exist under client/src/components/admin/leads/ that own the table body and the detail dialog body"
    - "All data-testid attributes are preserved verbatim"
    - "All React Query keys are unchanged (['/api/forms'], ['/api/form-leads', filters])"
    - "Admin.tsx import of LeadsSection still resolves (named export unchanged)"
    - "npm run check passes"
  artifacts:
    - path: "client/src/components/admin/LeadsSection.tsx"
      provides: "Top-level Leads section component, named export LeadsSection"
      max_lines: 600
    - path: "client/src/components/admin/leads/LeadsTable.tsx"
      provides: "Table rendering of the leads list (rows, status select per-row, action buttons)"
    - path: "client/src/components/admin/leads/LeadDetailDialog.tsx"
      provides: "Lead detail Dialog body (detail items grid, form responses list, badges)"
  key_links:
    - from: "client/src/components/admin/LeadsSection.tsx"
      to: "client/src/components/admin/leads/LeadsTable.tsx"
      via: "import { LeadsTable } from './leads/LeadsTable'"
    - from: "client/src/components/admin/LeadsSection.tsx"
      to: "client/src/components/admin/leads/LeadDetailDialog.tsx"
      via: "import { LeadDetailDialog } from './leads/LeadDetailDialog'"
---

<objective>
Split `client/src/components/admin/LeadsSection.tsx` (currently 618 LOC) into the top-level section file plus two cohesive sub-components under `client/src/components/admin/leads/`. Pure mechanical refactor — no behavior, no styling, no React Query key, no `data-testid`, no prop change. End state: parent file ≤ 600 LOC, full test/type pass.

Purpose: Honor the CLAUDE.md hard cap of 600 lines per file and pave the way for Phase 42 dynamic imports.

Output:
- 1 refactored parent file (`LeadsSection.tsx`)
- 2 new sub-component files in `admin/leads/`
</objective>

<context>
@CLAUDE.md
@client/src/components/admin/LeadsSection.tsx
@client/src/components/admin/leads/FormEditorContent.tsx

The `admin/leads/` folder already exists and contains form-editor helpers (`FormEditorContent.tsx`, `QuestionForm.tsx`, `SortableQuestionItem.tsx`, `ThresholdsForm.tsx`) — follow the same naming/exports style.

The parent file currently contains everything inline: state, two `useQuery` hooks (forms + leads), two `useMutation` (delete + update status), a large table block, a delete-confirm `AlertDialog`, and a 90-line lead-detail `Dialog`.
</context>

<interfaces>
Key types the executor needs (already imported by parent):

```ts
import type { Form, FormConfig, FormLead, FormQuestion, LeadClassification, LeadStatus } from '@shared/schema';
import { DEFAULT_FORM_CONFIG, getSortedQuestions } from '@shared/form';
import { getLeadClassificationLabel, getLeadStatusLabel } from '@/lib/leadDisplay';
```

Shared display helpers (defined inline in current `LeadsSection.tsx`) that BOTH sub-components need — extract them into a new file or co-locate. Recommended: keep them in the parent file and pass as props to the children:

- `formatDate(value?: string | null) => string`
- `classificationBadgeClass(c?: LeadClassification | null) => string`
- `questionLabel(lead: FormLead) => string`
- `getCompletionStatus(lead) / completionStatusLabel / completionStatusClass`
- `getQuestionsForLead(lead) => FormQuestion[]`
- `getAnswerForQuestion(lead, q) / getConditionalAnswer(lead, q)`
- `ghlBadgeClass(status?: string | null) => string`

PREFERRED: create a colocated helper module `client/src/components/admin/leads/leadDisplayHelpers.ts` that exports these pure functions (no React state). Both `LeadsTable` and `LeadDetailDialog` import from it. This keeps the parent under 600 LOC.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract leadDisplayHelpers + LeadsTable</name>
  <files>
    client/src/components/admin/leads/leadDisplayHelpers.ts,
    client/src/components/admin/leads/LeadsTable.tsx,
    client/src/components/admin/LeadsSection.tsx
  </files>
  <action>
    Step A — create `client/src/components/admin/leads/leadDisplayHelpers.ts`. Move these PURE functions out of `LeadsSection.tsx` verbatim (lines 178-258 of the current file). They must NOT close over component state — they only take `lead` / `formsById` / `status` arguments:

      export function formatDate(value?: string | null): string
      export function classificationBadgeClass(c?: LeadClassification | null): string
      export function ghlBadgeClass(status?: string | null): string
      export function questionLabel(lead: FormLead, formsById: Map<number, Form>): string
      export function getCompletionStatus(lead: FormLead): 'completo' | 'em_progresso' | 'abandonado'
      export function completionStatusLabel(status: 'completo' | 'em_progresso' | 'abandonado'): string
      export function completionStatusClass(status: 'completo' | 'em_progresso' | 'abandonado'): string
      export function getQuestionsForLead(lead: FormLead, formsById: Map<number, Form>): FormQuestion[]
      export function getConfigForLead(lead: FormLead, formsById: Map<number, Form>): FormConfig
      export function getLeadFieldValue(lead: FormLead, fieldId: string): string
      export function getAnswerForQuestion(lead: FormLead, question: FormQuestion): string
      export function getConditionalAnswer(lead: FormLead, question: FormQuestion): string

    Note: the current `getQuestionsForLead` closes over the parent's `formsById`. Change its signature to take `formsById` as a second argument and pass it in from both parent and child sites.

    Imports for the helpers file:
      import { DEFAULT_FORM_CONFIG, getSortedQuestions } from '@shared/form';
      import { format } from 'date-fns';
      import type { Form, FormConfig, FormLead, FormQuestion, LeadClassification } from '@shared/schema';

    Step B — create `client/src/components/admin/leads/LeadsTable.tsx`. Move the `<div className="overflow-x-auto"><table>...</table></div>` block (current LeadsSection.tsx lines 379-481) into a default-exported `LeadsTable` named-export component:

      type LeadsTableProps = {
        leads: FormLead[] | undefined;
        isLoading: boolean;
        formsById: Map<number, Form>;
        hasMultipleForms: boolean;
        statusOptions: { value: LeadStatus | 'all'; label: string }[];
        onOpenLead: (lead: FormLead) => void;
        onRequestDelete: (lead: FormLead) => void;
        onStatusChange: (id: number, status: LeadStatus) => void;
        isDeletePending: boolean;
      };
      export function LeadsTable(props: LeadsTableProps): JSX.Element

    Inside, call `questionLabel(lead, formsById)`, `getCompletionStatus(lead)`, etc. from the helpers module. Preserve every JSX attribute INCLUDING `data-testid` (none in the table block currently, but verify with grep before commit).

    Imports for LeadsTable.tsx:
      import { Eye, Trash2 } from 'lucide-react';
      import { Badge } from '@/components/ui/badge';
      import { Button } from '@/components/ui/button';
      import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '@/components/ui/select';
      import { Loader2 } from '@/components/ui/loader';
      import { clsx } from 'clsx';
      import { getLeadClassificationLabel } from '@/lib/leadDisplay';
      import type { Form, FormLead, LeadStatus } from '@shared/schema';
      import {
        formatDate, classificationBadgeClass, questionLabel,
        getCompletionStatus, completionStatusLabel, completionStatusClass,
      } from './leadDisplayHelpers';

    Step C — update parent `LeadsSection.tsx`:
      - Delete the inline helper functions (lines 178-258) — they now live in the helpers module.
      - Delete the inline `<table>` block (lines 379-481).
      - Add at top: `import { LeadsTable } from './leads/LeadsTable';`
      - Add at top: `import { ghlBadgeClass, getAnswerForQuestion, getConditionalAnswer, getQuestionsForLead, classificationBadgeClass, formatDate, questionLabel } from './leads/leadDisplayHelpers';`
      - Replace the deleted table block with:

          <div className="rounded-2xl border bg-card shadow-sm overflow-hidden">
            <div className="p-4 border-b border-border flex flex-col lg:flex-row gap-3 lg:items-center lg:justify-between">
              {/* keep the existing filters block unchanged (current lines 313-377) */}
            </div>
            <LeadsTable
              leads={leads}
              isLoading={isLoading}
              formsById={formsById}
              hasMultipleForms={hasMultipleForms}
              statusOptions={statusOptions}
              onOpenLead={openLeadDialog}
              onRequestDelete={setLeadPendingDelete}
              onStatusChange={(id, status) => updateLead.mutate({ id, status })}
              isDeletePending={deleteLead.isPending}
            />
          </div>

      - Update the dialog body (lines 517-613) to use the helper-module imports for `formatDate`, `classificationBadgeClass`, `questionLabel`, `getAnswerForQuestion`, `getConditionalAnswer`, `ghlBadgeClass`. (Pass `formsById` to `questionLabel` / `getQuestionsForLead`.)
      - Keep `selectedLeadQuestions` memo, but change its body to: `useMemo(() => selectedLead ? getQuestionsForLead(selectedLead, formsById) : [], [selectedLead, formsById])`.

    Run `npm run check` after Step C to confirm types match.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/LeadsSection.tsx client/src/components/admin/leads/LeadsTable.tsx client/src/components/admin/leads/leadDisplayHelpers.ts</automated>
  </verify>
  <done>
    - `client/src/components/admin/leads/leadDisplayHelpers.ts` exists and exports the pure helpers listed above.
    - `client/src/components/admin/leads/LeadsTable.tsx` exists and exports `LeadsTable`.
    - `client/src/components/admin/LeadsSection.tsx` `wc -l` reports ≤ 460 lines (after extracting the helpers and table, ~160 lines removed).
    - `npm run check` reports 0 new errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract LeadDetailDialog</name>
  <files>
    client/src/components/admin/leads/LeadDetailDialog.tsx,
    client/src/components/admin/LeadsSection.tsx
  </files>
  <action>
    Step A — create `client/src/components/admin/leads/LeadDetailDialog.tsx`. Move the entire `<Dialog open={isLeadDialogOpen} ...>...</Dialog>` block (current LeadsSection.tsx lines 517-613) into a named-export component:

      type LeadDetailDialogProps = {
        open: boolean;
        onOpenChange: (open: boolean) => void;
        lead: FormLead | null;
        formsById: Map<number, Form>;
        hasMultipleForms: boolean;
      };
      export function LeadDetailDialog({ open, onOpenChange, lead, formsById, hasMultipleForms }: LeadDetailDialogProps): JSX.Element

    Inside, derive `questions = lead ? getQuestionsForLead(lead, formsById) : []` and `extraCustomAnswers` locally (move the `useMemo` from the parent). The `DetailItem` helper is also moved into this file (was inline at lines 266-271).

    Imports for LeadDetailDialog.tsx:
      import { useMemo, type ReactNode } from 'react';
      import { Badge } from '@/components/ui/badge';
      import { Dialog, DialogContent, DialogHeader, DialogTitle } from '@/components/ui/dialog';
      import { clsx } from 'clsx';
      import { getLeadClassificationLabel, getLeadStatusLabel } from '@/lib/leadDisplay';
      import type { Form, FormLead } from '@shared/schema';
      import {
        formatDate, classificationBadgeClass, ghlBadgeClass, questionLabel,
        getAnswerForQuestion, getConditionalAnswer, getQuestionsForLead,
      } from './leadDisplayHelpers';

    Step B — update parent `LeadsSection.tsx`:
      - Delete the inline DetailItem helper (lines 266-271).
      - Delete the inline `extraCustomAnswers` memo (lines 260-264) — it now lives in the child.
      - Delete the inline `<Dialog>...</Dialog>` block (lines 517-613).
      - Add at top: `import { LeadDetailDialog } from './leads/LeadDetailDialog';`
      - Replace the deleted dialog with:

          <LeadDetailDialog
            open={isLeadDialogOpen}
            onOpenChange={setIsLeadDialogOpen}
            lead={selectedLead}
            formsById={formsById}
            hasMultipleForms={hasMultipleForms}
          />

      - Also remove the now-unused `selectedLeadQuestions` memo, the `getAnswerForQuestion`, `getConditionalAnswer`, `getConfigForLead`, `getQuestionsForLead`, `getLeadFieldValue` imports from the parent (only the dialog used them).
      - Keep `ghlBadgeClass` / `classificationBadgeClass` / `questionLabel` etc. ONLY if the parent's remaining JSX still references them. Otherwise remove.

    Run `wc -l client/src/components/admin/LeadsSection.tsx` — must be ≤ 380.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/LeadsSection.tsx client/src/components/admin/leads/LeadDetailDialog.tsx && echo "---testids---" && grep -c "data-testid" client/src/components/admin/LeadsSection.tsx client/src/components/admin/leads/*.tsx</automated>
  </verify>
  <done>
    - `client/src/components/admin/leads/LeadDetailDialog.tsx` exists and exports `LeadDetailDialog`.
    - `client/src/components/admin/LeadsSection.tsx` `wc -l` ≤ 380.
    - Total `data-testid` count across these files is unchanged versus pre-refactor (originally 0 in this section — confirm with grep on git HEAD).
    - `npm run check` passes.
    - Manual smoke (out of scope for this plan): opening `/admin/leads` still shows the same table with the same row actions and the same detail dialog content.
  </done>
</task>

</tasks>

<verification>
After Task 2 completes, the parent file must satisfy ALL of:
1. `wc -l client/src/components/admin/LeadsSection.tsx` ≤ 600.
2. `npm run check` passes with no new TypeScript errors.
3. `grep -c "data-testid" client/src/components/admin/LeadsSection.tsx client/src/components/admin/leads/*.tsx` returns a total ≥ the value of `git show HEAD:client/src/components/admin/LeadsSection.tsx | grep -c data-testid` (preservation rule).
4. `grep -c "queryKey" client/src/components/admin/LeadsSection.tsx` matches the original 2 (`/api/forms`, `/api/form-leads`).
5. `Admin.tsx` import line for `LeadsSection` is unchanged.
</verification>

<success_criteria>
LeadsSection refactor complete when:
- [x] `wc -l client/src/components/admin/LeadsSection.tsx` ≤ 600
- [x] 3 new files exist in `client/src/components/admin/leads/`: `leadDisplayHelpers.ts`, `LeadsTable.tsx`, `LeadDetailDialog.tsx`
- [x] `npm run check` passes
- [x] React Query keys unchanged
- [x] `data-testid` attributes unchanged
- [x] Named export `LeadsSection` from `LeadsSection.tsx` unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/41-split-admin-sections-over-600-loc-into-sub-components-seed-002/41-01-SUMMARY.md`.
</output>

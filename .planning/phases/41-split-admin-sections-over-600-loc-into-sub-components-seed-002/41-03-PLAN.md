---
phase: 41-split-admin-sections-over-600-loc-into-sub-components-seed-002
plan: 03
type: execute
wave: 3
depends_on: [41-02]
files_modified:
  - client/src/components/admin/SkaleHubSection.tsx
  - client/src/components/admin/hub/HubLiveLineupCard.tsx
  - client/src/components/admin/hub/HubParticipationSnapshotCard.tsx
  - client/src/components/admin/hub/HubParticipantHistoryCard.tsx
  - client/src/components/admin/hub/HubLiveFormDialog.tsx
autonomous: true
requirements: [REFACTOR-41-03]
must_haves:
  truths:
    - "client/src/components/admin/SkaleHubSection.tsx is ≤ 600 lines"
    - "Four new sub-components exist under client/src/components/admin/hub/"
    - "All React Query keys unchanged (/api/skale-hub/dashboard, /api/skale-hub/lives, /api/skale-hub/lives/:id, /api/skale-hub/participants)"
    - "Admin.tsx import of SkaleHubSection still resolves (named export unchanged)"
    - "npm run check passes"
  artifacts:
    - path: "client/src/components/admin/SkaleHubSection.tsx"
      provides: "Top-level Skale Hub orchestration: state, queries, mutations, composition"
      max_lines: 600
    - path: "client/src/components/admin/hub/HubLiveLineupCard.tsx"
      provides: "Lives list card (clickable rows, Edit / Activate buttons, per-row counters)"
    - path: "client/src/components/admin/hub/HubParticipationSnapshotCard.tsx"
      provides: "Selected-live participation card (3 stat tiles + recent registrations + live link CTA)"
    - path: "client/src/components/admin/hub/HubParticipantHistoryCard.tsx"
      provides: "Participant history card with search input and per-participant detail rows"
    - path: "client/src/components/admin/hub/HubLiveFormDialog.tsx"
      provides: "Create/Edit live Dialog with form fields + save mutation trigger via onSubmit callback"
  key_links:
    - from: "client/src/components/admin/SkaleHubSection.tsx"
      to: "client/src/components/admin/hub/*.tsx"
      via: "named imports from './hub/...'"
---

<objective>
Split `client/src/components/admin/SkaleHubSection.tsx` (655 LOC) into the section shell plus four cohesive cards/dialogs under `client/src/components/admin/hub/`. The shell keeps state, queries, and mutations; children are presentational and receive props.

Output: 1 refactored shell + 4 new files.
</objective>

<context>
@CLAUDE.md
@client/src/components/admin/SkaleHubSection.tsx

The folder `client/src/components/admin/hub/` does not yet exist — create it. The file currently exposes pure helpers at the top (`toDateTimeLocal`, `fromSummaryToForm`, `statusBadge`, `ghlStatusBadge`) and types (`LiveFormState`, `EMPTY_FORM`, `COMMON_TIMEZONES`). Keep helpers in the parent (small) or co-locate with the dialog (better — see Task 4 instructions).
</context>

<interfaces>
Types already in the file:
```ts
type HubStatus = 'draft' | 'scheduled' | 'live' | 'ended' | 'cancelled';
type HubLiveDetail = { live: HubLiveSummary; registrations: HubRegistrationSummary[]; accessEvents: HubAccessEvent[] };
type LiveFormState = { id: number | null; slug: string; title: string; description: string; startsAt: string; timezone: string; streamUrl: string; replayUrl: string; status: HubStatus; capacity: string };
```
Imported shared types:
```ts
import type { HubAccessEvent, HubDashboardSummary, HubLiveSummary, HubParticipantHistory, HubRegistrationSummary } from '@shared/schema';
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract HubLiveLineupCard + HubParticipationSnapshotCard</name>
  <files>
    client/src/components/admin/hub/HubLiveLineupCard.tsx,
    client/src/components/admin/hub/HubParticipationSnapshotCard.tsx,
    client/src/components/admin/SkaleHubSection.tsx
  </files>
  <action>
    Step A — create folder `client/src/components/admin/hub/` (use `mkdir` via Write tool side-effect when creating the first file).

    Step B — create `HubLiveLineupCard.tsx`. Move the `<AdminCard className="space-y-4">...</AdminCard>` containing the lives list (current SkaleHubSection.tsx lines 300-400) into it. Also move the `statusBadge` helper (lines 93-98) into this file.

    Props:
      type HubLiveLineupCardProps = {
        lives: HubLiveSummary[] | undefined;
        isLoading: boolean;
        selectedId: number | null;
        onSelect: (live: HubLiveSummary) => void;
        onEdit: (live: HubLiveSummary) => void;
        onActivate: (id: number) => void;
        onRefresh: () => void;
        isActivating: boolean;
      };
      export function HubLiveLineupCard(props: HubLiveLineupCardProps): JSX.Element

    Imports:
      import { CalendarDays, CheckCircle2, RefreshCcw } from 'lucide-react';
      import { AdminCard, EmptyState } from '../shared';
      import { Badge } from '@/components/ui/badge';
      import { Button } from '@/components/ui/button';
      import { Loader2 } from '@/components/ui/loader';
      import type { HubLiveSummary } from '@shared/schema';

      function statusBadge(status: string) { /* moved verbatim from parent */ }

    Step C — create `HubParticipationSnapshotCard.tsx`. Move the `<AdminCard className="space-y-4">...</AdminCard>` containing the participation snapshot (current SkaleHubSection.tsx lines 403-472) into it.

    Props:
      type HubParticipationSnapshotCardProps = {
        selectedId: number | null;
        detail: HubLiveDetail | undefined;
        isLoading: boolean;
        isFetching: boolean;
        selectedLive: HubLiveSummary | null;
      };
      export function HubParticipationSnapshotCard(props: HubParticipationSnapshotCardProps): JSX.Element

    The `HubLiveDetail` type is shared — move its declaration to a small shared file `client/src/components/admin/hub/hubTypes.ts`:
      export type HubLiveDetail = { live: HubLiveSummary; registrations: HubRegistrationSummary[]; accessEvents: HubAccessEvent[] };
      export type HubStatus = 'draft' | 'scheduled' | 'live' | 'ended' | 'cancelled';

    Import `HubLiveDetail` from there in both the parent and `HubParticipationSnapshotCard.tsx`.

    Imports for HubParticipationSnapshotCard.tsx:
      import { ExternalLink, Users } from 'lucide-react';
      import { AdminCard, EmptyState } from '../shared';
      import { Badge } from '@/components/ui/badge';
      import { Loader2 } from '@/components/ui/loader';
      import type { HubLiveSummary } from '@shared/schema';
      import type { HubLiveDetail } from './hubTypes';

    Step D — update parent `SkaleHubSection.tsx`:
      - Add imports for the two new components and the new `hubTypes` module.
      - Remove the inline `HubLiveDetail` / `HubStatus` declarations (now in hubTypes).
      - Remove the inline `statusBadge` helper.
      - Replace the two `<AdminCard>` blocks with:

          <HubLiveLineupCard
            lives={livesQuery.data}
            isLoading={livesQuery.isLoading}
            selectedId={selectedId}
            onSelect={(live) => { setSelectedId(live.id); setForm(fromSummaryToForm(live)); }}
            onEdit={openEditLiveDialog}
            onActivate={(id) => activateMutation.mutate(id)}
            onRefresh={() => livesQuery.refetch()}
            isActivating={activateMutation.isPending}
          />
          {/* ... */}
          <HubParticipationSnapshotCard
            selectedId={selectedId}
            detail={detailQuery.data}
            isLoading={detailQuery.isLoading}
            isFetching={detailQuery.isFetching}
            selectedLive={selectedLive}
          />

    Run `npm run check` and `wc -l`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/SkaleHubSection.tsx client/src/components/admin/hub/*.tsx client/src/components/admin/hub/*.ts</automated>
  </verify>
  <done>
    - `client/src/components/admin/hub/HubLiveLineupCard.tsx` exists.
    - `client/src/components/admin/hub/HubParticipationSnapshotCard.tsx` exists.
    - `client/src/components/admin/hub/hubTypes.ts` exists.
    - `SkaleHubSection.tsx` `wc -l` ≤ 480 (after ~175 lines removed).
    - `npm run check` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract HubParticipantHistoryCard + HubLiveFormDialog</name>
  <files>
    client/src/components/admin/hub/HubParticipantHistoryCard.tsx,
    client/src/components/admin/hub/HubLiveFormDialog.tsx,
    client/src/components/admin/SkaleHubSection.tsx
  </files>
  <action>
    Step A — create `HubParticipantHistoryCard.tsx`. Move the `<AdminCard className="space-y-4">...</AdminCard>` containing the participant history (current SkaleHubSection.tsx lines 474-549) into it. Move the `ghlStatusBadge` helper (lines 100-105) too.

    Props:
      type HubParticipantHistoryCardProps = {
        history: HubParticipantHistory[] | undefined;
        isLoading: boolean;
        searchValue: string;
        onSearchChange: (value: string) => void;
      };
      export function HubParticipantHistoryCard(props: HubParticipantHistoryCardProps): JSX.Element

    Imports:
      import { Search, Users } from 'lucide-react';
      import { AdminCard, EmptyState } from '../shared';
      import { Badge } from '@/components/ui/badge';
      import { Input } from '@/components/ui/input';
      import { Loader2 } from '@/components/ui/loader';
      import type { HubParticipantHistory } from '@shared/schema';

      function ghlStatusBadge(status?: string | null) { /* moved verbatim from parent */ }

    Step B — create `HubLiveFormDialog.tsx`. Move:
      - the `LiveFormState` type (currently parent line 32-43)
      - the `EMPTY_FORM` constant (lines 45-56)
      - the `COMMON_TIMEZONES` constant (lines 58-67)
      - the `toDateTimeLocal` helper (lines 69-76)
      - the `fromSummaryToForm` helper (lines 78-91)
      - the `<Dialog open={isLiveFormOpen} ...>...</Dialog>` block (lines 553-652)

    Re-export `fromSummaryToForm` so the parent can still call it when a row is selected:
      export { fromSummaryToForm, EMPTY_FORM };
      export type { LiveFormState };

    Props:
      type HubLiveFormDialogProps = {
        open: boolean;
        onOpenChange: (open: boolean) => void;
        form: LiveFormState;
        onFormChange: (next: LiveFormState | ((prev: LiveFormState) => LiveFormState)) => void;
        selectedLive: HubLiveSummary | null;
        onSubmit: () => void;
        isPending: boolean;
      };
      export function HubLiveFormDialog(props: HubLiveFormDialogProps): JSX.Element

    Internally the dialog reads `form` and calls `onFormChange((current) => ({ ...current, title: e.target.value }))`. The Reset button still calls `onFormChange(selectedLive ? fromSummaryToForm(selectedLive) : EMPTY_FORM)`. The Save button calls `onSubmit()` from the parent.

    Imports:
      import { Save } from 'lucide-react';
      import { Badge } from '@/components/ui/badge';
      import { Button } from '@/components/ui/button';
      import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from '@/components/ui/dialog';
      import { Input } from '@/components/ui/input';
      import { Label } from '@/components/ui/label';
      import { Textarea } from '@/components/ui/textarea';
      import { Loader2 } from '@/components/ui/loader';
      import type { HubLiveSummary } from '@shared/schema';
      import type { HubStatus } from './hubTypes';

    Step C — update parent `SkaleHubSection.tsx`:
      - Remove `LiveFormState`, `EMPTY_FORM`, `COMMON_TIMEZONES`, `toDateTimeLocal`, `fromSummaryToForm`, `ghlStatusBadge` from the parent.
      - Import: `import { HubParticipantHistoryCard } from './hub/HubParticipantHistoryCard';`
      - Import: `import { HubLiveFormDialog, fromSummaryToForm, EMPTY_FORM, type LiveFormState } from './hub/HubLiveFormDialog';`
      - Replace the participant history card with:

          <HubParticipantHistoryCard
            history={participantHistoryQuery.data}
            isLoading={participantHistoryQuery.isLoading}
            searchValue={participantSearch}
            onSearchChange={setParticipantSearch}
          />

      - Replace the dialog with:

          <HubLiveFormDialog
            open={isLiveFormOpen}
            onOpenChange={setIsLiveFormOpen}
            form={form}
            onFormChange={setForm}
            selectedLive={selectedLive}
            onSubmit={() => { if (canSave && !saveMutation.isPending) saveMutation.mutate(); }}
            isPending={saveMutation.isPending}
          />

    Run `npm run check` and `wc -l`.
  </action>
  <verify>
    <automated>cd "C:/Users/Vanildo/Dev/skaleclub" && npm run check 2>&1 | tail -30 && echo "---LOC---" && wc -l client/src/components/admin/SkaleHubSection.tsx client/src/components/admin/hub/*.tsx client/src/components/admin/hub/*.ts</automated>
  </verify>
  <done>
    - All four new files exist in `client/src/components/admin/hub/`.
    - `SkaleHubSection.tsx` `wc -l` ≤ 320 (target — the parent should be very lean after removing all four blocks).
    - `npm run check` passes.
    - Named export `SkaleHubSection` is preserved.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `wc -l client/src/components/admin/SkaleHubSection.tsx` ≤ 600.
2. `npm run check` passes.
3. `grep -rE "queryKey: \['/api/skale-hub/(dashboard|lives|participants)" client/src/components/admin/` shows the original 4 queries unchanged.
4. `Admin.tsx` import of `SkaleHubSection` is unchanged.
</verification>

<success_criteria>
- [x] Parent ≤ 600 LOC.
- [x] 4 new sub-components + 1 small types module in `admin/hub/`.
- [x] `npm run check` passes.
- [x] React Query keys unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/41-split-admin-sections-over-600-loc-into-sub-components-seed-002/41-03-SUMMARY.md`.
</output>

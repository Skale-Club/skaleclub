---
phase: 17-brand-guidelines
plan: 01
type: execute
wave: 1
depends_on: [15-01]
autonomous: true
requirements: [PRES-09, PRES-10]
---

<objective>
Implement the Brand Guidelines feature for v1.4 Admin Presentations Page:

1. PRES-09: `GET /api/brand-guidelines` (public, no auth) + `PUT /api/brand-guidelines` (admin-auth required). GET returns `{ content: string }` — empty string when no row. PUT validates string content and upserts via storage layer.

2. PRES-10: Admin **Brand Guidelines** sub-section accessible at `/admin/presentations` — plain textarea markdown editor that loads current guidelines and saves via PUT /api/brand-guidelines.

Output: schema file, storage methods, route file, admin UI component, translations, wired into Admin.tsx sidebar.
</objective>

<tasks>

<task id="1" type="auto">
Add presentations schema (brandGuidelines table + presentations + presentationViews) to shared/schema/presentations.ts, export from barrel, add getBrandGuidelines + upsertBrandGuidelines to IStorage interface and DatabaseStorage class, add SQL migration and runner script.
</task>

<task id="2" type="auto">
Create server/routes/brandGuidelines.ts with GET /api/brand-guidelines (public) and PUT /api/brand-guidelines (requireAdmin), register in server/routes.ts.
</task>

<task id="3" type="auto">
Create BrandGuidelinesSection.tsx admin component using AdminCard + Textarea + useQuery/useMutation pattern, add to AdminSection type, add Presentations sidebar entry, wire into Admin.tsx at /admin/presentations, add PT translations.
</task>

</tasks>

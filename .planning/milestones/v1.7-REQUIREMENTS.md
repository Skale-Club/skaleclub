# Requirements Archive: v1.7 Translation System Completeness

**Archived:** 2026-05-04
**Status:** SHIPPED

---

# Requirements: Skale Club Web Platform - v1.7 Translation System Completeness

**Defined:** 2026-05-03
**Core Value:** The entire site (public pages and admin panel) renders correctly in Portuguese with zero strings falling back to the API fallback — every visible string is covered by a static translation key in `translations.ts`.

## v1.7 Requirements

### Static Key Coverage

- [x] **TRX-01**: Every `t()` call in all client-side files maps to an existing key in `translations.pt` — no call references a key that is absent from `translations.ts`.
- [x] **TRX-02**: All `t()` keys in PresentationsSection, DashboardSection, EstimatesSection, LeadsSection, SEOSection, NewFormDialog, LinksSection, and `not-found.tsx` are defined and return correct PT strings.
- [x] **TRX-03**: All "Back to X" variant strings are consistently defined in `translations.ts` — no variant triggers an API fallback.

### Hardcoded String Elimination

- [x] **TRX-04**: No visible user-facing string in any admin component is hardcoded without going through `t()`.
- [x] **TRX-05**: All placeholder and label strings in admin forms use `t()`.

### Key Hygiene

- [x] **TRX-06**: All keys defined in `translations.ts` that are not referenced by any `t()` call are removed — zero dead keys.
- [x] **TRX-07**: `translations.ts` contains no duplicate keys.

### Build-Time Validation

- [x] **TRX-08**: TypeScript enforces that `t()` only accepts keys of type `TranslationKey` — passing an undefined key causes a compile-time error.
- [x] **TRX-09**: `npm run check` passes with zero TypeScript errors after all translation changes.

### Correctness

- [x] **TRX-10**: The wrong 404 page message key is fixed — `not-found.tsx` uses the correct key.
- [x] **TRX-11**: All PT translations are correct Brazilian Portuguese — no placeholder, English, or machine-translated strings left.

## Traceability

| Requirement | Phase | Status |
|-------------|-------|--------|
| TRX-01 | Phase 30 | ✅ Complete |
| TRX-02 | Phase 30 | ✅ Complete |
| TRX-03 | Phase 30 | ✅ Complete |
| TRX-04 | Phase 30 | ✅ Complete |
| TRX-05 | Phase 30 | ✅ Complete |
| TRX-06 | Phase 30 | ✅ Complete |
| TRX-07 | Phase 30 | ✅ Complete |
| TRX-08 | Phase 30 | ✅ Complete |
| TRX-09 | Phase 30 | ✅ Complete |
| TRX-10 | Phase 30 | ✅ Complete |
| TRX-11 | Phase 30 | ✅ Complete |

**Coverage:** 11/11 requirements — 100%

---
*Archived: 2026-05-04 after v1.7 milestone completion*

---
phase: quick-260906-g80
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/xphere-contract.ts
  - shared/schema/integrations.ts
  - shared/schema.ts
  - migrations/0054_xphere_lead_handoff.sql
  - scripts/migrate-xphere-lead-handoff.ts
  - scripts/verify-xphere-contract.ts
  - server/storage.ts
  - server/integrations/xphere.ts
  - server/lib/lead-processing.ts
  - server/routes/forms.ts
  - server/routes/integrations.ts
  - server/cron.ts
  - .env.example
  - client/src/components/admin/shared/types.ts
  - client/src/components/admin/integrations/XphereCard.tsx
  - client/src/components/admin/IntegrationsSection.tsx
  - client/src/components/LeadFormModal.tsx
  - client/src/pages/LeadThankYou.tsx
  - scripts/seed-barbershop-landing.ts
  - scripts/seed-barbershop-translations.ts
autonomous: true
requirements: [QUICK-260906-G80]

must_haves:
  truths:
    - "When the Xphere integration is enabled in admin and a lead completes a form, a `lead.captured` delivery row is created and POSTed to https://xphere.app/api/v1/leads with Idempotency-Key == payload.event_id; failures retry with backoff and land in dead_letter after permanent errors or 8 attempts"
    - "A lead submission NEVER fails because of Xphere (handoff is best-effort; errors are logged with xph_ tokens redacted)"
    - "Admin can paste an xph_ key, click Test connection and see the Xphere organization name, toggle the handoff on, configure booking (profile slug + in-person/online event slugs), see recent deliveries and retry a dead_letter one"
    - "When booking is enabled and configured, a barbershop lead who answered tipoVisita=presencial (or online) is redirected to /thankyou?booking=https://xphere.app/book/<profile>/<eventSlug>?name&email&phone and sees a prominent 'Schedule your visit' CTA; leads without a matching answer see the unchanged thank-you page"
    - "The thank-you page ignores any ?booking= value that does not start with https://xphere.app/ (open-redirect guard)"
    - "The barbershop form asks 'What's your shop address?' only when tipoVisita=presencial, in Portuguese on /barbershops-br"
    - "POST /api/integrations/xphere/sweep with Bearer CRON_SECRET reconciles missed completed leads and drains due deliveries; the in-process cron does the same every 5 minutes when not disabled"
    - "Leads from an unscored form (maxScore 0 / no option carries points, e.g. the barbershop form) are shipped with lead.score == null and lead.classification == null even though the DB row holds scoreTotal 0 / classificacao HOT; scored forms still ship the real score + enum classification"
    - "source.tenant_ref is read from xphere_settings.tenant_ref (admin-editable under Advanced in XphereCard, default 'skaleclub'), never from a code constant; source.product stays the module constant 'skaleclub'"
    - "scripts/verify-xphere-contract.ts exists and passes: fixture with product 'skaleclub' parses, 'skaleclub_websites' parses, an unknown product throws, an extra top-level field throws"
  artifacts:
    - path: "shared/xphere-contract.ts"
      provides: "xphereLeadEnvelopeSchema with product enum ['skaleclub_websites','skaleclub']"
      contains: "z.enum([\"skaleclub_websites\", \"skaleclub\"])"
    - path: "shared/schema/integrations.ts"
      provides: "xphereSettings + integrationDeliveries Drizzle tables, insert schema, types"
      exports: ["xphereSettings", "integrationDeliveries", "insertXphereSettingsSchema", "XphereSettings", "InsertXphereSettings", "IntegrationDelivery"]
    - path: "migrations/0054_xphere_lead_handoff.sql"
      provides: "xphere_settings (incl. tenant_ref text NOT NULL DEFAULT 'skaleclub') + integration_deliveries tables (no tenants FK)"
      contains: "CREATE TABLE IF NOT EXISTS integration_deliveries"
    - path: "scripts/migrate-xphere-lead-handoff.ts"
      provides: "Runner that applies 0054 and verifies both tables"
    - path: "scripts/verify-xphere-contract.ts"
      provides: "Contract regression check for shared/xphere-contract.ts (4 assertions, synthetic fixture only)"
      contains: "verify-xphere-contract"
    - path: "server/integrations/xphere.ts"
      provides: "validateKey, isUnscoredForm, serializeLeadForXphere, enqueueXphereLead, queueXphereDeliverySweep, listXphereDeliveries, retryXphereDelivery, reconcileMissingXphereDeliveries, cancelPendingXphereDeliveries, buildXphereBookingUrl"
      contains: "isUnscoredForm"
      min_lines: 200
    - path: "server/lib/lead-processing.ts"
      provides: "Step 4 Xphere handoff + bookingUrl in PostProcessResult"
      contains: "buildXphereBookingUrl"
    - path: "server/routes/integrations.ts"
      provides: "GET/PUT /api/integrations/xphere, POST .../test, GET .../deliveries, POST .../deliveries/:id/retry, POST .../sweep"
      contains: "/api/integrations/xphere/sweep"
    - path: "client/src/components/admin/integrations/XphereCard.tsx"
      provides: "Admin card: key/test/enable, visit booking config, recent deliveries + retry"
      min_lines: 150
    - path: "client/src/pages/LeadThankYou.tsx"
      provides: "Guarded booking CTA"
      contains: "https://xphere.app/"
  key_links:
    - from: "server/lib/lead-processing.ts"
      to: "server/integrations/xphere.ts"
      via: "enqueueXphereLead + buildXphereBookingUrl when lead.formCompleto"
      pattern: "enqueueXphereLead\\("
    - from: "server/routes/forms.ts"
      to: "client/src/components/LeadFormModal.tsx"
      via: "progress route responds { ...lead, bookingUrl }; handleFinish redirects with ?booking="
      pattern: "bookingUrl"
    - from: "server/integrations/xphere.ts"
      to: "https://xphere.app/api/v1/leads"
      via: "fetch with Idempotency-Key header equal to row.idempotencyKey"
      pattern: "Idempotency-Key"
    - from: "server/cron.ts"
      to: "server/integrations/xphere.ts"
      via: "5-minute setInterval lazy-importing queueXphereDeliverySweep"
      pattern: "queueXphereDeliverySweep"
    - from: "client/src/components/admin/IntegrationsSection.tsx"
      to: "client/src/components/admin/integrations/XphereCard.tsx"
      via: "rendered in CRM tab above GHLCard"
      pattern: "<XphereCard"
---

<objective>
Ship the approved Xphere integration (plan/xphere-booking-integration-plan.md) for the skaleclub marketing site: (1) a durable, idempotent lead handoff to `POST https://xphere.app/api/v1/leads` on form completion, ported from the skaleclub-websites reference implementation and adapted to this single-tenant repo; (2) a post-submit redirect to Xphere's public booking page chosen by the lead's `tipoVisita` answer, surfaced as a CTA on the thank-you page; (3) an admin card to configure it all; (4) the barbershop form's conditional shop-address field with pt-BR copy.

Purpose: Barbershop (and future niche) landing leads must land in Xphere as structured leads and be able to book their in-person/online visit without leaving the funnel, with no fire-and-forget data loss and no hardcoded tenant identifiers.

Output: new tables `xphere_settings` + `integration_deliveries` (applied to prod), `shared/xphere-contract.ts` + `scripts/verify-xphere-contract.ts`, `server/integrations/xphere.ts`, five admin routes + cron sweep, `XphereCard.tsx`, thank-you CTA, updated barbershop seeds (re-run), `npm run check` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@plan/xphere-booking-integration-plan.md
@C:/Users/Vanildo/Dev/skaleclub-websites/server/integrations/xphere.ts
@C:/Users/Vanildo/Dev/skaleclub-websites/shared/xphere-contract.ts
@C:/Users/Vanildo/Dev/skaleclub-websites/scripts/verify-xphere-contract.ts
@server/lib/lead-processing.ts
@server/routes/integrations.ts
@server/storage.ts
@server/cron.ts
@shared/schema/settings.ts
@client/src/components/admin/integrations/GHLCard.tsx
@client/src/components/LeadFormModal.tsx
@client/src/pages/LeadThankYou.tsx
@scripts/seed-barbershop-landing.ts
@scripts/seed-barbershop-translations.ts

<verified_facts>
- Xphere side is already unblocked: `product` accepts `'skaleclub'`; `https://xphere.app/book/<profileSlug>/<eventSlug>` accepts `?name=&email=&phone=` prefill (each trimmed, max 200 chars). `GET /api/v1/integration-info` returns `{ organization:{id,name}, scopes, capabilities:{ lead_ingestion, lead_schema_versions } }`. `POST /api/v1/leads`: 201 accepted / 200 duplicate / 409 conflict / 422 invalid / 413 >64KB; header `Idempotency-Key` MUST equal `payload.event_id`.
- Single tenant: `form_leads.tenant_id integer default 1`, NO `tenants` table. Settings tables are per-provider singletons with plaintext credentials (`telegram_settings` pattern, `storage.getTelegramSettings()/saveTelegramSettings()` at server/storage.ts:570-594). No pgcrypto.
- `form_leads` has sessionId (uuid unique), formId, nome/email/telefone, cidadeEstado, tipoNegocio, tipoNegocioOutro, tempoNegocio, experienciaMarketing, orcamentoAnuncios, principalDesafio, disponibilidade, expectativaResultado, scoreTotal, classificacao, urlOrigem, utmSource/utmMedium/utmCampaign, formCompleto, customAnswers jsonb, createdAt/updatedAt. NO utmContent/utmTerm/firstTouch*/lastTouch*.
- Progress route `POST /api/forms/slug/:slug/leads/progress` (server/routes/forms.ts:366-393) ends with `res.json(lead)`; `runLeadPostProcessing(initialLead, formConfig, companyName, visitorUuid?)` returns `{ lead }`. The skale-hub group route (forms.ts:~348) also calls it and only uses `lead`.
- `LeadFormModal.persistProgress` returns `FormLead | null`; `handleFinish` (line 938) redirects `window.location.href = pagePaths.thankYou`. `pagePaths` comes from `usePagePaths()` (`buildPagePaths` in shared/pageSlugs.ts, key `thankYou`).
- LeadFormModal conditional fields: `t(field.title)` is wrapped (line 466) but `placeholder={field.placeholder}` is NOT (lines 481, 495).
- `getConditionalFields(question)` in shared/form.ts returns the question's conditional fields (deduped). Conditional answers flow into `customAnswers` via persistProgress's KNOWN_FIELD_IDS filter.
- `startCron()` in server/cron.ts returns early on `process.env.VERCEL` and `DISABLE_INPROCESS_CRON === "true"`; blog code lazy-imports storage inside ticks. `isAuthorizedCronRequest(req)` in server/routes/_shared.ts checks Bearer CRON_SECRET (permits all in non-production when unset).
- Routes register in `registerIntegrationRoutes(app)` (server/routes/integrations.ts:173); telegram GET/PUT/test pattern at lines 884-975 uses local zod `telegramSettingsSchema` and masks secrets as `'********'`.
- Migrations: `migrations/0053_blog_autopost_openrouter.sql` style (BEGIN/COMMIT, IF NOT EXISTS); runner style `scripts/migrate-skale-hub-ghl-sync.ts` (dotenv/config, `pool` from server/db.js, information_schema verification). DB is the production Supabase pooler; schema additions are approved.
- Shared schema barrel is `shared/schema.ts` (flat `export * from "./schema/<file>.js"` lines). settings.ts imports `pgTable, text, serial, integer, timestamp, boolean, jsonb` from drizzle-orm/pg-core.
- Admin card UI strings are plain English (no t()); public page strings must be t()-wrapped and need pt-BR rows in `translations` (seeded by scripts/seed-barbershop-translations.ts, idempotent).
- GHL is discontinued: do not touch GHL code; ADD step 4 to runLeadPostProcessing, never replace step 2.
- Classification is NOT a reliable "scored form" signal: `form_leads.classificacao` is the PG enum `lead_classificacao` (HOT/WARM/COLD/DISQUALIFIED) and `storage.upsertFormLeadProgress` (server/storage.ts:881-883) stamps `classifyLead(scoreTotal, config.thresholds)` on completion. `classifyLead` (shared/form.ts:259-265) returns "HOT" for `score >= thresholds.hot`, so with the barbershop config (maxScore 0, thresholds all 0) EVERY barbershop lead is stored as `classificacao: "HOT", scoreTotal: 0`. Unscored-ness must be derived from the form config, not the row.
- The progress route's `23505` duplicate-session fallback (server/routes/forms.ts:403-408) responds `res.json(existing)` from `storage.getFormLeadBySession(sessionId)` — no post-processing runs there, so it carries no `bookingUrl` unless Task 2 step 3b adds it.
- Sibling `scripts/verify-xphere-contract.ts` = `node:assert/strict` + a literal fixture + 4 `assert.equal` calls on `xphereLeadEnvelopeSchema.parse/safeParse`, then a console.log summary; run with `npx tsx`. Its fixture contains real sibling tenant data (`mvp`, `mvpbuildergroup.com`) which MUST NOT be copied (memory rule: no hardcoded tenant data from source projects).
</verified_facts>

<interfaces>
<!-- Contracts the executor implements against. Use directly; no exploration needed. -->

From shared/schema/forms.ts:
```typescript
export type FormLead = typeof formLeads.$inferSelect; // fields listed in verified_facts
export interface FormConditionalField { showWhen: string; id: string; title: string; placeholder: string; type?: Exclude<FormQuestionType, 'select'>; }
export interface FormQuestion { id: string; order: number; title: string; type: FormQuestionType; required: boolean; placeholder?: string; options?: {value,label,points}[]; conditionalField?: FormConditionalField; conditionalFields?: FormConditionalField[]; ghlFieldId?: string; }
export interface FormConfig { questions: FormQuestion[]; maxScore: number; thresholds: { hot: number; warm: number; cold: number } }
```

From shared/form.ts:
```typescript
export function getConditionalFields(question: FormQuestion): FormConditionalField[];
```

From server/routes/_shared.ts:
```typescript
export async function requireAdmin(req, res, next): Promise<void>;
export function isAuthorizedCronRequest(req: Request): boolean;
```

From server/storage.ts (pattern to mirror, lines 570-594):
```typescript
async getTelegramSettings(): Promise<TelegramSettings | undefined>  // select first row ordered by id; auto-insert {} when empty
async saveTelegramSettings(settings: InsertTelegramSettings): Promise<TelegramSettings>  // update existing row else insert
```

Sibling contract (port verbatim except product):
```typescript
// shared/xphere-contract.ts — see C:/Users/Vanildo/Dev/skaleclub-websites/shared/xphere-contract.ts
source.product: z.enum(["skaleclub_websites", "skaleclub"])   // <-- only change
```

New module contract (Task 2 implements; Task 3's admin card consumes the routes):
```typescript
// server/integrations/xphere.ts
export type XphereInfo = { organization: { id: string; name: string }; scopes: string[]; capabilities: { lead_ingestion: boolean; lead_schema_versions: string[] } };
export async function validateKey(apiKey: string): Promise<XphereInfo>;
export function isUnscoredForm(formConfig: FormConfig): boolean; // maxScore === 0 || no option has points > 0
export function serializeLeadForXphere(lead: FormLead, formConfig: FormConfig, formSlug: string, tenantRef: string): XphereLeadEnvelope;
export async function enqueueXphereLead(lead: FormLead, formConfig: FormConfig, formSlug?: string): Promise<IntegrationDelivery | null>;
export function queueXphereDeliverySweep(): void;
export async function listXphereDeliveries(limit?: number): Promise<IntegrationDelivery[]>;
export async function retryXphereDelivery(id: string): Promise<IntegrationDelivery>;
export async function reconcileMissingXphereDeliveries(): Promise<void>;
export async function cancelPendingXphereDeliveries(): Promise<void>;
export function buildXphereBookingUrl(lead: FormLead, formConfig: FormConfig, settings: XphereSettings): string | null;
```

Admin API shapes (Task 2 serves, Task 3 consumes):
```typescript
// GET /api/integrations/xphere  and  PUT response
interface XphereSettings {
  enabled: boolean; apiKey: string /* '********' | '' */; keyPrefix: string | null;
  xphereOrgId: string | null; xphereOrgName: string | null; status: string;
  lastValidatedAt: string | null; lastSuccessAt: string | null; lastErrorAt: string | null; lastErrorCode: string | null;
  bookingEnabled: boolean; bookingProfileSlug: string; inPersonEventSlug: string; onlineEventSlug: string;
  visitTypeQuestionId: string; inPersonAnswerValue: string; onlineAnswerValue: string;
  tenantRef: string; // source.tenant_ref on every delivery; default 'skaleclub'
}
// PUT body: Partial of the editable fields incl. tenantRef (apiKey may be '********' = keep existing)
// POST /api/integrations/xphere/test  body { apiKey?: string } -> { success: true, organization: { id, name } } | { success: false, message }
// GET  /api/integrations/xphere/deliveries?limit=20 -> IntegrationDelivery[] (id, status, attemptCount, lastErrorCode, lastErrorMessage, responseStatus, createdAt, deliveredAt, nextAttemptAt, aggregateId)
// POST /api/integrations/xphere/deliveries/:id/retry -> IntegrationDelivery
// POST /api/integrations/xphere/sweep  (Bearer CRON_SECRET) -> { ok: true }
```
</interfaces>
</context>

<execution_notes>
- **Commit after EACH task, not once at the end.** This plan intentionally stays a single plan (the executor has verbatim interfaces plus a working sibling reference), but it touches ~20 files / ~1000 lines. A context failure between tasks must lose nothing: after Task 1's verify passes, commit (`feat(quick-260906-g80): xphere data layer — contract, tables, storage, migration 0054`); after Task 2 (`feat(quick-260906-g80): xphere server module, lead handoff hook, admin/cron routes`); after Task 3 (`feat(quick-260906-g80): xphere admin card, booking CTA, barbershop seeds`). Use `git add` with the explicit file list from each task's `<files>` — never `git add -A` (the working tree already has unrelated untracked files).
- Run `npm run check` before every commit; do not commit a red typecheck.
- If Task 2 or 3 is interrupted, resume from the last commit — Task 1's migration is idempotent and MUST NOT be re-authored.
- `source.product` is deliberately a module constant (`"skaleclub"`) while `source.tenant_ref` is a DB setting: `product` is a closed contract enum with exactly two legal values that identify the sending codebase, so a UI knob would only allow misconfiguration; `tenant_ref` identifies the sending organisation and the scoping doc requires it to be admin-configurable.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Data layer — contract, schema tables, storage accessors, migration 0054 applied</name>
  <files>shared/xphere-contract.ts, scripts/verify-xphere-contract.ts, shared/schema/integrations.ts, shared/schema.ts, server/storage.ts, migrations/0054_xphere_lead_handoff.sql, scripts/migrate-xphere-lead-handoff.ts</files>
  <action>
1. **`shared/xphere-contract.ts`** — copy the sibling file verbatim (`C:/Users/Vanildo/Dev/skaleclub-websites/shared/xphere-contract.ts`) with exactly one change: `product: z.enum(["skaleclub_websites", "skaleclub"])`. Keep `.strict()` everywhere and the exported `XphereLeadEnvelope` type. Add a 3-line header comment: duplicated (not shared) with skaleclub-websites on purpose; drift surfaces as Xphere 422s; keep in sync manually.

2. **`shared/schema/integrations.ts`** (new) — import `pgTable, text, serial, integer, timestamp, boolean, jsonb, uuid, index, uniqueIndex` from `drizzle-orm/pg-core` and `z` from zod. Define:
   - `xphereSettings = pgTable("xphere_settings", {...})` singleton: `id serial PK`, `enabled boolean default false`, `apiKey text("api_key")`, `keyPrefix text("key_prefix")`, `xphereOrgId text("xphere_org_id")`, `xphereOrgName text("xphere_org_name")`, `status text default 'disconnected'`, `lastValidatedAt`, `lastSuccessAt`, `lastErrorAt` (timestamps), `lastErrorCode text`, `bookingEnabled boolean("booking_enabled") default false`, `bookingProfileSlug text("booking_profile_slug")`, `inPersonEventSlug text("in_person_event_slug")`, `onlineEventSlug text("online_event_slug")`, `visitTypeQuestionId text("visit_type_question_id") default 'tipoVisita'`, `inPersonAnswerValue text("in_person_answer_value") default 'presencial'`, `onlineAnswerValue text("online_answer_value") default 'online'`, `tenantRef: text("tenant_ref").notNull().default("skaleclub")` (admin-editable value sent as `source.tenant_ref`; the default is this product's own identifier, not a customer's, so it is allowed), `createdAt`/`updatedAt` defaultNow. NO booking slug / org id defaults anywhere (per CLAUDE.md no-hardcoded-tenant-data rule); slugs are admin-entered.
   - `integrationDeliveries = pgTable("integration_deliveries", {...})` — same columns as the sibling (shared/schema.ts:570-598 in skaleclub-websites) with these deltas: `tenantId: integer("tenant_id").notNull().default(1)` (NO `.references()` — there is no tenants table here), `providerReceiptId: text(...)`, `providerContactId: text(...)`, `payload: jsonb("payload").$type<Record<string, unknown>>().notNull()`. Keep `uniqueIndex("integration_deliveries_aggregate_unique").on(tenantId, provider, eventType, aggregateType, aggregateId)`, `index("integration_deliveries_due_idx").on(status, nextAttemptAt)`, `index("integration_deliveries_tenant_idx").on(tenantId, createdAt)`.
   - `insertXphereSettingsSchema = z.object({...})` manual zod (mirror `insertTelegramSettingsSchema` style in settings.ts:168): all fields optional/nullable except `enabled`/`bookingEnabled` `z.boolean().default(false)` and `tenantRef: z.string().trim().min(1).max(80).optional()`.
   - Types: `XphereSettings`, `InsertXphereSettings`, `IntegrationDelivery` (`$inferSelect`), `InsertIntegrationDelivery` (`$inferInsert`).
   - Append `export * from "./schema/integrations.js";` to `shared/schema.ts`.

3. **`server/storage.ts`** — add `getXphereSettings(): Promise<XphereSettings | undefined>` and `saveXphereSettings(settings: InsertXphereSettings): Promise<XphereSettings>` to `IStorage` (next to the Telegram entries ~line 222) and to `DatabaseStorage` right after `saveResendSettings`, copying the telegram implementation exactly (select first by id, auto-insert `{}` when empty; save = update existing with `updatedAt: new Date()` else insert). Import `xphereSettings` table + the two types from `#shared/schema.js` in the existing import block.

4. **`migrations/0054_xphere_lead_handoff.sql`** — `BEGIN;` ... `COMMIT;` with a header comment referencing quick task 260906-g80. `CREATE TABLE IF NOT EXISTS xphere_settings (...)` matching step 2 column-for-column (snake_case, defaults: `enabled boolean NOT NULL DEFAULT false`, `status text NOT NULL DEFAULT 'disconnected'`, `booking_enabled boolean NOT NULL DEFAULT false`, `visit_type_question_id text NOT NULL DEFAULT 'tipoVisita'`, `in_person_answer_value text NOT NULL DEFAULT 'presencial'`, `online_answer_value text NOT NULL DEFAULT 'online'`, `tenant_ref text NOT NULL DEFAULT 'skaleclub'`, `created_at/updated_at timestamp NOT NULL DEFAULT now()`). `CREATE TABLE IF NOT EXISTS integration_deliveries (...)` = sibling migration `0025_xphere_lead_delivery.sql` body with `tenant_id integer NOT NULL DEFAULT 1` (no REFERENCES), `provider_receipt_id text`, `provider_contact_id text`, `id uuid PRIMARY KEY DEFAULT gen_random_uuid()` (native PG13+, no extension needed), the `integration_deliveries_aggregate_unique UNIQUE (tenant_id, provider, event_type, aggregate_type, aggregate_id)` constraint, and the two `CREATE INDEX IF NOT EXISTS` statements (`_due_idx` on `(status, next_attempt_at)`, `_tenant_idx` on `(tenant_id, created_at DESC)`). Do NOT `CREATE EXTENSION pgcrypto`. Make the Drizzle `notNull()` flags in step 2 match these NOT NULLs.

5. **`scripts/migrate-xphere-lead-handoff.ts`** — clone `scripts/migrate-skale-hub-ghl-sync.ts`: read `migrations/0054_xphere_lead_handoff.sql`, execute, then verify via information_schema that tables `xphere_settings` and `integration_deliveries` exist and columns `xphere_settings.booking_profile_slug`, `xphere_settings.visit_type_question_id`, `xphere_settings.tenant_ref`, `integration_deliveries.idempotency_key`, `integration_deliveries.provider_receipt_id` exist; also verify `pg_indexes` contains `integration_deliveries_due_idx`. Throw if any missing; `pool.end()` in finally.

6. **`scripts/verify-xphere-contract.ts`** (new) — port of the sibling script (`C:/Users/Vanildo/Dev/skaleclub-websites/scripts/verify-xphere-contract.ts`): `import assert from "node:assert/strict"; import { xphereLeadEnvelopeSchema } from "../shared/xphere-contract.js";`. Build a SYNTHETIC fixture — do NOT copy the sibling's tenant values: `schema_version: "1.0"`, `event_id: "skaleclub:barbershop-leads:9d2f1c3e-6a4b-4f0e-8c7d-2b1a0e9f8d7c"`, `occurred_at: "2026-09-06T12:00:00.000Z"`, `source: { product: "skaleclub", tenant_ref: "skaleclub", site_domain: "skale.club", form: "barbershop-leads" }`, `contact: { name: "Test Lead", email: "lead@example.com", phone: "+15555550100" }`, `lead: { status: "new", score: null, classification: null, page_url: "https://skale.club/barbershops", answers: { tipoVisita: "presencial", enderecoBarbearia: "123 Example St" } }`, `attribution: { utm_source: null, utm_medium: null, utm_campaign: null }` (plus whatever other attribution keys the contract requires — mirror the sibling fixture's key set exactly, with `null`/empty values, so the fixture stays schema-valid). Four assertions: (a) `assert.equal(xphereLeadEnvelopeSchema.parse(fixture).event_id, fixture.event_id)`; (b) `assert.equal(xphereLeadEnvelopeSchema.safeParse({ ...fixture, source: { ...fixture.source, product: "skaleclub_websites" } }).success, true)`; (c) `assert.equal(xphereLeadEnvelopeSchema.safeParse({ ...fixture, source: { ...fixture.source, product: "not_a_product" } }).success, false)`; (d) `assert.equal(xphereLeadEnvelopeSchema.safeParse({ ...fixture, org_id: "attacker-org" }).success, false)` (proves `.strict()` at the top level). End with `console.log("Xphere contract verification passed: 4/4 assertions")`. No DB, no env, no network — it must run in CI-like isolation.

7. Run it against the DB: `npx tsx scripts/migrate-xphere-lead-handoff.ts` (uses dotenv/config → POSTGRES_URL from .env). Then run `npm run check` and `npx tsx scripts/verify-xphere-contract.ts`. If the migration fails midway, the BEGIN/COMMIT wrapper rolls back — fix and re-run (idempotent via IF NOT EXISTS). Then commit Task 1 (see execution_notes).
  </action>
  <verify>
    <automated>npx tsx scripts/migrate-xphere-lead-handoff.ts && npm run check && npx tsx scripts/verify-xphere-contract.ts</automated>
  </verify>
  <done>Runner prints "Verified:" for both tables/columns/index (incl. `xphere_settings.tenant_ref`) and exits 0 on a second (idempotent) run; `npm run check` is clean; `scripts/verify-xphere-contract.ts` prints "4/4 assertions" and exits 0, and contains no sibling tenant strings (`grep -ci "mvp" scripts/verify-xphere-contract.ts` == 0); `grep -n 'z.enum(\["skaleclub_websites", "skaleclub"\])' shared/xphere-contract.ts` matches; `grep -n "integrations.js" shared/schema.ts` matches; `grep -n "getXphereSettings\|saveXphereSettings" server/storage.ts` shows interface + implementation; no `references(` in shared/schema/integrations.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Server — integration module, lead-processing hook, admin/cron routes, in-process sweep, .env.example</name>
  <files>server/integrations/xphere.ts, server/lib/lead-processing.ts, server/routes/forms.ts, server/routes/integrations.ts, server/cron.ts, .env.example</files>
  <action>
1. **`server/integrations/xphere.ts`** (new) — port `C:/Users/Vanildo/Dev/skaleclub-websites/server/integrations/xphere.ts` to single-tenant + plaintext key. Imports: `randomUUID` from node:crypto; `and, asc, desc, eq, inArray, lte` from drizzle-orm; `db` from ../db.js; `formLeads, forms, integrationDeliveries, xphereSettings` + types `FormConfig, FormLead, IntegrationDelivery, XphereSettings` from `#shared/schema.js`; `xphereLeadEnvelopeSchema` from `#shared/xphere-contract.js`; `getConditionalFields` from `#shared/form.js`; `storage` from ../storage.js. Constants: `XPHERE_API_BASE = "https://xphere.app/api/v1"`, `XPHERE_BOOK_BASE = "https://xphere.app/book"`, `WORKER_ID = \`skaleclub-${process.pid}-${randomUUID()}\``, `MAX_ATTEMPTS = 8`, `RETRY_DELAYS_MS = [60_000, 300_000, 1_800_000, 7_200_000, 28_800_000, 86_400_000]`, `TENANT_ID = 1`, `PRODUCT = "skaleclub"` (module constant on purpose — closed contract enum identifying this codebase; see execution_notes), `DEFAULT_TENANT_REF = "skaleclub"` (ONLY a fallback when `settings.tenantRef` is empty — the real value comes from `xphere_settings.tenant_ref`; there is NO `TENANT_REF` constant used in payloads), `DEFAULT_SITE_DOMAIN = "skale.club"`, `EMPTY_FORM_CONFIG: FormConfig = { questions: [], maxScore: 0, thresholds: { hot: 0, warm: 0, cold: 0 } }`, `LEAD_CLASSIFICATIONS = new Set(["HOT","WARM","COLD","DISQUALIFIED"])`.
   - `sanitizeError` (redact `/xph_[a-zA-Z0-9]+/g`, slice 300) — apply to EVERY logged/persisted error message.
   - `export async function validateKey(apiKey)` — verbatim from sibling (GET integration-info, 8s timeout, `redirect: "error"`, require organization.id/name + capabilities.lead_ingestion).
   - `leadValue(lead, id)` verbatim. `configuredAnswerIds(formConfig)` = `formConfig.questions.flatMap(q => [q.id, ...getConditionalFields(q).map(f => f.id)])` — conditional field ids MUST be included so `enderecoBarbearia` (Task 3) ships in `answers`. Fallback when the list is empty: the nine native answer columns + `Object.keys(lead.customAnswers ?? {})`, as the sibling does.
   - `export function isUnscoredForm(formConfig)`: `return formConfig.maxScore === 0 || !formConfig.questions.some(q => (q.options ?? []).some(o => o.points > 0));`. Rationale (verified_facts): `storage.upsertFormLeadProgress` stamps `classifyLead(0, {0,0,0})` = "HOT" on every completed barbershop lead, so the DB row cannot tell us whether the form was scored — only the config can.
   - `export function serializeLeadForXphere(lead, formConfig, formSlug, tenantRef)` — as sibling, with: `event_id: \`skaleclub:${formSlug}:${lead.sessionId}\``, `source: { product: PRODUCT, tenant_ref: (tenantRef || DEFAULT_TENANT_REF).trim(), site_domain: <hostname of lead.urlOrigem, else DEFAULT_SITE_DOMAIN>, form: formSlug }`; scoring: `const unscored = isUnscoredForm(formConfig); lead.score: unscored ? null : (lead.scoreTotal ?? null); lead.classification: unscored ? null : (lead.classificacao && LEAD_CLASSIFICATIONS.has(lead.classificacao) ? lead.classificacao : null)` (the enum guard is a secondary safety net; the primary rule is "unscored form ⇒ both null"); `attribution: { utm_source, utm_medium, utm_campaign }` only (no utm_content/utm_term/first_touch/last_touch — columns do not exist here; if the contract requires those keys, send them as `null` / `{ source:null, medium:null, campaign:null }` exactly as the sibling fixture shape demands — never omit a required key). Return `xphereLeadEnvelopeSchema.parse(...)`.
   - `async function resolveFormSlug(lead, hint?)`: return `hint` if given; else if `lead.formId` select `forms.slug` where id; else `"default"`.
   - `export async function enqueueXphereLead(lead, formConfig, formSlug?)`: `const s = await storage.getXphereSettings(); if (!s?.enabled || !s.apiKey) return null;` serialize with resolved slug and `s.tenantRef`; insert into `integrationDeliveries` `{ tenantId: TENANT_ID, provider: "xphere", eventType: "lead.captured", aggregateType: "form_lead", aggregateId: lead.id, idempotencyKey: payload.event_id, payload }` `.onConflictDoNothing().returning()`; if inserted call `queueXphereDeliverySweep()`; return row ?? null.
   - `deliver(row)`: key from `storage.getXphereSettings()`; if missing throw `{ permanent: true, code: "missing_credential" }`; POST `${XPHERE_API_BASE}/leads` with `Authorization: Bearer`, `Content-Type: application/json`, `"Idempotency-Key": row.idempotencyKey`, 10s timeout, `redirect: "error"`; non-ok → throw with `permanent = [400,401,403,409,413,422].includes(status)`, `status`, `code` (body.code or `http_${status}`). 200 and 201 both count as success.
   - `processDelivery(id)`: verbatim claim → deliver → mark `delivered` (set `providerReceiptId`/`providerContactId` from body when string) and update the settings singleton `status: "enabled", lastSuccessAt, lastErrorAt: null, lastErrorCode: null`; on error compute `attempts`, `permanent || attempts >= MAX_ATTEMPTS` → `dead_letter` else `retry`, `nextAttemptAt = now + RETRY_DELAYS_MS[min(attempts-1, len-1)]`, `lastErrorCode` (`timeout` for `TimeoutError`, else `network_error`), `lastErrorMessage: sanitizeError(error)`; settings → `status: "degraded", lastErrorAt, lastErrorCode`. Settings updates go through `storage.saveXphereSettings({...})` or a direct `db.update(xphereSettings)` on the singleton row — either is fine; be consistent.
   - `export function queueXphereDeliverySweep()`: verbatim (release stale `processing` locks older than 5 min → `retry`; process up to 25 due rows sequentially; `sweepRunning` guard; log with sanitizeError).
   - `export async function listXphereDeliveries(limit = 20)`: provider = xphere, order by createdAt desc, `Math.min(limit, 100)`.
   - `export async function retryXphereDelivery(id)`: verbatim minus tenant filter; only `dead_letter` rows; throw "Delivery not found" otherwise; queue sweep.
   - `export async function cancelPendingXphereDeliveries()`: set `status: "cancelled"` where provider = xphere and status in `["pending","retry"]`.
   - `export async function reconcileMissingXphereDeliveries()`: return early unless settings enabled + apiKey; select `formLeads` left-joined to `forms` (select BOTH `forms.slug` and `forms.config`) where `formCompleto = true`, order createdAt desc, limit 500; for each row: `const cfg = (row.formConfig as FormConfig | null) ?? EMPTY_FORM_CONFIG` — the REAL per-form config must be used so `isUnscoredForm` and `configuredAnswerIds` see the actual questions; fall back to `EMPTY_FORM_CONFIG` (fallback ids path, unscored ⇒ null score/classification) ONLY when the form row is missing (`lead.formId` null or the join produced no form). Serialize with `cfg`, `row.formSlug ?? "default"`, `settings.tenantRef`; wrap each row's serialize+insert in try/catch (log with sanitizeError, continue) so one malformed legacy row cannot abort the sweep; insert onConflictDoNothing; finally `queueXphereDeliverySweep()`.
   - `export function buildXphereBookingUrl(lead, formConfig, settings)`: return null unless `settings.bookingEnabled && settings.bookingProfileSlug`; `qid = settings.visitTypeQuestionId || "tipoVisita"`; return null unless `formConfig.questions.some(q => q.id === qid)` (guard: the default form has no visit-type question); `answer = leadValue(lead, qid)`; `eventSlug = answer === (settings.inPersonAnswerValue || "presencial") ? settings.inPersonEventSlug : answer === (settings.onlineAnswerValue || "online") ? settings.onlineEventSlug : null`; return null if falsy; build `new URL(\`${XPHERE_BOOK_BASE}/${encodeURIComponent(profile)}/${encodeURIComponent(eventSlug)}\`)`, `searchParams.set` `name`/`email`/`phone` from `lead.nome/email/telefone` when non-empty (trim, slice(0, 200)); return `url.toString()`. Pure function, never throws (wrap in try/catch → null).

2. **`server/lib/lead-processing.ts`** — import `enqueueXphereLead, buildXphereBookingUrl` from ../integrations/xphere.js. Change `PostProcessResult` to `{ lead: FormLead; bookingUrl: string | null }`. Add an optional 5th param `formSlug?: string`. After step 3 (attribution) add:
   ```
   // 4) Xphere lead handoff + visit booking URL (quick 260906-g80) — best-effort, never throws
   let bookingUrl: string | null = null;
   if (lead.formCompleto) {
     try {
       const xphere = await storage.getXphereSettings();
       if (xphere?.enabled) await enqueueXphereLead(lead, formConfig, formSlug);
       if (xphere) bookingUrl = buildXphereBookingUrl(lead, formConfig, xphere);
     } catch (err) { console.error("[xphere] lead handoff error (non-blocking):", err instanceof Error ? err.message.replace(/xph_[a-zA-Z0-9]+/g, "[REDACTED]") : err); }
   }
   return { lead, bookingUrl };
   ```
   Leave steps 1-3 (including the GHL step) byte-identical. Update the header comment to mention Xphere.

3. **`server/routes/forms.ts`** progress route (~line 384): destructure `{ lead, bookingUrl }`, pass `form.slug` as the 5th arg, respond `res.json(bookingUrl ? { ...lead, bookingUrl } : lead)`. The skale-hub group route keeps destructuring only `{ lead }` (unchanged behaviour).

3b. **`server/routes/forms.ts`** `23505` duplicate-session fallback of the same route (lines ~403-408): today it responds `res.json(existing)` with no `bookingUrl`, so a double-submitted final step would land on the thank-you page without the CTA. Change the branch to:
   ```
   if (existing) {
     if (existing.formCompleto) {
       try {
         const form = await storage.getFormBySlug(req.params.slug);
         const cfg = (form?.config as FormConfig | null) ?? DEFAULT_FORM_CONFIG;
         const xphere = await storage.getXphereSettings();
         const bookingUrl = xphere ? buildXphereBookingUrl(existing, cfg, xphere) : null;
         if (bookingUrl) return res.json({ ...existing, bookingUrl });
       } catch { /* fall through: respond exactly as before */ }
     }
     return res.json(existing);
   }
   ```
   Import `buildXphereBookingUrl` from `../integrations/xphere.js` at the top of forms.ts. Do NOT enqueue a delivery here (the original request already did, or the sweep's reconcile will).

4. **`server/routes/integrations.ts`** — add imports (`insertXphereSettingsSchema` not needed; define a local zod `xphereSettingsSchema` next to `telegramSettingsSchema` with all editable fields `.optional()`: `apiKey z.string().trim()`, `enabled z.boolean()`, `bookingEnabled z.boolean()`, `bookingProfileSlug/inPersonEventSlug/onlineEventSlug/visitTypeQuestionId/inPersonAnswerValue/onlineAnswerValue z.string().trim().max(120)`, `tenantRef z.string().trim().min(1).max(80)`), import `isAuthorizedCronRequest` from ./_shared.js and the module functions from ../integrations/xphere.js. Add a `toXphereResponse(s)` helper returning the `XphereSettings` wire shape from `<interfaces>` (mask apiKey as `'********'` when set, `''` otherwise; coalesce nulls to `''` for the six booking strings, to defaults `tipoVisita`/`presencial`/`online` for the three answer/question fields, and `tenantRef: s?.tenantRef || 'skaleclub'`; ISO strings for dates). Register inside `registerIntegrationRoutes` under a `// Xphere Integration Routes` banner:
   - `GET /api/integrations/xphere` (requireAdmin) → `toXphereResponse(await storage.getXphereSettings())`.
   - `PUT /api/integrations/xphere` (requireAdmin) → parse; load existing; `keyFromRequest = apiKey && apiKey !== '********' ? apiKey : undefined`; if `keyFromRequest`: `info = await validateKey(keyFromRequest)` (400 with message on failure), set `apiKey, keyPrefix: key.slice(0,12), xphereOrgId: info.organization.id, xphereOrgName: info.organization.name, lastValidatedAt: now, lastErrorAt: null, lastErrorCode: null`. `enabled = parsed.enabled ?? existing.enabled`; if `enabled && !(keyFromRequest || existing.apiKey)` → 400 "Add and test an Xphere API key before enabling". Booking: `bookingEnabled = parsed.bookingEnabled ?? existing.bookingEnabled`; if `bookingEnabled && (!profileSlug || (!inPersonEventSlug && !onlineEventSlug))` → 400 "Booking needs a profile slug and at least one event slug". `tenantRef`: when present in the body persist it (already trimmed/min(1) by zod; a blank string is rejected with 400 "Tenant reference cannot be empty" via the zod message), otherwise keep `existing.tenantRef`. `status = enabled ? "enabled" : (existing.xphereOrgId || keyFromRequest ? "connected" : "disconnected")` unless existing status is `degraded` and enabled stays true (keep `degraded`). If `existing.enabled && !enabled` → `await cancelPendingXphereDeliveries()`. Save via `storage.saveXphereSettings`, respond `toXphereResponse(saved)`. ZodError → 400 like telegram.
   - `POST /api/integrations/xphere/test` (requireAdmin) → body `{ apiKey? }`; key = unmasked body key or stored; 400 `{ success:false, message }` if none; `info = await validateKey(key)`; persist `keyPrefix, xphereOrgId, xphereOrgName, lastValidatedAt: now, lastErrorAt: null, lastErrorCode: null, status: existing.enabled ? "enabled" : "connected"` and, when the key came from the body, also `apiKey`; respond `{ success: true, organization: info.organization, message: \`Connected to ${info.organization.name}\` }`; on error respond 400 `{ success: false, message: sanitized }`.
   - `GET /api/integrations/xphere/deliveries` (requireAdmin) → `listXphereDeliveries(Number(req.query.limit) || 20)`; strip `payload` from each row before responding (keeps the admin payload small; payload contains PII).
   - `POST /api/integrations/xphere/deliveries/:id/retry` (requireAdmin) → `retryXphereDelivery(req.params.id)`; 404 on "Delivery not found".
   - `POST /api/integrations/xphere/sweep` — NO requireAdmin; `if (!isAuthorizedCronRequest(req)) return res.status(401).json({ message: "Unauthorized" })`; `await reconcileMissingXphereDeliveries(); queueXphereDeliverySweep(); res.json({ ok: true })`; 500 with sanitized message on error.

5. **`server/cron.ts`** — after the RSS `setInterval`, add:
   ```
   // Xphere delivery sweep (quick 260906-g80): drains retry/pending lead deliveries.
   console.log("[xphere] delivery sweep cron starting — runs every 5 minutes");
   setInterval(() => {
     void import("./integrations/xphere.js")
       .then((m) => m.queueXphereDeliverySweep())
       .catch((err) => console.error("[xphere] sweep cron error:", err));
   }, 5 * 60_000);
   ```
   Keep it after both existing early-return gates so VERCEL / DISABLE_INPROCESS_CRON behaviour is unchanged.

6. **`.env.example`** — under the `CRON_SECRET` block add a comment: the Xphere API key is configured in Admin → Integrations → CRM → Xphere (stored in `xphere_settings`, no env var); when `DISABLE_INPROCESS_CRON=true` schedule `POST /api/integrations/xphere/sweep` with `Authorization: Bearer $CRON_SECRET` every 5 minutes externally. No new env vars.

7. `npm run check` must pass. Smoke: `npx tsx -e "import('./server/integrations/xphere.js').then(m => console.log(Object.keys(m)))"` lists all exported functions (needs POSTGRES_URL in env because db.ts throws without it — run with `--env-file=.env`). The automated verify below also asserts the unscored rule: a lead row with `scoreTotal: 0, classificacao: "HOT"` serialized against the unscored barbershop-style config must yield `lead.score === null && lead.classification === null`, and against a scored config (one option with points 10) must keep `score: 7, classification: "WARM"`. Then commit Task 2 (see execution_notes).
  </action>
  <verify>
    <automated>npm run check && npx tsx --env-file=.env -e "import('./server/integrations/xphere.js').then(m => { const need=['validateKey','serializeLeadForXphere','enqueueXphereLead','queueXphereDeliverySweep','listXphereDeliveries','retryXphereDelivery','reconcileMissingXphereDeliveries','cancelPendingXphereDeliveries','buildXphereBookingUrl']; const missing=need.filter(k=>typeof m[k]!=='function'); if(missing.length){console.error('missing',missing);process.exit(1);} const url=m.buildXphereBookingUrl({nome:'Ana',email:'a@b.c',telefone:'+1555',customAnswers:{tipoVisita:'presencial'}},{questions:[{id:'tipoVisita',order:1,title:'',type:'select',required:true}],maxScore:0,thresholds:{hot:0,warm:0,cold:0}},{bookingEnabled:true,bookingProfileSlug:'p',inPersonEventSlug:'e',onlineEventSlug:null,visitTypeQuestionId:'tipoVisita',inPersonAnswerValue:'presencial',onlineAnswerValue:'online'}); console.log(url); if(url!=='https://xphere.app/book/p/e?name=Ana&email=a%40b.c&phone=%2B1555'){process.exit(1);} const base={id:1,sessionId:'9d2f1c3e-6a4b-4f0e-8c7d-2b1a0e9f8d7c',formId:4,nome:'Ana',email:'a@b.c',telefone:'+1555',urlOrigem:'https://skale.club/barbershops',utmSource:null,utmMedium:null,utmCampaign:null,customAnswers:{tipoVisita:'presencial'},createdAt:new Date(),updatedAt:new Date()}; const unscoredCfg={questions:[{id:'tipoVisita',order:1,title:'',type:'select',required:true,options:[{value:'presencial',label:'',points:0}]}],maxScore:0,thresholds:{hot:0,warm:0,cold:0}}; const scoredCfg={questions:[{id:'tipoVisita',order:1,title:'',type:'select',required:true,options:[{value:'presencial',label:'',points:10}]}],maxScore:10,thresholds:{hot:8,warm:5,cold:2}}; const u=m.serializeLeadForXphere({...base,scoreTotal:0,classificacao:'HOT'},unscoredCfg,'barbershop-leads','skaleclub'); if(u.lead.score!==null||u.lead.classification!==null||u.source.tenant_ref!=='skaleclub'||u.source.product!=='skaleclub'){console.error('unscored rule broken',u.lead,u.source);process.exit(1);} const sc=m.serializeLeadForXphere({...base,scoreTotal:7,classificacao:'WARM'},scoredCfg,'default','acme-ref'); if(sc.lead.score!==7||sc.lead.classification!=='WARM'||sc.source.tenant_ref!=='acme-ref'){console.error('scored rule broken',sc.lead,sc.source);process.exit(1);} console.log('serialize OK'); process.exit(0); })"</automated>
  </verify>
  <done>`npm run check` clean; the smoke script prints the expected booking URL and "serialize OK" and exits 0 (unscored config ⇒ null score/classification; scored config ⇒ real values; tenant_ref comes from the argument, not a constant — `grep -n 'TENANT_REF = ' server/integrations/xphere.ts` matches only `DEFAULT_TENANT_REF`); `grep -n "Idempotency-Key" server/integrations/xphere.ts` matches; `grep -n "buildXphereBookingUrl" server/routes/forms.ts` shows the 23505 fallback wiring; `grep -n "bookingUrl" server/lib/lead-processing.ts server/routes/forms.ts` shows the hook and the additive response; `grep -c "/api/integrations/xphere" server/routes/integrations.ts` >= 5; `grep -n "queueXphereDeliverySweep" server/cron.ts` matches; GHL step in lead-processing.ts unchanged (`git diff server/lib/lead-processing.ts` shows only the header comment, type, param, and step-4 additions).</done>
</task>

<task type="auto">
  <name>Task 3: Client + data — XphereCard, thank-you CTA, booking redirect, barbershop seeds re-run</name>
  <files>client/src/components/admin/shared/types.ts, client/src/components/admin/integrations/XphereCard.tsx, client/src/components/admin/IntegrationsSection.tsx, client/src/components/LeadFormModal.tsx, client/src/pages/LeadThankYou.tsx, scripts/seed-barbershop-landing.ts, scripts/seed-barbershop-translations.ts</files>
  <action>
1. **`client/src/components/admin/shared/types.ts`** — after `GHLSettings` add `export interface XphereSettings { ... }` exactly as in `<interfaces>` (including `tenantRef: string`) and `export interface XphereDelivery { id: string; status: string; attemptCount: number; responseStatus: number | null; lastErrorCode: string | null; lastErrorMessage: string | null; aggregateId: number; createdAt: string; deliveredAt: string | null; nextAttemptAt: string; }`.

2. **`client/src/components/admin/integrations/XphereCard.tsx`** (new) — mirror `GHLCard.tsx` structure (Card rounded-2xl shadow-none, header row with title + enable Switch, `useQuery<XphereSettings>({ queryKey: ['/api/integrations/xphere'] })`, `useEffect` to hydrate local state, `saveSettings` via `apiRequest('PUT', ...)` + `invalidateQueries`, `handleToggleEnabled` refusing to enable unless `testResult === 'success' || settings.xphereOrgId`, `testConnection` POSTing `/api/integrations/xphere/test` with `{ apiKey }` and, on `result.success`, setting `testResult='success'`, calling `saveSettings(settings)` and toasting `Connected to ${result.organization.name}`). Use a lucide icon (e.g. `CalendarCheck`) in the 10x10 box instead of a logo asset. Title "Xphere", subtitle "Send captured leads to Xphere and let them book a visit". Body:
   - **Connection**: API key `Input type="password"` (placeholder `xph_...`, help text "Xphere → Settings → API Keys, scope leads:write"), Test connection button (same tri-state class styling as GHL; disabled when no apiKey), and a status line showing `settings.status` + `xphereOrgName` when present + `lastErrorCode` in red when status is `degraded`.
   - **Visit booking** sub-section (border-t, heading + one-line help "Redirects completed leads to your Xphere booking page based on their visit-type answer"): booking `Switch` (`bookingEnabled`), `Input`s for Booking profile slug, In-person event slug, Online event slug, then a `<details>` element labelled "Advanced" containing Visit-type question id, In-person answer value, Online answer value, and **Tenant reference** (`tenantRef`, `data-testid="input-xphere-tenant-ref"`, help text "Sent to Xphere as source.tenant_ref on every lead. Default: skaleclub"; `placeholder="skaleclub"` is fine — it is this product's own identifier, not a customer's). Placeholders for slugs must be generic (`your-profile`, `in-person-visit`, `online-meeting`) — never a real tenant slug. A "Save booking settings" `Button` calls `saveSettings(settings)`; surface the server's 400 message via the destructive toast.
   - **Recent deliveries**: `useQuery<XphereDelivery[]>({ queryKey: ['/api/integrations/xphere/deliveries?limit=10'], refetchInterval: 30_000 })`; render a compact list (status text colour-coded: delivered green, retry/pending amber, dead_letter red, cancelled muted), `new Date(createdAt).toLocaleString()`, `lastErrorCode` when present, and a small outline "Retry" button only when `status === 'dead_letter'` that POSTs `/api/integrations/xphere/deliveries/${id}/retry` via `apiRequest` then invalidates the deliveries query. Empty state: "No deliveries yet". Admin strings stay plain English (matches GHLCard; no t()). Add `data-testid`s (`input-xphere-api-key`, `button-test-xphere`, `switch-xphere-enabled`, `switch-xphere-booking`, `button-save-xphere-booking`, `input-xphere-tenant-ref`). The "Save booking settings" button also persists `tenantRef` (it is part of the same `saveSettings(settings)` payload). Keep the file under 320 lines.

3. **`client/src/components/admin/IntegrationsSection.tsx`** — import `XphereCard`; change the CRM branch to `{activeTab === 'crm' && (<div className="space-y-6"><XphereCard /><GHLCard /></div>)}` (Xphere ABOVE GHL; GHLCard untouched).

4. **`client/src/components/LeadFormModal.tsx`** — in `handleFinish` (line ~938, inside `if (lead)`), read `const bookingUrl = typeof (lead as FormLead & { bookingUrl?: unknown }).bookingUrl === 'string' ? (lead as { bookingUrl: string }).bookingUrl : null;` and replace the redirect with `window.location.href = bookingUrl ? \`${pagePaths.thankYou}?booking=${encodeURIComponent(bookingUrl)}\` : pagePaths.thankYou;`. Add `booking: Boolean(bookingUrl)` to the existing `trackEvent("form_completed", {...})` payload. Also wrap the two conditional-field placeholders (lines ~481 and ~495) as `placeholder={t(field.placeholder)}` so the new PT placeholder row renders (the title on line 466 is already wrapped). No other changes.

5. **`client/src/pages/LeadThankYou.tsx`** — compute once: `const bookingUrl = useMemo(() => { const raw = new URLSearchParams(window.location.search).get('booking'); return raw && raw.startsWith('https://xphere.app/') ? raw : null; }, []);` (import `useMemo`, and `CalendarCheck` from lucide-react). When `bookingUrl` is set, render ABOVE the existing "Back to website" grid: a full-width primary `<a href={bookingUrl} data-testid="link-book-visit">` styled like the current primary button (`bg-[#5173D6] hover:bg-[#3B5BBE] ... py-3.5 text-lg`) with `CalendarCheck` icon and `{t('Schedule your visit')}`, followed by `<p className="mt-2 text-sm text-slate-300">{t('Pick the day and time that work best for you. It only takes a minute.')}</p>`; and demote "Back to website" to an outline style (`border border-white/20 bg-white/5 hover:bg-white/10`) only in that case. Same-tab navigation (no `target="_blank"`). When `bookingUrl` is null the page renders exactly as today.

6. **`scripts/seed-barbershop-landing.ts`** — on the `tipoVisita` question add `conditionalField: { showWhen: "presencial", id: "enderecoBarbearia", title: "What's your shop address?", placeholder: "Street, number, city" }` and extend the id comment list (`enderecoBarbearia` → customAnswers). Update the header comment block's Xphere note to say the booking redirect now consumes this answer (do not remove the "no date/time question" guidance).

7. **`scripts/seed-barbershop-translations.ts`** — add rows under the tipoVisita section: `{ source: "What's your shop address?", translated: "Qual é o endereço da sua barbearia?" }`, `{ source: "Street, number, city", translated: "Rua, número, cidade" }`; and a new `// ── Thank-you booking CTA` section: `{ source: "Schedule your visit", translated: "Agendar minha visita" }`, `{ source: "Pick the day and time that work best for you. It only takes a minute.", translated: "Escolha o dia e o horário que funcionam melhor para você. Leva só um minuto." }`. The source strings MUST match the t() literals in step 5 byte-for-byte.

8. Re-run both seeds: `npx tsx --env-file=.env scripts/seed-barbershop-landing.ts` then `npx tsx --env-file=.env scripts/seed-barbershop-translations.ts` (both idempotent; expect "Updated existing form (id=4)" and the four new rows reported as inserted). Then `npm run check` and commit Task 3 (see execution_notes).
  </action>
  <verify>
    <automated>npm run check && npx tsx --env-file=.env scripts/seed-barbershop-landing.ts && npx tsx --env-file=.env scripts/seed-barbershop-translations.ts && grep -q "startsWith('https://xphere.app/')" client/src/pages/LeadThankYou.tsx && grep -q "<XphereCard" client/src/components/admin/IntegrationsSection.tsx && grep -q "input-xphere-tenant-ref" client/src/components/admin/integrations/XphereCard.tsx && grep -q "enderecoBarbearia" scripts/seed-barbershop-landing.ts && grep -q "Agendar minha visita" scripts/seed-barbershop-translations.ts</automated>
  </verify>
  <done>`npm run check` clean; both seeds exit 0 (form id=4 updated with the conditional field; 4 new PT rows inserted, idempotent on re-run); CRM tab renders XphereCard above GHLCard; `handleFinish` appends `?booking=` only when the response carries a string `bookingUrl`; LeadThankYou renders the "Schedule your visit" CTA only for `https://xphere.app/` URLs and is otherwise unchanged; no real tenant slug/org id appears in any placeholder, seed, or default (`grep -rn "xphere.app/book/" client scripts shared server | grep -v XPHERE_BOOK_BASE` returns only the guard/comments, never a concrete slug).</done>
</task>

</tasks>

<verification>
- `npm run check` passes after each task; each task is committed separately (three commits).
- `npx tsx scripts/verify-xphere-contract.ts` passes (4/4).
- Migration 0054 applied to the DB and re-runnable; `xphere_settings` (incl. `tenant_ref`) and `integration_deliveries` exist with the documented columns/indexes; no `tenants` FK.
- `GET /api/integrations/xphere` (admin session) returns the masked settings shape; `PUT` with a bogus key returns 400 with a sanitized message; `POST /api/integrations/xphere/sweep` without a Bearer returns 401 in production mode.
- Manual QA (dev server): (1) in Admin → Integrations → CRM paste a real `xph_` key, Test connection shows the org name, enable the switch; (2) set booking profile + event slugs, enable booking, save; (3) complete the barbershop form on `/barbershops` choosing "In-person visit" — the shop address field appears, submission succeeds, redirect lands on `/thankyou?booking=https://xphere.app/book/...` with the CTA; the CTA opens the prefilled Xphere page; (4) Recent deliveries shows the row as `delivered` (or `retry`/`dead_letter` with an error code if the key lacks scope) and Retry works on a dead_letter row; inspect the stored `integration_deliveries.payload` for that barbershop row: `lead.score` and `lead.classification` are `null` and `source.tenant_ref` equals the Advanced field value; (4b) change Tenant reference in Advanced, save, submit another lead — the new payload carries the new value; (5) `/barbershops-br` shows "Qual é o endereço da sua barbearia?" and "Agendar minha visita"; (6) with the integration disabled, form submission and the thank-you page behave exactly as before.
- `git diff server/lib/lead-processing.ts` shows the GHL step untouched.
</verification>

<success_criteria>
- Completed leads are handed to Xphere through a durable, idempotent delivery queue with retry/dead-letter/reconcile semantics equivalent to the skaleclub-websites reference, using `product: 'skaleclub'` and `event_id = skaleclub:<formSlug>:<sessionId>`.
- Lead submission never fails because of Xphere; `xph_` tokens never appear in logs or stored error messages.
- Admin can configure key, enablement, booking slugs and `tenant_ref` entirely from the UI; nothing tenant-specific is hardcoded in code, seeds, or placeholders (`product` is the only code constant, by design).
- Unscored forms never ship a fake `HOT`/`0` — score and classification are `null` for them; scored forms ship real values.
- `scripts/verify-xphere-contract.ts` guards the duplicated contract against drift.
- Barbershop leads choosing in-person/online are redirected to the matching prefilled Xphere booking page via a guarded thank-you CTA; other leads see the unchanged thank-you page.
- Barbershop form asks for the shop address only for in-person visits, in EN and pt-BR.
- Sweep runs in-process every 5 minutes or via the CRON_SECRET-guarded endpoint when in-process cron is disabled.
</success_criteria>

<output>
After completion, create `.planning/quick/260906-g80-xphere-lead-handoff-visit-booking-for-ni/260906-g80-SUMMARY.md` (use the summary template) recording: tables created, exported module surface, route list, the admin settings fields (incl. tenant_ref), the three per-task commit hashes, verify-xphere-contract result, seeds re-run results, and the manual QA outcome.
</output>

---
quick_id: 260906-fu3
type: execute
description: NFC keychain landing + pricing explainer pages (EN+PT)
autonomous: false
files_modified:
  - client/src/components/pages/sections/PricingTableSection.tsx
  - client/src/components/pages/sections/FaqAccordionSection.tsx
  - client/src/components/pages/sections/ContentBlocksSection.tsx
  - client/src/components/pages/sections/ProcessStepperSection.tsx
  - client/src/components/pages/sectionRegistry.ts
  - scripts/seed-nfc-keychains-landing.ts

must_haves:
  truths:
    - "Three new section types (pricingTable, faqAccordion, contentBlocks) resolve in sectionRegistry and render without the DEV 'Unknown section type' fallback"
    - "ProcessStepperSection accepts an optional icons prop and still renders Search/Palette/Code2/Rocket when props is {}"
    - "/websites and /barbershops render byte-identically to today (both pass props: {})"
    - "npm run check (tsc) passes clean"
    - "scripts/seed-nfc-keychains-landing.ts is idempotent: re-running updates the same 5 rows in place"
    - "The seed is NOT run against production until the user explicitly approves"
  artifacts:
    - path: "client/src/components/pages/sections/PricingTableSection.tsx"
      provides: "pricingTable renderer + pricingTablePropsSchema"
    - path: "client/src/components/pages/sections/FaqAccordionSection.tsx"
      provides: "faqAccordion renderer + faqAccordionPropsSchema"
    - path: "client/src/components/pages/sections/ContentBlocksSection.tsx"
      provides: "contentBlocks renderer + contentBlocksPropsSchema"
    - path: "client/src/components/pages/sectionRegistry.ts"
      contains: "pricingTable"
    - path: "scripts/seed-nfc-keychains-landing.ts"
      provides: "nfc-keychain-leads form + 4 pages rows"
  key_links:
    - from: "client/src/components/pages/sectionRegistry.ts"
      to: "sections/PricingTableSection"
      via: "import + registry entry { component, propsSchema }"
    - from: "scripts/seed-nfc-keychains-landing.ts"
      to: "sectionRegistry keys"
      via: "PageSection.type strings must exactly match registry keys"
    - from: "scripts/seed-nfc-keychains-landing.ts"
      to: "forms.slug = 'nfc-keychain-leads'"
      via: "leadFormCta props.formSlug"
---

<objective>
Ship a bilingual NFC-keychain landing pair and a bilingual pricing/how-it-works
explainer pair, plus the three new section renderers the explainer needs, plus an
optional `icons` prop on the existing process stepper.

Purpose: the landing is the ad destination that captures leads; the explainer is
sent by WhatsApp automation after a lead comes in and must close price / process /
timeline / objection questions with no human present.

Output:
- 3 new section components + registry entries
- 1 non-breaking modification to `ProcessStepperSection.tsx`
- 1 idempotent seed script creating the `nfc-keychain-leads` form and 4 `pages` rows
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/quick/260906-fu3-nfc-keychain-landing-pricing-explainer-p/260906-fu3-CONTEXT.md
@client/src/components/pages/sections/ProcessStepperSection.tsx
@client/src/components/pages/sectionRegistry.ts
@client/src/pages/DynamicLanding.tsx
@scripts/seed-barbershop-landing.ts
@shared/schema/forms.ts
@shared/schema/pages.ts
@client/src/components/ui/accordion.tsx

<facts_verified_during_planning>
These were checked against the codebase. Do NOT re-derive them.

1. **No server-side section registry exists.** The comment in `sectionRegistry.ts`
   referencing `shared/landingSectionRegistry` is STALE — a repo-wide grep finds that
   name only inside that comment. `client/src/components/pages/sectionRegistry.ts` is
   the ONLY registration point. `insertPageSchema` accepts `props: z.record(z.unknown())`,
   so the server stores any props bag unvalidated.

2. **DynamicLanding render contract** (`client/src/pages/DynamicLanding.tsx`):
   `entry.propsSchema.safeParse(section.props ?? {})` then `<Component props={parsed.data} />`.
   Consequences: (a) every new props schema MUST accept `{}` (all fields `.optional()`),
   (b) zod objects strip unknown keys by default, (c) a `safeParse` failure renders
   NOTHING in production. So schemas must be permissive enough for the seeded props.

3. **`tipoNegocio` is a NATIVE `form_leads` column, not customAnswers.**
   `shared/form.ts:179` `KNOWN_FIELD_IDS` = [nome, email, telefone, cidadeEstado,
   tipoNegocio, tipoNegocioOutro, tempoNegocio, situacaoMarketing, orcamentoAnuncios,
   principalDesafio, expectativaTempo]. `LeadFormModal.persistProgress` filters those
   OUT of `customAnswers` and maps `tipoNegocio` to `payload.tipoNegocio` →
   `form_leads.tipo_negocio`. This MUST be documented in the seed's ID-mapping comment
   block (the barbershop script documents the equivalent `principalDesafio` case).
   `formLeadProgressSchema.tipoNegocio` is `z.string().max(120)` — keep option `value`
   strings short kebab-case.

4. **`observacoes` goes to customAnswers**, even though a native `observacoes` column
   exists — it is not in `KNOWN_FIELD_IDS` and `persistProgress` never sets it.
   Same behavior as the barbershop seed. Comment it as such.

5. **`phoneCountry` question type exists** (`FormQuestionType` in `shared/schema/forms.ts`)
   and `persistProgress` special-cases it: overrides `payload.telefone` with the
   international number and stashes `customAnswers.countryCode`.

6. **No date/time question type exists.** `FormQuestionType = 'text' | 'textarea' |
   'email' | 'tel' | 'select' | 'voice' | 'phoneCountry'`. This is why `tipoVisita`
   captures meeting FORMAT only.

7. **Reserved slugs**: `nfc-keychains`, `chaveiros-nfc`, `nfc-pricing`, `precos-chaveiros`
   are all absent from `RESERVED_SLUGS`. All four match `slugPattern`
   `/^[a-z0-9]+(?:-[a-z0-9]+)*$/`. Do NOT touch `shared/reservedSlugs.ts`.

8. **Accordion primitives available**: `client/src/components/ui/accordion.tsx` exports
   `Accordion, AccordionItem, AccordionTrigger, AccordionContent` (Radix-based).
</facts_verified_during_planning>

<interfaces>
Contracts the executor needs — use these directly, no exploration required.

```ts
// client/src/components/pages/sectionRegistry.ts
export interface SectionEntry {
  component: ComponentType<{ props: any }>;
  propsSchema: z.ZodTypeAny;
}
export const sectionRegistry: Record<string, SectionEntry>;
```

```ts
// shared/schema/pages.ts
export type PageSection = { type: string; props: Record<string, unknown> };
```

```ts
// shared/schema/forms.ts
export type FormQuestionType = 'text' | 'textarea' | 'email' | 'tel' | 'select' | 'voice' | 'phoneCountry';
export interface FormOption   { value: string; label: string; points: number }
export interface FormQuestion { id: string; order: number; title: string; type: FormQuestionType;
                                required: boolean; placeholder?: string; options?: FormOption[]; }
export interface FormConfig   { questions: FormQuestion[]; maxScore: number;
                                thresholds: { hot: number; warm: number; cold: number } }
```

```ts
// client/src/hooks/useTranslation
const { t } = useTranslation();  // t(englishSource: string) => string
```
</interfaces>

<copy_constraints>
**CONFIRMED PRICING — the ONLY real numbers. Invent nothing else.**
- $10 per piece
- minimum order 20 pieces ⇒ $200 minimum
- $50 one-time art/design fee, FIRST order only, waived from the 2nd order onward
- 100% payment upfront, before production starts

**BANNED in all copy:** any turnaround/lead time in days or weeks, any discount or
promo percentage, any invented testimonial or client name, any invented volume
discount tier, any "X businesses served" style stat, any shipping cost.

**Deliberate omission (flagged):** the explainer's "how long does it take?" FAQ answer
MUST NOT state a number. Write it as: production starts once payment clears, and the
exact production + delivery window is confirmed in writing when the order is approved.
There are therefore ZERO placeholder tokens in the seeded copy. If the user later wants
a published SLA, it is a one-field edit on the `faqAccordion` section in the admin page
editor — no code change needed.

**Real reviews only:** the landing uses the existing `reviews` adapter, which reads
`/api/company-settings`. Do not hand-author testimonials anywhere.

**Language:** ALL copy is authored in ENGLISH. English is the `t()` source language;
`pages.language = 'pt'` drives the Portuguese rendering. Writing Portuguese into props
would leak PT onto the EN pages.

**Audience:** Brazilian business owners living in the USA. Plain, direct, non-corporate
English (it gets machine-translated to pt-BR). Product: custom 3D-printed NFC keychains
— a phone tap opens a link (Google review page, Instagram, digital business card, menu,
or website).
</copy_constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three new section components + optional stepper icons + registry wiring</name>
  <files>
client/src/components/pages/sections/PricingTableSection.tsx (new)
client/src/components/pages/sections/FaqAccordionSection.tsx (new)
client/src/components/pages/sections/ContentBlocksSection.tsx (new)
client/src/components/pages/sections/ProcessStepperSection.tsx (modify)
client/src/components/pages/sectionRegistry.ts (modify)
  </files>
  <action>
Mirror `ProcessStepperSection.tsx` conventions EXACTLY in all three new files:
a top file-header comment naming the section type, `import { z } from "zod"`,
`import { useTranslation } from "@/hooks/useTranslation"`, an exported
`<name>PropsSchema` zod object declared beside the component, an exported
`<Name>Props = z.infer<...>` type, an English `DEFAULTS` const (`as const`),
`props.x ?? DEFAULTS.x` resolution, EVERY user-visible string wrapped in `t()`,
`data-testid` on the `<section>` and on each repeated item, and the same Tailwind
visual language (`container-custom mx-auto px-6`, `py-20 sm:py-24`, zinc palette,
`#1C53A3` accent, `font-display` on headings).

CRITICAL: every field in every props schema must be `.optional()` so `safeParse({})`
succeeds (see fact 2).

**1. `PricingTableSection.tsx` — type `pricingTable`**
```ts
const priceLineSchema = z.object({
  label: z.string(),
  price: z.string(),
  note:  z.string().optional(),
  kind:  z.enum(["one-time", "per-unit", "minimum"]),
});
export const pricingTablePropsSchema = z.object({
  eyebrow:    z.string().optional(),
  heading:    z.string().optional(),
  subheading: z.string().optional(),
  lines:      z.array(priceLineSchema).min(1).optional(),
  footnote:   z.string().optional(),
});
```
Render a bordered white card on a `bg-zinc-50` section: one row per line with
`label` (+ optional `t(note)` in `text-sm text-zinc-500` underneath) on the left and
`price` on the right in `text-2xl font-bold text-zinc-900`. Show a small
`kind` badge (rounded-full, `bg-[#1C53A3]/10 text-[#1C53A3] text-xs`) with `t()`-wrapped
human labels: `one-time` → "One-time", `per-unit` → "Per unit", `minimum` → "Minimum order".
Rows separated by `divide-y divide-zinc-200`. Footnote below the card in
`text-sm text-zinc-600`. `data-testid="section-pricing-table"` on the section and
`data-testid={`pricing-line-${idx + 1}`}` per row.
DEFAULTS carry the confirmed pricing (see copy_constraints) so a bare `props: {}`
still renders correct numbers.

**2. `FaqAccordionSection.tsx` — type `faqAccordion`**
```ts
const faqItemSchema = z.object({ question: z.string(), answer: z.string() });
export const faqAccordionPropsSchema = z.object({
  eyebrow:    z.string().optional(),
  heading:    z.string().optional(),
  subheading: z.string().optional(),
  items:      z.array(faqItemSchema).min(1).optional(),
});
```
Use the existing `Accordion / AccordionItem / AccordionTrigger / AccordionContent`
from `@/components/ui/accordion` with `type="single" collapsible`, `value={`faq-${idx}`}`.
White section background. `data-testid="section-faq-accordion"` +
`data-testid={`faq-item-${idx + 1}`}`. Wrap `t(item.question)` / `t(item.answer)`.
DEFAULTS: 3 generic FAQ entries (no numbers).

**3. `ContentBlocksSection.tsx` — type `contentBlocks`**
```ts
const contentBlockSchema = z.object({
  heading:    z.string(),
  paragraphs: z.array(z.string()),
  bullets:    z.array(z.string()).optional(),
});
export const contentBlocksPropsSchema = z.object({
  eyebrow:    z.string().optional(),
  heading:    z.string().optional(),
  subheading: z.string().optional(),
  blocks:     z.array(contentBlockSchema).min(1).optional(),
});
```
Section header block identical in structure to the stepper's, then a
`max-w-3xl mx-auto space-y-10` stack. Each block: `h3` (`text-xl sm:text-2xl font-bold
font-display text-zinc-900`), paragraphs as `<p className="text-base text-zinc-600
leading-relaxed">`, optional bullets as a `<ul>` with a `Check` lucide icon
(`h-5 w-5 text-[#1C53A3] shrink-0 mt-0.5`) per `<li>`.
`data-testid="section-content-blocks"` + `data-testid={`content-block-${idx + 1}`}`.

**4. Modify `ProcessStepperSection.tsx` — optional `icons` prop (NON-BREAKING)**
Introduce a zod ENUM allowlist (NOT `z.string()`), keyed to a lucide component map:
```ts
export const processStepperIconNames = [
  "Search", "Palette", "Code2", "Rocket",
  "MessageCircle", "PenTool", "Factory", "Truck",
  "Nfc", "Smartphone", "QrCode", "Star",
  "CreditCard", "Package", "Link2", "Check",
] as const;
const ICON_MAP: Record<(typeof processStepperIconNames)[number], LucideIcon> = { /* imported components */ };
```
Add `icons: z.array(z.enum(processStepperIconNames)).length(4).optional()` to
`processStepperPropsSchema`. Resolve with
`const icons = props.icons ? props.icons.map(n => ICON_MAP[n]) : ICONS;` where `ICONS`
is the existing `[Search, Palette, Code2, Rocket]` const. Inside the map use
`const Icon = icons[idx];`. Do NOT change any existing default, class, markup, or
`data-testid`. `/websites` and `/barbershops` pass `props: {}` and MUST render
identically. Type the map with `import type { LucideIcon } from "lucide-react"`.
If any icon name in the allowlist does not exist in lucide-react 0.453.0, `npm run check`
will fail — drop that name from the allowlist rather than shimming it.

**5. Register all three in `client/src/components/pages/sectionRegistry.ts`**
Add the three imports and three entries, aligned with the existing column formatting:
```ts
pricingTable:   { component: PricingTableSection,   propsSchema: pricingTablePropsSchema },
faqAccordion:   { component: FaqAccordionSection,   propsSchema: faqAccordionPropsSchema },
contentBlocks:  { component: ContentBlocksSection,  propsSchema: contentBlocksPropsSchema },
```
Also fix the stale comment above `sectionRegistry` — replace the
"Server-side validation lives in shared/landingSectionRegistry ... must stay in sync"
line with a note that this registry is the only registration point and the server
stores `props` unvalidated.

Do NOT create/modify any other file. Do NOT touch `shared/reservedSlugs.ts`.
  </action>
  <verify>
    <automated>npm run check</automated>
    Additionally confirm by reading back:
    - `grep -n "pricingTable\|faqAccordion\|contentBlocks" client/src/components/pages/sectionRegistry.ts` → 6 hits (3 imports + 3 entries)
    - `grep -n "props.icons" client/src/components/pages/sections/ProcessStepperSection.tsx` → present
    - `grep -c "z.string()" ...` sanity: no `icons` field typed as free-form string
  </verify>
  <done>
`npm run check` exits 0. Three new section files exist, each exporting a component +
props schema whose `safeParse({})` succeeds. `processStepperPropsSchema` has an
optional 4-length enum `icons` array. All three types are keys in `sectionRegistry`.
`ProcessStepperSection` renders the original 4 icons when `props` is `{}`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Author scripts/seed-nfc-keychains-landing.ts (do NOT run it)</name>
  <files>scripts/seed-nfc-keychains-landing.ts (new)</files>
  <action>
Mirror `scripts/seed-barbershop-landing.ts` STRUCTURALLY, section for section:
`import "dotenv/config"`, relative `.js` import specifiers (`../server/db.js`,
`../shared/schema/pages.js`, `../shared/schema/forms.js` — the `#shared/` alias is
unavailable to standalone `tsx`), `// ── Config ──` / `// ── Form questions ──` /
`// ── Landing sections ──` / `// ── Seed runner ──` dividers, `upsertForm()` and
`upsertLanding(spec)` SELECT-then-UPDATE-or-INSERT helpers, `main()` awaiting the
form then each page, `console.log` on each row, `await pool.end()`, and the
`main().catch(...)` tail that logs, closes the pool in a try/catch, and `process.exit(1)`.

Header comment must state the run command
`npx tsx --env-file=.env scripts/seed-nfc-keychains-landing.ts`, note idempotency,
and list the 5 rows it touches.

**Form** — `FORM_SLUG = "nfc-keychain-leads"`, sensible name/description,
`isActive: true`, `isDefault: false`. Config: `maxScore: 0`,
`thresholds: { hot: 0, warm: 0, cold: 0 }`, and `points: 0` on EVERY option.

Include an ID-mapping comment block above the questions array covering:
- all copy is authored in English because English is the `t()` source language;
- `nome` / `email` / `telefone` → native `form_leads` columns;
- **`tipoNegocio` ALSO maps to a native column** (`form_leads.tipo_negocio`) because it
  is in `shared/form.ts` `KNOWN_FIELD_IDS` — `LeadFormModal` filters it out of
  `customAnswers` and sends it as a top-level payload field. Keep its option `value`s
  short kebab-case (`formLeadProgressSchema` caps it at 120 chars);
- `observacoes` falls through to `customAnswers` despite a same-named native column
  existing, because it is not in `KNOWN_FIELD_IDS` and `persistProgress` never sets it;
- every remaining id (`nomeEmpresa`, `quantidadeEstimada`, `objetivoChaveiro`,
  `jaTemLogo`, `prazoDesejado`, `tipoVisita`) → `form_leads.customAnswers` (jsonb);
- the form is UNSCORED, so all leads land as `novo`;
- `telefone` uses `phoneCountry` (Plan 44-03), which also stashes
  `customAnswers.countryCode`.

Questions, in this exact order (`order` 1..11):
| # | id | type | required | notes |
|---|----|------|----------|-------|
| 1 | `nome` | text | yes | full name |
| 2 | `telefone` | phoneCountry | yes | WhatsApp |
| 3 | `email` | email | yes | |
| 4 | `nomeEmpresa` | text | yes | business name |
| 5 | `tipoNegocio` | select | yes | business types relevant to a US-based Brazilian owner: restaurant/food, beauty/barbershop, cleaning/home services, auto services, retail/store, health/fitness, other |
| 6 | `quantidadeEstimada` | select | yes | options ANCHORED on the 20-piece minimum — lowest option must be the minimum itself (e.g. `20` "20 pieces (minimum order)"), then 21-50, 51-100, 100+, plus a "not sure yet" option |
| 7 | `objetivoChaveiro` | select | yes | exactly: Google reviews / Instagram / digital business card (vCard) / menu / website — plus "more than one of these" |
| 8 | `jaTemLogo` | select | yes | drives whether the $50 art fee applies: "Yes, I have my logo in a file" / "I have a logo but not the file" / "No, I need one created". Add an inline comment saying this signals the $50 first-order art fee. Do NOT state the fee in the option labels. |
| 9 | `prazoDesejado` | select | yes | urgency WITHOUT inventing an SLA: "As soon as possible" / "Within this month" / "Next month" / "Just researching for now" |
| 10 | `tipoVisita` | select | yes | meeting FORMAT only — "In-person visit at my business" / "Online meeting (video call)" / "WhatsApp only" |
| 11 | `observacoes` | text | no | optional free text |

Above question 10, reproduce the **Xphere block comment** in the same spirit as the
barbershop script: `tipoVisita` captures MEETING FORMAT only; actual date/slot picking
is a deliberate follow-up owned by Xphere (the separate ops platform at
`C:\Users\Vanildo\Dev\xphere`), which will inject a Calendly-style free-slots picker at
this branch point later; do NOT add a date/time question here because no such form
question type exists.

**Sections** — two arrays, each shared by its EN/PT pair.

`LANDING_SECTIONS` (slugs `nfc-keychains` / `chaveiros-nfc`):
1. `heroWebsites` with explicit English `headline` / `subheadline` / `ctaLabel` props
   about NFC keychains — one tap opens your Google review page, Instagram, digital
   card, menu, or website. No pricing in the hero.
2. `trustBadges` — `props: {}` (adapter, reads `/api/company-settings`)
3. `processStepper` — custom 4 steps for this product AND a custom `icons` array of 4
   names drawn from the Task 1 allowlist (suggested: `["MessageCircle", "PenTool", "Factory", "Truck"]`
   — talk to us / we design your art / we 3D-print and program the tag / you receive and
   start tapping). Step descriptions must contain NO day counts.
4. `reviews` — `props: {}` (adapter; real reviews only)
5. `leadFormCta` with `formSlug: FORM_SLUG` plus heading / subheading / ctaLabel

`PRICING_SECTIONS` (slugs `nfc-pricing` / `precos-chaveiros`):
1. `contentBlocks` — 3 blocks: what an NFC keychain is and what a tap does; where
   businesses use it (counter, reception, car, keyring, table tent); what you get and
   what we need from you (logo file or we create the art). Bullets allowed.
2. `pricingTable` — lines EXACTLY:
   - `{ label: "Per keychain", price: "$10", kind: "per-unit" }`
   - `{ label: "Minimum order", price: "$200", note: "20 pieces × $10", kind: "minimum" }`
   - `{ label: "Art / design fee", price: "$50", note: "First order only — waived from your second order onward", kind: "one-time" }`
   footnote: 100% payment upfront, production starts after payment clears.
3. `processStepper` — same 4 steps + `icons` as the landing (repeat the props; the two
   arrays are independent).
4. `faqAccordion` — 6-8 items answering the objections a WhatsApp lead has with no human
   present: what exactly is an NFC keychain; do customers need an app (no — modern
   iPhone/Android read it natively); why is there a minimum; why the $50 art fee and when
   it is waived; can I change the link later; why 100% upfront; how long does it take
   (**NO number** — production starts once payment clears and the exact production +
   delivery window is confirmed in writing when the order is approved); what if I do not
   have a logo.
5. `leadFormCta` with `formSlug: FORM_SLUG` — CTA framed as "I want to order".

**Page specs** (4 rows), following the barbershop `LandingSpec` shape:
| slug | name | language | alternateSlug | sections |
|------|------|----------|---------------|----------|
| `nfc-keychains` | NFC Keychains (EN) | en | `chaveiros-nfc` | LANDING_SECTIONS |
| `chaveiros-nfc` | NFC Keychains (PT) | pt | `nfc-keychains` | LANDING_SECTIONS |
| `nfc-pricing` | NFC Pricing (EN) | en | `precos-chaveiros` | PRICING_SECTIONS |
| `precos-chaveiros` | NFC Pricing (PT) | pt | `nfc-pricing` | PRICING_SECTIONS |

Since two different sections arrays are involved, extend the barbershop's
`LandingSpec` type with a `sections: PageSection[]` field and have `upsertLanding`
write `spec.sections` (rather than closing over a single module-level const).

Type the arrays as `PageSection[]`, the questions as `FormQuestion[]`, and the config
as `FormConfig`.

**DO NOT RUN THE SCRIPT.** No DB write happens in this task. Do not modify
`scripts/seed-websites-landing.ts` or `scripts/seed-barbershop-landing.ts`.
  </action>
  <verify>
    <automated>npm run check</automated>
    Read-back checks (no DB access):
    - `grep -c "points: 0" scripts/seed-nfc-keychains-landing.ts` equals the total option count; `grep -n "points: [1-9]"` returns nothing
    - `grep -n "maxScore: 0" scripts/seed-nfc-keychains-landing.ts` → present; thresholds all 0
    - every `type:` string in both sections arrays is a key in `sectionRegistry`
      (`hero​Websites, trustBadges, processStepper, reviews, leadFormCta, contentBlocks, pricingTable, faqAccordion`)
    - `grep -nE "[0-9]+ ?(day|days|week|weeks|business day)" scripts/seed-nfc-keychains-landing.ts` returns nothing (no invented SLA)
    - only `$10`, `$200`, `$50` appear as dollar figures
    - `grep -n "Xphere" scripts/seed-nfc-keychains-landing.ts` → block comment present above `tipoVisita`
    - `git status --short` shows exactly one new file for this task
  </verify>
  <done>
`npm run check` exits 0. `scripts/seed-nfc-keychains-landing.ts` exists, compiles, mirrors
the barbershop structure, defines 11 questions in the specified order with zero scoring,
two sections arrays, and 4 landing specs. Nothing has been written to any database.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: GATED — run the seed against the production Supabase DB</name>
  <what-built>
Tasks 1 and 2 are complete and `npm run check` is clean: three new section renderers are
registered, `processStepper` accepts an optional icon allowlist, and
`scripts/seed-nfc-keychains-landing.ts` is authored but has NEVER been executed. The
production database is still untouched.
  </what-built>
  <action>
**HARD GATE — DO NOT RUN ANYTHING BEFORE THE USER SAYS YES.**

This task performs the ONLY write to the live Supabase production database in this quick
task. It inserts/updates 1 `forms` row and 4 `pages` rows.

1. STOP and ask the user, verbatim in substance:
   "Ready to seed production? This will upsert `forms.nfc-keychain-leads` and the 4 pages
   rows (`nfc-keychains`, `chaveiros-nfc`, `nfc-pricing`, `precos-chaveiros`) into the
   live Supabase DB. Approve?"
   Include the exact command you are about to run.
2. WAIT for an explicit affirmative from the user. Silence, an unrelated reply, or any
   hedge is NOT approval. If the user declines or does not answer, stop here and report
   the code as shipped-but-unseeded.
3. Only after explicit approval, run:
   `npx tsx --env-file=.env scripts/seed-nfc-keychains-landing.ts`
4. Report the 5 row ids the script logs.
5. Ask the user to spot-check `/nfc-keychains`, `/chaveiros-nfc`, `/nfc-pricing`,
   `/precos-chaveiros` in the browser, and to confirm `/websites` and `/barbershops` still
   render exactly as before (the stepper regression check).

If the run fails, do NOT retry blindly — report the error, fix the script, and re-ask for
approval before the next attempt. The script is idempotent, so a corrected re-run is safe.
  </action>
  <verify>
    <automated>MISSING — this task is a human-approval gate; there is no automated command that may run before the user approves.</automated>
    After approval and execution: the script prints "Inserted new form" / "Updated existing form"
    plus 4 landing lines and "Done." with a non-zero row id for each, and exits 0.
  </verify>
  <resume-signal>Type "approved" to run the seed, or "skip" to ship the code without seeding.</resume-signal>
  <done>
Either (a) the user explicitly approved and the seed ran successfully, printing 5 row ids
and exiting 0, or (b) the user declined and no database write was attempted.
  </done>
</task>

</tasks>

<verification>
- `npm run check` (tsc, strict) exits 0 after Task 1 and again after Task 2.
- No file outside `files_modified` is changed: `git status --short` lists at most
  4 new files + 2 modified files.
- `shared/reservedSlugs.ts`, `scripts/seed-websites-landing.ts`,
  `scripts/seed-barbershop-landing.ts` are untouched.
- No migration file created; no `shared/schema/*` file modified; no new
  `FormQuestionType` added.
- Regression: `/websites` and `/barbershops` pass `props: {}` to `processStepper` and
  render with the original `[Search, Palette, Code2, Rocket]` icons.
- Only `$10`, `$200`, `$50` appear as prices anywhere; no day/week SLA; no invented
  testimonial or discount.
</verification>

<success_criteria>
1. `pricingTable`, `faqAccordion`, `contentBlocks` are registered section types whose
   props schemas each accept `{}`.
2. `processStepperPropsSchema.icons` is an optional 4-element zod ENUM array (not free
   strings) with a lucide component map, and the existing pages are unaffected.
3. `scripts/seed-nfc-keychains-landing.ts` is idempotent, structurally mirrors the
   barbershop seed, carries the ID-mapping and Xphere comment blocks, and defines the
   11-question unscored `nfc-keychain-leads` form plus 4 bilingual page rows.
4. `npm run check` is clean.
5. The production seed ran only after explicit user approval — or did not run at all.
</success_criteria>

<output>
After completion, create
`.planning/quick/260906-fu3-nfc-keychain-landing-pricing-explainer-p/260906-fu3-SUMMARY.md`
recording: the three new section types, the stepper icon allowlist actually shipped
(after any lucide-availability drops), the 5 seeded row ids (or "not seeded"), and the
deliberate omission of any turnaround-time claim.
</output>
